---
phase: 15-el-horario-entra-al-contrato
plan: 07
type: execute
wave: 6
depends_on: ["15-06"]
files_modified:
  - AGENTS.md
  - .claude/skills/di-nuevo/SKILL.md
  - .claude/skills/di-validar/SKILL.md
autonomous: true
requirements: [REQ-50, REQ-51, REQ-52]

must_haves:
  truths:
    - "El contrato canónico lista bloques e imparte"
    - "La descripción de R6 dice lo que R6 comprueba ahora"
    - "La deuda documental de la Fase 9 queda cerrada: componentes, unidad y total están listados"
    - "Las skills que preguntan el horario preguntan lo que el contrato acepta hoy"
    - "La fase cierra con la suite, las plantillas y la huella en verde"
  artifacts:
    - path: "AGENTS.md"
      provides: "El contrato de curso.yaml y la tabla de las ocho reglas, al día"
      contains: "bloques"
    - path: ".claude/skills/di-nuevo/SKILL.md"
      provides: "El paso 5 del orquestador pregunta bloques, no días sueltos"
      contains: "bloques"
  key_links:
    - from: "AGENTS.md §Contrato de curso.yaml"
      to: "src/modelo.py"
      via: "el contrato escrito coincide con el que carga"
      pattern: "imparte"
---

<objective>
Poner al día el archivo canónico y las dos skills que hablan del horario, y cerrar la fase con las
tres comprobaciones de siempre en verde.

Propósito: `AGENTS.md` es el contrato que lee cualquier agente que trabaje aquí. Si dice que el
horario son «días de sesión» y el modelo ya acepta bloques con hora, el siguiente agente escribirá
un `curso.yaml` que no carga. De paso se cierra la deuda documental que la Fase 9 dejó abierta y que
esta fase hereda por tocar el mismo párrafo.
Salida: `AGENTS.md`, las dos skills, y el cierre verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/15-el-horario-entra-al-contrato/15-CONTEXT.md
@.claude/skills/di-nuevo/SKILL.md
@.claude/skills/di-validar/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: El contrato canónico dice lo que el modelo acepta</name>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md` líneas 84-108 (§«Arquitectura», las cuatro decisiones de diseño)
    - `AGENTS.md` líneas 142-181 (§«Contrato de `curso.yaml`»)
    - `AGENTS.md` líneas 182-196 (§«Las ocho reglas de validación»)
    - `AGENTS.md` líneas 302-320 (§«Comandos»)
    - `src/modelo.py` — `Bloque`, `Horario`, `Grupo` tal como quedaron
  </read_first>
  <action>
Cuatro ediciones en `AGENTS.md`, con los valores exactos:

**1. §«Contrato de `curso.yaml`», bloque `grupos:` (líneas 161-162).** Sustituye:

```yaml
grupos:          [{numero, horario:{dias_presencial, dia_entrega, hora_entrega, aula},
                   jefe_grupo, plataforma}]     # o la forma corta: ["961", "962"]
```
por:
```yaml
grupos:          [{numero, imparte, jefe_grupo, plataforma,
                   horario:{bloques:[{dia, inicio, fin, ambiente}],
                            dias_presencial, dia_entrega, hora_entrega, aula}}]
                                             # o la forma corta: ["961", "962"]
```

y añade, después del párrafo de `rubrica` (línea 178-180), este bloque de prosa:

> **El horario del grupo se declara por bloques.** Cada `bloque` es una franja de clase con `dia`
> (0=lunes … 5=sábado), `inicio` y `fin` en `"HH:MM"`, y `ambiente` `presencial` o `virtual`. Un
> bloque no es una `Sesion`: la sesión es un tramo de la actividad de una meta y el bloque es una
> franja del horario del grupo; comparten vocabulario de ambiente y nada más. Con bloques,
> `dias_presencial` **se deriva** de los presenciales, ordenado y sin repetir, y declarar los dos a
> la vez es error de esquema: el contrato no admite dos verdades sobre el mismo hecho. Un grupo que
> solo declara `dias_presencial` sigue cargando igual y con el comportamiento de siempre. La fecha
> de la sesión presencial la fija el **primer** día presencial de la semana; `Sesion.dia` es el
> escape declarado para la sesión que deba caer en otro. El bloque virtual entra al contrato, al
> manifiesto y al grafo, y **no toca el `.docx`**: la entrega sigue venciendo en `dia_entrega`.
>
> **`imparte: false`** dice que el grupo está declarado y este ciclo no se imparte: conserva su
> aula, su plataforma y su número para cuando vuelva, y `generar.py` lo salta salvo que se le pida
> por su número o con `--incluir-no-impartidos`. El horario del semestre decide quién va; el
> `curso.yaml` lo declara en vez de que el código lo adivine.

**2. La misma sección, deuda de la Fase 9** —esta fase toca el contrato, así que le toca cerrarla—:
en `metas:` (líneas 153-155) añade `componentes` después de `sesiones`, y en `rubros:` (línea 159)
añade `unidad, total` después de `porcentaje`. Deja las dos listas así:

```yaml
metas:           [{id, unidad, semanas, valor, rubro, tipo, enunciado,
                   sesiones, componentes, evidencias, criterios_evaluacion, reflexion,
                   cubre_temas, practica_pua, caracter, que_voy_a_aprender}]
...
                  rubros:[{id, etiqueta, porcentaje, unidad, total, detalle, parciales}]}
```

y una línea de prosa junto al párrafo de `exencion_contra`:

> `componentes` son los aportes que viven dentro de la actividad de una meta —un examen parcial,
> un proyecto— cada uno con su tipo y su evidencia; cuentan para el rubro igual que la meta.
> `unidad: puntos` y `total` en un rubro dicen que ese rubro se califica en puntos y cuántos vale;
> ausentes, el rubro va en porcentaje.

**3. §«Las ocho reglas de validación», fila R6 (línea 191).** Sustitúyela por:

```
| R6 | Ninguna entrega cae en suspensión ni después del fin de cursos. Y si el grupo declara horario: sus horas están bien escritas, ningún bloque acaba antes de empezar, dos bloques del mismo día no se solapan, y una semana cuya suspensión deja al grupo sin ningún día de clase se avisa en vez de imprimirse en silencio. | Calendario |
```

La tabla sigue teniendo **ocho** filas: R6 se amplía, no nace una R9. Es el mismo trato que la
Fase 12 dio a la rúbrica dentro de R2.

**4. §«Arquitectura», decisión de diseño 1 (líneas 86-90) y §«Comandos» (línea 311-313).** En la
decisión 1, donde dice «Cada entrada de `grupos:` lleva su `horario` (días de sesión, día y hora de
entrega, aula)», escribe «lleva su `horario` (los bloques de clase con su día, sus horas y su
ambiente; día y hora de entrega; aula)». En §«Comandos», añade la bandera a la línea de
`generar.py`:

```
                                                       #   [--incluir-no-impartidos]
```
  </action>
  <acceptance_criteria>
    - `grep -n "bloques:\[{dia, inicio, fin, ambiente}\]" AGENTS.md` devuelve una línea
    - `grep -n "imparte" AGENTS.md` devuelve al menos dos líneas
    - `grep -n "componentes" AGENTS.md` devuelve una línea dentro del bloque `metas:` del contrato
    - `grep -n "porcentaje, unidad, total" AGENTS.md` devuelve una línea
    - `grep -c "^| R[1-8] " AGENTS.md` sigue devolviendo 8
    - `grep -n "incluir-no-impartidos" AGENTS.md` devuelve una línea
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^| R[1-8] " AGENTS.md</automated>
  </verify>
  <done>El contrato canónico describe el horario, el grupo no impartido y R6 tal como son, y la deuda de la Fase 9 queda cerrada.</done>
</task>

<task type="auto">
  <name>Tarea 2: Las skills preguntan lo que el contrato acepta</name>
  <files>.claude/skills/di-nuevo/SKILL.md, .claude/skills/di-validar/SKILL.md</files>
  <read_first>
    - `.claude/skills/di-nuevo/SKILL.md` líneas 60-90 (los seis pasos) y 111-125 (§«Generar»)
    - `.claude/skills/di-validar/SKILL.md` líneas 30-45 (la tabla de las ocho reglas)
  </read_first>
  <action>
**1. `di-nuevo/SKILL.md`, paso 5 (líneas 80-82).** Sustituye el párrafo por:

> 5. **Grupos y horario.** Uno o varios. Por cada grupo: sus **bloques de clase** —día, hora de
>    inicio, hora de fin y si es en salón (`presencial`) o `virtual`—, día y hora de entrega, aula,
>    jefe de grupo y plataforma. Los días y las horas se **teclean**, no se ofrecen en opción
>    múltiple: salen de la carga académica del semestre (`horarios/<ciclo>.md` si existe).
>    **El horario vive en el grupo**: si dos grupos difieren en días, difieren todas sus fechas. La
>    fecha de clase la fija el **primer** día presencial de la semana; un bloque virtual no cuenta
>    como día de clase y las entregas siguen venciendo el sábado. Si un grupo está declarado pero
>    este ciclo no se imparte, se marca `imparte: false` en vez de borrarlo: conserva sus datos
>    para cuando vuelva. Un documento por grupo que se imparte.

**2. `di-nuevo/SKILL.md`, §«Generar» (después del párrafo de `--grupo`, línea 120-122).** Añade:

> Un grupo con `imparte: false` no se genera en una corrida normal. `--incluir-no-impartidos` lo
> incluye, y pedirlo por su número con `--grupo` también.

Actualiza además la línea de uso del comando de la línea 117 para que muestre la bandera.

**3. `di-validar/SKILL.md`, fila R6 de la tabla de las ocho reglas.** Sustitúyela por la misma
redacción que quedó en `AGENTS.md`:

```
| R6 | Ninguna entrega cae en día de suspensión ni después del fin de cursos. Y si el grupo declara horario: sus horas están bien escritas, ningún bloque acaba antes de empezar, dos bloques del mismo día no se solapan, y una semana cuya suspensión deja al grupo sin ningún día de clase se avisa en vez de imprimirse en silencio. | Calendario oficial |
```

Las dos skills siguen teniendo ocho reglas y seis pasos: esto no cambia su forma, solo lo que dicen
del horario.
  </action>
  <acceptance_criteria>
    - `grep -n "bloques de clase" .claude/skills/di-nuevo/SKILL.md` devuelve una línea
    - `grep -n "imparte: false" .claude/skills/di-nuevo/SKILL.md` devuelve al menos una línea
    - `grep -c "incluir-no-impartidos" .claude/skills/di-nuevo/SKILL.md` devuelve al menos 2
    - `grep -n "se solapan" .claude/skills/di-validar/SKILL.md` devuelve una línea
    - `grep -c "^| R[1-8] " .claude/skills/di-validar/SKILL.md` sigue devolviendo 8
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^| R[1-8] " .claude/skills/di-validar/SKILL.md</automated>
  </verify>
  <done>Las dos skills describen el horario por bloques y el grupo no impartido.</done>
</task>

<task type="auto">
  <name>Tarea 3: Cerrar la fase con las cuatro comprobaciones</name>
  <files>(ninguno — verificación de cierre)</files>
  <read_first>
    - `.planning/phases/15-el-horario-entra-al-contrato/15-VALIDATION.md` §«Firma de validación»
    - `.planning/ROADMAP.md` §«Fase 15» — los seis criterios de éxito
  </read_first>
  <action>
Corre las cuatro y transcribe su salida al SUMMARY:

```bash
python -X utf8 -m unittest discover -s pruebas
python src/plantillas.py verificar
python -X utf8 src/huella.py verificar
python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml
```

Después contrasta uno por uno los seis criterios de éxito del roadmap y escribe en el SUMMARY, para
cada uno, con qué salida concreta queda demostrado. Dos matices que hay que declarar, no esconder:

- **Criterio 3**: el tercer error no es el que el roadmap enunciaba —«un día declarado con hora no
  está en `dias_presencial`»—, porque D-01 lo volvió imposible al derivar una lista de la otra. Es
  el **solapamiento** (D-05).
- **Criterio 6**: dos pruebas de `PaqueteDeBigData` cambiaron de expectativa —ya no salen dos
  documentos de 39056 por defecto— porque 962 quedó declarado como no impartido. Es la consecuencia
  buscada del criterio 5, no una regresión, y `ElGrupoQueNoSeImparteNoSeGenera` cubre las dos ramas.

Anota también el número de pruebas final frente a las 283 de partida.

Commitea a mano la documentación de este plan, listando los archivos:

```bash
git add AGENTS.md .claude/skills/di-nuevo/SKILL.md .claude/skills/di-validar/SKILL.md
git commit -m "docs(15): el contrato dice que el horario son bloques"
```
  </action>
  <acceptance_criteria>
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y con más pruebas que las 283 de partida
    - `python src/plantillas.py verificar` sale con código 0
    - `python -X utf8 src/huella.py verificar` imprime `Todo intacto. 4 documentos comparados.`
    - `python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` sale con código 0
    - El SUMMARY contrasta los seis criterios del roadmap con la salida concreta que los demuestra
    - `git status --short` queda vacío
  </acceptance_criteria>
  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <done>La fase cierra con la suite, las plantillas y la huella en verde, y con los seis criterios contrastados.</done>
</task>

</tasks>

<verification>
1. `python -X utf8 -m unittest discover -s pruebas` — 0 fallos.
2. `python src/plantillas.py verificar` — en verde.
3. `python -X utf8 src/huella.py verificar` — `Todo intacto. 4 documentos comparados.`
4. `git diff --name-only HEAD~1` no incluye nada bajo `referencias/` ni `src/render_docx.py`.
</verification>

<success_criteria>
- `AGENTS.md` lista `bloques:` e `imparte:` en el contrato y describe R6 tal como es.
- La deuda documental de la Fase 9 (`componentes`, `unidad`, `total`) queda cerrada.
- Las dos skills describen el horario por bloques y el grupo no impartido.
- Los seis criterios del roadmap quedan contrastados con salida concreta, con sus dos matices
  declarados.
</success_criteria>

<output>
Al terminar, escribe `.planning/phases/15-el-horario-entra-al-contrato/15-07-SUMMARY.md`.
</output>
