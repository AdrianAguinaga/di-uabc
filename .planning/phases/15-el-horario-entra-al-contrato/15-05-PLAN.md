---
phase: 15-el-horario-entra-al-contrato
plan: 05
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03", "15-04"]
files_modified:
  - cursos/2026-2/39056-big-data/curso.yaml
  - cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml
  - pruebas/test_generar.py
  - grafo/grafo.json
  - grafo/index.html
  - grafo/AUDITORIA.md
  - .planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md
autonomous: false
requirements: [REQ-52]

must_haves:
  truths:
    - "Los cuatro grupos declaran los días y horas reales de horarios/2026-2.md"
    - "39056·962 queda declarado como no impartido, con su horario intacto y su porqué escrito"
    - "El cambio de huella queda medido campo por campo antes de aceptarse"
    - "El informe anticipa las cuatro líneas de huella movidas, incluida la de 962 por acoplamiento"
    - "El .docx de 962 sale byte por byte igual"
  artifacts:
    - path: "cursos/2026-2/39056-big-data/curso.yaml"
      provides: "Los cuatro bloques reales de 961 e imparte: false en 962"
      contains: "bloques:"
    - path: "cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml"
      provides: "Los bloques reales de 971 y 972, con sus tramos virtuales"
      contains: "ambiente: virtual"
    - path: ".planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md"
      provides: "La medición fecha por fecha y campo por campo, escrita ANTES de registrar"
      contains: "manifiesto"
  key_links:
    - from: "cursos/2026-2/*/curso.yaml"
      to: "src/huella.py verificar"
      via: "el diff medido contra el informe escrito antes"
      pattern: "bloques:"
---

<objective>
Que los cuatro grupos digan por fin la verdad sobre sus días, y que el cambio de huella que eso
provoca quede **medido antes de aceptarse**.

Propósito: todas las fechas de todos los documentos generados hasta hoy salen de un
`dias_presencial` de relleno —dos grupos dicen martes, dos dicen jueves— que no coincide con la
carga académica real. REQ-52 es la única excepción a REQ-48 del proyecto y está acotada a estos dos
cursos: se mide, se justifica por documento y se re-registra en un commit propio (el plan 15-06).
Salida: los dos `curso.yaml` con su horario real y `15-MEDICION-HUELLA.md`, escrito **antes** de
correr `huella registrar`.

**El orden es el contrato de la fase.** Este plan hace los pasos 4 a 8 de la secuencia de huella de
`15-VALIDATION.md`; el paso 9 —aceptar el cambio— es del plan 15-06 y no se toca aquí.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/15-el-horario-entra-al-contrato/15-CONTEXT.md
@.planning/phases/15-el-horario-entra-al-contrato/15-VALIDATION.md
@horarios/2026-2.md
@cursos/2026-2/39056-big-data/curso.yaml
@cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Los cuatro grupos declaran su horario real</name>
  <files>cursos/2026-2/39056-big-data/curso.yaml, cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml, pruebas/test_generar.py</files>
  <read_first>
    - `cursos/2026-2/39056-big-data/curso.yaml` líneas 195-205 (el bloque `grupos:` actual)
    - `cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` líneas 180-190 (ídem)
    - `horarios/2026-2.md` §«Clases del semestre» — la fuente de la que se copian días y horas
    - `pruebas/test_generar.py` líneas 45-84 (`PaqueteDeBigData`: `test_un_documento_por_grupo` y `test_la_traza_reporta_cada_paso`)
  </read_first>
  <action>
**1. `cursos/2026-2/39056-big-data/curso.yaml`.** Sustituye el bloque `grupos:` (líneas 197-203)
por exactamente esto:

```yaml
grupos:
  - numero: "961"
    horario:
      bloques:
        - {dia: 0, inicio: "12:00", fin: "13:00", ambiente: presencial}
        - {dia: 1, inicio: "12:00", fin: "13:00", ambiente: presencial}
        - {dia: 1, inicio: "16:00", fin: "17:00", ambiente: presencial}
        - {dia: 2, inicio: "11:00", fin: "13:00", ambiente: presencial}
      dia_entrega: 5
      hora_entrega: "23:59"
      aula: "Laboratorio de cómputo"
    plataforma: Blackboard
  # 962 no se imparte en 2026-2: no aparece en la carga académica del semestre. Se conserva su
  # horario heredado —jueves— porque no hay día real con qué sustituirlo, y porque 962 es uno de
  # los cuatro documentos de control de pruebas/huellas.yaml: es el testigo de que la corrección
  # de días está acotada a los grupos que sí van. El porqué va aquí y no en avisos: un aviso
  # entra al informe de validación, que es por curso, y movería el hash del informe de 961.
  - numero: "962"
    imparte: false
    horario: {dias_presencial: [3], dia_entrega: 5, hora_entrega: "23:59", aula: "Laboratorio de cómputo"}
    plataforma: Blackboard
```

`dias_presencial` de 961 **desaparece**: declararlo junto a `bloques` es `ErrorModelo` (D-02), y el
derivado sale `[0, 1, 2]` de los bloques presenciales. El de 962 **se queda tal cual**: no declara
bloques, así que conserva el comportamiento de siempre y su `.docx` sale byte por byte igual.

**2. `cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml`.** Sustituye el bloque `grupos:`
(líneas 182-188) por:

```yaml
grupos:
  - numero: "971"
    horario:
      bloques:
        - {dia: 0, inicio: "10:00", fin: "12:00", ambiente: presencial}
        - {dia: 1, inicio: "17:00", fin: "19:00", ambiente: virtual}
      dia_entrega: 5
      hora_entrega: "23:59"
      aula: "Laboratorio de cómputo"
    plataforma: Blackboard
  - numero: "972"
    horario:
      bloques:
        - {dia: 1, inicio: "10:00", fin: "12:00", ambiente: virtual}
        - {dia: 2, inicio: "18:00", fin: "19:00", ambiente: presencial}
      dia_entrega: 5
      hora_entrega: "23:59"
      aula: "Laboratorio de cómputo"
    plataforma: Blackboard
```

Derivados: 971 → `[0]` (el martes es virtual y no es día de clase), 972 → `[2]`. Es lo que mueve
sus fechas de martes a lunes y de jueves a miércoles.

**3. Dos pruebas de `PaqueteDeBigData` dejan de ser ciertas y hay que actualizarlas.** Copian el
`curso.yaml` real de 39056 y llaman `generar.paquete(ruta, pdf=False)` **sin** `grupos=`, así que
con `imparte: false` en 962 ya no salen dos documentos. Es la consecuencia buscada de D-09/D-12, no
una regresión: déjalo escrito en el docstring.

- `test_un_documento_por_grupo` (líneas 64-68) pasa a esperar
  `["DI-2026-2-39056-961.docx"]` y se renombra a `test_un_documento_por_grupo_que_se_imparte`,
  con el docstring: «962 está declarado y no se imparte este ciclo (D-09): el manifiesto lo lista,
  la corrida no lo genera. `ElGrupoQueNoSeImparteNoSeGenera` cubre las dos ramas.»
- `test_la_traza_reporta_cada_paso` (líneas 79-84) pasa a esperar las etiquetas
  `["validación", "parcial", "grupo 961", "MANIFIESTO"]`, y la aserción
  `all(p[0] == "✓" …)` se sustituye por: el paso `parcial` va con `!` y los demás con `✓`.

No toques `test_el_manifiesto_lista_los_dos_grupos_con_su_horario` (líneas 119-125): el manifiesto
sigue listando los dos grupos —itera `curso.grupos` completo— y `[0, 1, 2] != [3]` sigue siendo
cierto.

**No corras `huella registrar` en esta tarea.** El paso 9 de la secuencia es del plan 15-06.
  </action>
  <acceptance_criteria>
    - `grep -c "bloques:" cursos/2026-2/39056-big-data/curso.yaml` devuelve 1 y `grep -c "bloques:" cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` devuelve 2
    - `grep -n "imparte: false" cursos/2026-2/39056-big-data/curso.yaml` devuelve una línea
    - `grep -n "dias_presencial" cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` no devuelve ninguna línea
    - `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sale con código 0
    - `python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` sale con código 0 y su salida contiene tres avisos con `bloque presencial` — semanas 13 y 15 del 971 y semana 6 del 972
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0
  </acceptance_criteria>
  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <done>Los cuatro grupos declaran lo que dice el horario del semestre y la suite sigue en verde.</done>
</task>

<task type="auto">
  <name>Tarea 2: Medir el cambio y escribirlo, antes de aceptarlo</name>
  <files>grafo/grafo.json, grafo/index.html, grafo/AUDITORIA.md, .planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md</files>
  <read_first>
    - `.planning/phases/15-el-horario-entra-al-contrato/15-VALIDATION.md` §«Secuencia de huella» (pasos 6, 7 y 8)
    - `.planning/phases/15-el-horario-entra-al-contrato/15-CONTEXT.md` §D-11, §D-14, §D-16
    - `src/huella.py` líneas 181-226 (`CAMPOS`, `QUE_CAMBIO`, qué imprime `verificar`)
    - `.planning/phases/09-valor-de-una-meta/09-05-SUMMARY.md` si existe — el precedente de una medición de huella deliberada
  </read_first>
  <action>
En este orden, sin saltarte ninguno:

**1.** `python src/grafo.py` — el grafo sigue al `curso.yaml` (precedente D-24 de la Fase 9).
Anota los nodos y aristas que reporta; el grafo no entra en la huella, se revisa por `git diff`.

**2.** `python -X utf8 src/huella.py verificar` — **va a fallar, y eso es el objetivo de la fase**.
Guarda su salida literal.

**3.** `git diff --stat cursos/` y `git diff cursos/` — confirma que el diff es solo el bloque
`grupos:` de los dos `curso.yaml`.

**4.** Escribe `.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md`. Tiene tres
secciones obligatorias.

**(a) Qué campo de huella se mueve, documento por documento.** Es la única forma de distinguir los
tres cambios deliberados del cuarto, que se mueve por acoplamiento (D-16). La predicción medida es:

| Documento | `texto_docx` | `informe` | `manifiesto` | Por qué |
|---|---|---|---|---|
| 39056 · 961 | **cambia** | intacto | **cambia** | martes → lunes; el manifiesto gana `bloques` |
| 39056 · 962 | intacto | intacto | **cambia** | su horario no se toca; `MANIFIESTO.yaml` es un archivo **por curso** y `huella.py` aplica su hash a los dos grupos (`:147`, `:171`) |
| 39062 · 971 | **cambia** | **cambia** | **cambia** | martes → lunes; el informe gana los avisos de D-15 y es por curso |
| 39062 · 972 | **cambia** | **cambia** | **cambia** | jueves → miércoles; comparte informe con 971 |

De ahí que `huella verificar` reporte **cuatro documentos** tocados y no tres, y que `huella
registrar` imprima **cuatro líneas con `!`**. Escríbelo explícitamente en el informe: sin esa frase,
quien lo lea después leerá la cuarta línea como un defecto de implementación.

Lo que D-10 promete y **se sostiene**: el `.docx` de 962 sale byte por byte igual — `texto_docx`
intacto — y su informe tampoco se mueve. El testimonio de que la excepción está acotada es de dos
campos de tres, no de tres de tres.

**(b) Fecha por fecha, grupo por grupo.** Estas tablas están calculadas contra
`calendarios/2026-2.yaml` real. Cópialas y **compruébalas** contra el `.docx` regenerado; si alguna
no coincide, para y averigua por qué antes de seguir.

39056 · 961 — martes → lunes (14 de 16 semanas se mueven):

| Sem | Antes | Ahora | | Sem | Antes | Ahora |
|---|---|---|---|---|---|---|
| 1 | 11 ago | 10 ago | | 9 | 6 oct | 5 oct |
| 2 | 18 ago | 17 ago | | 10 | 13 oct | 12 oct |
| 3 | 25 ago | 24 ago | | 11 | 20 oct | 19 oct |
| 4 | 1 sep | 31 ago | | 12 | 27 oct | 26 oct |
| 5 | 8 sep | 7 sep | | 13 | 3 nov | **3 nov (igual)** |
| 6 | 15 sep | 14 sep | | 14 | 10 nov | 9 nov |
| 7 | 22 sep | 21 sep | | 15 | 17 nov | **17 nov (igual)** |
| 8 | 29 sep | 28 sep | | 16 | 24 nov | 23 nov |

Las semanas 13 y 15 **no se mueven**: el lunes está suspendido (2 y 16 nov) y el recorrido de D-07
lleva la clase al martes, que es justo el día que 961 declaraba antes. Coincidencia medida, no
diseño.

39062 · 971 — martes → lunes (las 16 se mueven):

| Sem | Antes | Ahora | | Sem | Antes | Ahora |
|---|---|---|---|---|---|---|
| 1 | 11 ago | 10 ago | | 9 | 6 oct | 5 oct |
| 2 | 18 ago | 17 ago | | 10 | 13 oct | 12 oct |
| 3 | 25 ago | 24 ago | | 11 | 20 oct | 19 oct |
| 4 | 1 sep | 31 ago | | 12 | 27 oct | 26 oct |
| 5 | 8 sep | 7 sep | | 13 | 3 nov | **2 nov — suspensión** |
| 6 | 15 sep | 14 sep | | 14 | 10 nov | 9 nov |
| 7 | 22 sep | 21 sep | | 15 | 17 nov | **16 nov — suspensión** |
| 8 | 29 sep | 28 sep | | 16 | 24 nov | 23 nov |

39062 · 972 — jueves → miércoles (las 16 se mueven):

| Sem | Antes | Ahora | | Sem | Antes | Ahora |
|---|---|---|---|---|---|---|
| 1 | 13 ago | 12 ago | | 9 | 8 oct | 7 oct |
| 2 | 20 ago | 19 ago | | 10 | 15 oct | 14 oct |
| 3 | 27 ago | 26 ago | | 11 | 22 oct | 21 oct |
| 4 | 3 sep | 2 sep | | 12 | 29 oct | 28 oct |
| 5 | 10 sep | 9 sep | | 13 | 5 nov | 4 nov |
| 6 | 17 sep | **16 sep — suspensión** | | 14 | 12 nov | 11 nov |
| 7 | 24 sep | 23 sep | | 15 | 19 nov | 18 nov |
| 8 | 1 oct | 30 sep | | 16 | 26 nov | 25 nov |

39056 · 962 — **ninguna fecha cambia**: conserva `dias_presencial: [3]`.

**(c) Las tres semanas que quedan sin día de clase.** 971 en las semanas 13 y 15, y 972 en la 6: la
suspensión cae sobre su único día presencial y no hay otro esa semana. La celda conserva la fecha
del día suspendido —el recorrido no cruza de semana, porque hacerlo imprimiría en la fila «semana
13» una fecha de la semana 14— y `validar.py` levanta un aviso por cada una. Transcribe los tres
avisos literales que imprime
`python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml`.
Cierra la sección diciendo que reprogramar esas tres clases es decisión del docente, no del
generador.

Añade al final la salida literal de `huella verificar` (paso 2) y el `git diff --stat` (paso 3).

**No corras `huella registrar`.** El informe se escribe **antes** de aceptar el cambio: es lo que
hace que el criterio 4 —«se mide antes de aceptarse»— signifique algo.
  </action>
  <acceptance_criteria>
    - Existe `.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md` con las tres secciones: campo por campo, fecha por fecha y las tres semanas sin día de clase
    - El informe contiene la palabra `manifiesto` en la fila de `39056 · 962` y dice explícitamente que `huella verificar` reporta **cuatro** documentos, no tres
    - El informe contiene los tres avisos literales de `validar.py` sobre 39062
    - El informe contiene la salida literal de `python src/huella.py verificar`
    - `git status --short pruebas/huellas.yaml` no devuelve nada: la línea base **no** se ha re-registrado todavía
    - `python src/grafo.py` corrió y `git diff --stat grafo/` muestra cambios solo bajo `grafo/`
  </acceptance_criteria>
  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <done>El cambio está medido y escrito, y `pruebas/huellas.yaml` sigue sin tocarse.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: El profesor acepta el cambio de fechas antes de que se registre</name>
  <files>(ninguno — punto de control)</files>
  <read_first>
    - `.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md` (el artefacto que se revisa)
    - `horarios/2026-2.md` §«Clases del semestre» (contra qué se contrasta)
  </read_first>
  <action>
Presenta al usuario el informe de medición y la salida de `huella verificar`, y **espera su
aprobación explícita** antes de que el plan 15-06 corra `huella registrar`. No registres nada aquí:
el criterio 4 del roadmap exige que el cambio se mida **antes** de aceptarse, y esta pausa es lo que
hace que «antes» signifique algo. Si el usuario corrige un día, una hora o pide reprogramar una de
las tres clases suspendidas, el arreglo va en el `curso.yaml` y se vuelve a medir.
  </action>
  <acceptance_criteria>
    - El usuario respondió «aprobado» o pidió correcciones concretas
    - `git status --short pruebas/huellas.yaml` sigue sin devolver nada
  </acceptance_criteria>
  <verify>
    <automated>python -X utf8 src/huella.py verificar</automated>
  </verify>
  <done>El usuario aceptó el cambio de fechas, o pidió correcciones que se aplicaron y se volvieron a medir.</done>
  <what-built>
Los cuatro grupos ya declaran los días y horas de `horarios/2026-2.md`, el grafo está regenerado, y
`15-MEDICION-HUELLA.md` predice campo por campo y fecha por fecha lo que el cambio mueve. Nada se ha
aceptado todavía: `pruebas/huellas.yaml` sigue con la línea base de la v2.0.
  </what-built>
  <how-to-verify>
1. Abre `.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md` y léelo contra la
   salida de `python -X utf8 src/huella.py verificar` que trae al final: **cada campo que el informe
   dice que se mueve tiene que aparecer en la salida, y cada línea de la salida tiene que estar en
   el informe.** Si sobra o falta una, el informe no sirve como justificación.
2. Confirma que la cuarta línea —el `manifiesto` de 39056·962— está explicada como acoplamiento del
   archivo compartido y no como un descuido.
3. Revisa las tres tablas de fechas contra tu horario real: 961 los lunes 12:00–13:00, 971 los lunes
   10:00–12:00, 972 los miércoles 18:00–19:00. Si una hora o un día no es el tuyo, dilo ahora: son
   las fechas que van a imprimirse en un documento con valor legal frente a tus alumnos.
4. Decide sobre las tres clases que el calendario suspende y que se quedan sin día: **971 semanas 13
   (2 nov) y 15 (16 nov)** y **972 semana 6 (16 sep)**. Hoy la celda conserva la fecha del día
   suspendido y el validador lo avisa. Si prefieres reprogramar alguna a otro día, es decisión tuya
   y se hace en el `curso.yaml`, no en el código.
5. Confirma que 39056·962 se queda declarado y sin impartirse, con su horario de jueves intacto.
  </how-to-verify>
  <resume-signal>Escribe «aprobado» para que el plan 15-06 registre el cambio, o describe qué corregir</resume-signal>
</task>

</tasks>

<verification>
1. `python -X utf8 -m unittest discover -s pruebas` — 0 fallos.
2. `python -X utf8 src/validar.py` sobre los dos cursos — 0 errores; 39062 con sus tres avisos.
3. `python src/plantillas.py verificar` — en verde.
4. `git status --short` muestra los dos `curso.yaml`, `pruebas/test_generar.py`, `grafo/` y el
   informe de medición — y **no** `pruebas/huellas.yaml`.
</verification>

<success_criteria>
- Los cuatro grupos declaran los días y horas de `horarios/2026-2.md`.
- 962 queda declarado, no impartido, con su horario y su `.docx` intactos.
- El cambio está medido campo por campo y fecha por fecha, escrito antes de aceptarse.
- El informe anticipa las cuatro huellas movidas y explica cuál se mueve por acoplamiento.
- La línea base sigue sin re-registrarse.
</success_criteria>

<output>
Al terminar, escribe `.planning/phases/15-el-horario-entra-al-contrato/15-05-SUMMARY.md`.
</output>
