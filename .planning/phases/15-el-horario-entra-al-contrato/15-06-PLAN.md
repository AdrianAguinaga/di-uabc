---
phase: 15-el-horario-entra-al-contrato
plan: 06
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - pruebas/huellas.yaml
  - cursos/2026-2/39056-big-data/MANIFIESTO.yaml
  - cursos/2026-2/39062-patrones-de-comportamiento/MANIFIESTO.yaml
autonomous: false
requirements: [REQ-52]

must_haves:
  truths:
    - "La línea base de huella queda re-registrada con el cambio medido"
    - "El re-registro va en un commit propio, sin código mezclado"
    - "El mensaje del commit distingue los tres cambios deliberados del cuarto acoplado"
    - "Después del registro, huella verificar vuelve a decir 4/4 intacto"
  artifacts:
    - path: "pruebas/huellas.yaml"
      provides: "La línea base nueva de los cuatro documentos de control"
      contains: "documentos"
  key_links:
    - from: "15-MEDICION-HUELLA.md"
      to: "el mensaje del commit de re-registro"
      via: "el commit referencia la medición escrita antes"
      pattern: "15-MEDICION-HUELLA"
---

<objective>
Aceptar el cambio de huella que el plan 15-05 midió, en un commit propio cuyo mensaje dice que es
deliberado y por qué.

Propósito: REQ-52 es la única excepción a REQ-48 del proyecto. Lo que la hace defendible no es que
el código funcione, es el orden: primero se midió, después se acepta, y el commit que la acepta no
lleva código dentro para que se pueda leer de un vistazo dentro de un año.
Salida: `pruebas/huellas.yaml` re-registrada, los dos `MANIFIESTO.yaml` reescritos, y un commit
propio (paso 9 y 10 de la secuencia de huella).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/15-el-horario-entra-al-contrato/15-CONTEXT.md
@.planning/phases/15-el-horario-entra-al-contrato/15-VALIDATION.md
@.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md
@src/huella.py
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Aceptar el cambio con `huella registrar`</name>
  <files>pruebas/huellas.yaml, cursos/2026-2/39056-big-data/MANIFIESTO.yaml, cursos/2026-2/39062-patrones-de-comportamiento/MANIFIESTO.yaml</files>
  <read_first>
    - `.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md` completo — es contra lo que se lee la salida
    - `src/huella.py` líneas 229-254 (`registrar`: escribe los `MANIFIESTO.yaml`, a diferencia de `verificar`)
    - `.planning/phases/15-el-horario-entra-al-contrato/15-VALIDATION.md` §«Secuencia de huella», pasos 9 y 10
  </read_first>
  <action>
**1. Comprueba que el árbol está limpio de código antes de registrar.** `git status --short` no
debe mostrar nada de `src/` ni de `pruebas/test_*.py`: el re-registro va en commit propio (D-14) y
mezclar código lo estropea. Si el trabajo del plan 15-05 sigue sin commitear, commitéalo **ahora**,
a mano y en dos commits (nunca `git add -A`; en este repositorio los archivos se listan):

```bash
git add cursos/2026-2/39056-big-data/curso.yaml \
        cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml \
        pruebas/test_generar.py
git commit -m "feat(15): los cuatro grupos declaran el horario real de 2026-2"

git add grafo/ .planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md
git commit -m "docs(15): medir el cambio de fechas antes de aceptarlo"
```

**2.** `python -X utf8 src/huella.py registrar`.

Va a imprimir **cuatro** líneas con `!`, una por documento —`39056 grupo 961`, `39056 grupo 962`,
`39062 grupo 971`, `39062 grupo 972`— porque `registrar` marca con `!` cualquier documento en el
que **alguno** de los tres campos cambió, y el `manifiesto` de 962 se mueve por acoplamiento del
archivo compartido (D-16). Cuatro `!` es lo esperado; tres sería el síntoma de que algo no se
aplicó. Guarda la salida literal.

**3.** `python -X utf8 src/huella.py verificar` — ahora tiene que volver a decir
`Todo intacto. 4 documentos comparados.` con código 0. La línea base nueva es la buena.

**4.** `python -X utf8 -m unittest discover -s pruebas` y `python src/plantillas.py verificar`:
los dos en verde. Si `plantillas verificar` falla, **para**: algo escribió sobre una plantilla y
eso invalida cualquier documento generado.

**5.** `git status --short` — solo deben aparecer `pruebas/huellas.yaml` y los dos
`MANIFIESTO.yaml`. Si aparece un `.docx` o un `.pdf` bajo `cursos/*/salida/`, no los añadas al
commit del re-registro: la huella la fija `pruebas/huellas.yaml`, no los binarios.

Anota en el SUMMARY que `huella registrar` genera con `pdf=False`, así que los `MANIFIESTO.yaml`
quedan sin listar los `.pdf` y los PDF que haya en disco son anteriores a este cambio de fechas —
es la misma nota que dejó la Fase 9 y sigue siendo cierta.
  </action>
  <acceptance_criteria>
    - La salida de `python -X utf8 src/huella.py registrar` contiene cuatro líneas que empiezan con `!`
    - `python -X utf8 src/huella.py verificar` imprime `Todo intacto. 4 documentos comparados.` y sale con código 0
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0
    - `python src/plantillas.py verificar` sale con código 0
    - `git status --short` solo lista `pruebas/huellas.yaml` y los dos `MANIFIESTO.yaml`
  </acceptance_criteria>
  <verify>
    <automated>python -X utf8 src/huella.py verificar</automated>
  </verify>
  <done>La línea base nueva está registrada y vuelve a verificar 4/4.</done>
</task>

<task type="auto">
  <name>Tarea 2: El commit propio del re-registro</name>
  <files>(ninguno — solo git)</files>
  <read_first>
    - `.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md` §campo por campo
    - `AGENTS.md` §«Git» (commits atómicos, nunca `git add -A`, el push solo si el usuario lo pide)
  </read_first>
  <action>
Commitea **solo** el re-registro, con los archivos listados uno a uno:

```bash
git add pruebas/huellas.yaml \
        cursos/2026-2/39056-big-data/MANIFIESTO.yaml \
        cursos/2026-2/39062-patrones-de-comportamiento/MANIFIESTO.yaml
```

y usa este mensaje (ajusta solo si la medición real difirió de la predicha; si difirió, el informe
tiene que decir por qué antes de que lo ajustes):

```
chore(15): re-registrar la huella de control — el cambio de días es deliberado

Los cuatro grupos declaraban días de relleno: 961 y 971 decían martes, 962 y 972 jueves,
y ninguno coincidía con la carga académica de 2026-2. Corregirlos mueve las fechas de los
documentos, que es el objetivo de REQ-52 —la única excepción a REQ-48 del proyecto,
acotada a 39056 y 39062— y no una contaminación.

Qué se movió, campo por campo:

  39056:961   texto_docx y manifiesto   martes -> lunes; el manifiesto gana los bloques
  39056:962   solo manifiesto           su horario no se toca. MANIFIESTO.yaml es un
                                        archivo por curso y su hash se aplica a los dos
                                        grupos, asi que se mueve por acoplamiento
  39062:971   los tres                  martes -> lunes; el informe gana los avisos de
                                        las semanas 13 y 15, sin dia de clase
  39062:972   los tres                  jueves -> miercoles; comparte informe con 971

El .docx de 962 sale byte por byte igual: su texto_docx y su informe siguen intactos. Es
el testigo de que la excepcion esta acotada a los grupos que si cambiaron de dia.

Medido antes de aceptarse:
.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md
```

Dos cosas que el mensaje **no** puede decir: que la huella de 962 no se movió (uno de sus tres
campos sí lo hizo) y que el cambio es una regresión (es el objetivo del requisito). No hagas
`git push`: publicar se hace solo cuando el usuario lo pide.
  </action>
  <acceptance_criteria>
    - `git show --stat HEAD` lista exactamente tres archivos: `pruebas/huellas.yaml` y los dos `MANIFIESTO.yaml`
    - `git show --stat HEAD` no lista ningún archivo bajo `src/`
    - `git log -1 --format=%B` contiene las cadenas `REQ-52`, `39056:962` y `15-MEDICION-HUELLA.md`
    - `git status --short` queda vacío
  </acceptance_criteria>
  <verify>
    <automated>git show --stat HEAD</automated>
  </verify>
  <done>El re-registro está en un commit propio que explica el cambio con precisión de campo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: El mensaje del commit dice la verdad</name>
  <files>(ninguno — punto de control)</files>
  <read_first>
    - `.planning/phases/15-el-horario-entra-al-contrato/15-MEDICION-HUELLA.md`
    - La salida de `git log -1` y de `git show --stat HEAD`
  </read_first>
  <action>
Enseña al usuario el commit del re-registro —`git show --stat HEAD` y `git log -1`— y la salida de
`huella verificar`. Espera su aprobación. Si el mensaje promete algo que el código no cumple, o si
falta la precisión de campo del 962, corrígelo con `git commit --amend` antes de continuar. No
hagas `git push`: publicar se hace solo cuando el usuario lo pide.
  </action>
  <acceptance_criteria>
    - El usuario respondió «aprobado» o pidió una corrección del mensaje
    - `git show --stat HEAD` sigue listando tres archivos y ninguno de `src/`
  </acceptance_criteria>
  <verify>
    <automated>git show --stat HEAD</automated>
  </verify>
  <done>El commit del re-registro está revisado y aprobado, o enmendado y aprobado.</done>
  <what-built>
`pruebas/huellas.yaml` re-registrada con las huellas nuevas, los dos `MANIFIESTO.yaml` reescritos, y
todo ello en un commit propio sin código. `huella verificar` vuelve a decir 4/4 intacto.
  </what-built>
  <how-to-verify>
1. `git show --stat HEAD` — confirma que el commit lleva **tres** archivos y ninguno de `src/`.
2. `git log -1` — lee el mensaje entero. Tiene que decir tres cosas: que el cambio es deliberado,
   qué campo se movió en cada uno de los cuatro documentos, y que el `.docx` de 962 sale igual.
   Si promete algo que el código no cumple, corrígelo con `git commit --amend` antes de seguir.
3. `python -X utf8 src/huella.py verificar` — `Todo intacto. 4 documentos comparados.`
4. Abre `cursos/2026-2/39062-patrones-de-comportamiento/salida/DI-2026-2-39062-971.docx` (o su PDF
   regenerado) y mira la fila de la **semana 13**: tiene que decir lunes 2 de noviembre, que es día
   de suspensión, porque esa semana el grupo no tiene otro día de clase. Es la decisión de D-15 —el
   recorrido no cruza de semana— y el validador la avisa. Confirma que prefieres eso a una fecha de
   la semana siguiente.
  </how-to-verify>
  <resume-signal>Escribe «aprobado» o describe qué corregir en el mensaje</resume-signal>
</task>

</tasks>

<verification>
1. `python -X utf8 src/huella.py verificar` — `Todo intacto. 4 documentos comparados.`
2. `python -X utf8 -m unittest discover -s pruebas` — 0 fallos.
3. `python src/plantillas.py verificar` — en verde.
4. `git show --stat HEAD` — tres archivos, ninguno de `src/`.
</verification>

<success_criteria>
- La línea base nueva está registrada y verifica 4/4.
- El re-registro va en un commit propio, sin código.
- El mensaje distingue los tres cambios deliberados del cuarto, movido por acoplamiento.
- El informe de medición queda referenciado desde el commit.
</success_criteria>

<output>
Al terminar, escribe `.planning/phases/15-el-horario-entra-al-contrato/15-06-SUMMARY.md`.
</output>
