---
phase: 09-valor-de-una-meta
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/validar.py
  - pruebas/test_validar.py
autonomous: true
requirements: [REQ-42]

must_haves:
  truths:
    - "Dos metas con el mismo id son un error de la regla R2: el curso carga y se puede inspeccionar, pero no valida"
    - "Las ocho reglas siguen atrapando exactamente lo que atrapaban: test_detecta_el_defecto_del_ejemplo_961 pasa sin haberse tocado"
    - "La aritmética de R1 y R2 no se reescribe en esta fase: sigue sumando porcentaje y valor sin conocer las unidades"
  artifacts:
    - path: "src/validar.py"
      provides: "Comprobación de ids de meta duplicados dentro de regla_2"
      contains: "Metas duplicadas"
    - path: "pruebas/test_validar.py"
      provides: "Prueba hermana en la clase Regla2Metas que fija el comportamiento"
      contains: "duplicad"
  key_links:
    - from: "src/validar.py:regla_2"
      to: "collections.Counter"
      via: "Counter(m.id for m in self.c.metas)"
      pattern: "Counter\\(m\\.id for m in"
---

<objective>
Con identificadores de meta libres (REQ-42), el riesgo de que dos metas colisionen sube: ya no hay
una convención numérica que lo haga evidente al leer el YAML. Esta colisión hoy no se detecta en
ninguna parte, y `src/grafo.py` construye la clave de cada nodo de meta a partir del id — dos metas
con el mismo id producirían la misma clave y una pisaría a la otra en silencio.

Propósito: cerrar ese hueco antes de que los ids libres se empiecen a usar.

Salida: una comprobación nueva dentro de R2 y su prueba. **La aritmética de R1 y R2 no se toca**:
contar en la unidad declarada es la Fase 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-valor-de-una-meta/09-CONTEXT.md
@.planning/phases/09-valor-de-una-meta/09-PATTERNS.md
@src/validar.py
@pruebas/test_validar.py

<interfaces>
<!-- El patrón exacto a replicar, ya escrito en este mismo archivo. -->

`src/validar.py:141-143` — el `Counter` de rubros duplicados dentro de `regla_1`. `Counter` ya está
importado en el módulo:
```python
cuenta = Counter(r.id for r in self.c.rubros)
if repetidos := [i for i, n in cuenta.items() if n > 1]:
    self.error("R1", f"Rubros duplicados: {', '.join(sorted(repetidos))}.")
```

`src/validar.py:115-122` — cómo se añade un hallazgo:
```python
def _add(self, regla: str, nivel: str, mensaje: str) -> None:
    self.hallazgos.append(Hallazgo(regla, nivel, mensaje))

def error(self, regla: str, mensaje: str) -> None:
    self._add(regla, ERROR, mensaje)

def aviso(self, regla: str, mensaje: str) -> None:
    self._add(regla, AVISO, mensaje)
```

`src/validar.py:173-207` — `regla_2` completa hoy: comprueba rubros inexistentes, el total de las
metas contra el total de los rubros, la suma rubro por rubro (el defecto del 961) y los valores
negativos. **Nada de eso cambia en esta fase.**

`src/validar.py:394` — la única distinción del encuadre en todo `src/`, y ya usa `tipo`, no `id`:
```python
if m.valor == 0 and m.tipo not in ("encuadre", "cierre"):
    self.error("IEDI 1.5", f"{m.etiqueta} no tiene valor porcentual.")
```
No se toca: es la forma correcta y REQ-42 la confirma.

Helpers de `pruebas/test_validar.py` (líneas 104-116):
```python
def curso(**cambios): ...        # deepcopy de CURSO_VALIDO + modelo.desde_dict
def informe(**cambios): ...      # validar.validar(curso(**cambios))
def reglas_con_error(inf) -> set[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: La prueba de las metas duplicadas</name>
  <files>pruebas/test_validar.py</files>
  <read_first>
    - `pruebas/test_validar.py` líneas 1-120 (fixture y helpers) y 168-200 (la clase
      `Regla2Metas` completa, donde va la prueba nueva).
    - `.planning/phases/09-valor-de-una-meta/09-CONTEXT.md` §D-17 — por qué es error de regla y no
      `ErrorModelo`.
  </read_first>
  <action>
Añade **un método nuevo** a la clase `Regla2Metas` que ya existe en `pruebas/test_validar.py`.
No modifiques ni reordenes ningún método existente; en particular
`test_detecta_el_defecto_del_ejemplo_961` debe quedar byte por byte igual.

```python
    def test_dos_metas_con_el_mismo_id_son_error_de_regla(self):
        """Con ids libres la colisión deja de ser evidente al leer el YAML, y el grafo
        construye la clave de cada nodo con el id: la segunda pisaría a la primera."""
        metas = copy.deepcopy(CURSO_VALIDO["metas"])
        metas[2]["id"] = metas[1]["id"]      # dos metas "1.1"
        inf = informe(metas=metas)
        self.assertIn("R2", reglas_con_error(inf))
        mensajes = " ".join(h.mensaje for h in inf.errores if h.regla == "R2")
        self.assertIn(metas[1]["id"], mensajes)

    def test_el_curso_con_ids_repetidos_carga_igual(self):
        """Es un defecto de regla, no de esquema: el curso tiene que poder cargarse para
        poder inspeccionarlo. Si reventara al cargar, quien lo escribió no vería el resto."""
        metas = copy.deepcopy(CURSO_VALIDO["metas"])
        metas[2]["id"] = metas[1]["id"]
        c = curso(metas=metas)               # no lanza ErrorModelo
        self.assertEqual(len(metas), len(c.metas))
```

Comprueba que el helper `curso` esté importado/definido en el ámbito del módulo (lo está: línea
104). No añadas imports nuevos.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest pruebas.test_validar.Regla2Metas -v</automated>
  </verify>
  <acceptance_criteria>
    - `pruebas/test_validar.py` contiene `def test_dos_metas_con_el_mismo_id_son_error_de_regla`
      y `def test_el_curso_con_ids_repetidos_carga_igual`, ambos dentro de `class Regla2Metas`.
    - `python -X utf8 -m unittest pruebas.test_validar.Regla2Metas -v` falla **solo** en
      `test_dos_metas_con_el_mismo_id_son_error_de_regla` (la comprobación aún no existe);
      `test_el_curso_con_ids_repetidos_carga_igual` y
      `test_detecta_el_defecto_del_ejemplo_961` pasan.
    - `git diff pruebas/test_validar.py` no muestra ninguna línea eliminada ni modificada, solo
      líneas añadidas.
  </acceptance_criteria>
  <done>La prueba existe y falla por lo que falta implementar, no por un error en la prueba.</done>
</task>

<task type="auto">
  <name>Tarea 2: R2 cuenta los ids repetidos</name>
  <files>src/validar.py</files>
  <read_first>
    - `src/validar.py` líneas 124-145 — `regla_1` completa, con el `Counter` que se replica.
    - `src/validar.py` líneas 171-208 — `regla_2` completa, donde entra la comprobación.
    - `src/grafo.py` líneas 290-325 — por qué importa: los nodos se llaman
      `meta:{ciclo}:{clave}:{m.id}`.
  </read_first>
  <action>
Añade al **final** de `regla_2` en `src/validar.py`, después del bloque de metas con valor
negativo (línea 206-207), estas tres líneas — el mismo patrón que `regla_1` usa para los rubros:

```python
        cuenta = Counter(m.id for m in self.c.metas)
        if repetidos := [i for i, n in cuenta.items() if n > 1]:
            self.error("R2", f"Metas duplicadas: {', '.join(sorted(repetidos))}.")
```

Es `self.error`, no `self.aviso`: dos metas con el mismo id bloquean; no es decisión del docente.

`Counter` ya está importado en el módulo (lo usa `regla_1`) — no añadas el import otra vez.

**Nada más se toca en este archivo.** En concreto:
- La aritmética de R1 (suma de porcentajes, exención) y de R2 (total de metas, suma rubro por
  rubro, valores negativos) se queda como está: contar en la unidad declarada es la Fase 10.
- La línea 394 (`m.tipo not in ("encuadre", "cierre")`) se queda como está: ya distingue el
  encuadre por su tipo, que es lo correcto según REQ-42.
- No añadas ninguna comprobación sobre la forma del id (ni que termine en `.0`, ni que empiece por
  el número de su unidad): el identificador es libre.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <acceptance_criteria>
    - `src/validar.py` contiene `Counter(m.id for m in self.c.metas)` y `"Metas duplicadas: "`.
    - La cadena `Metas duplicadas` aparece dentro del cuerpo de `regla_2`, no de `regla_1`.
    - `python -X utf8 -m unittest pruebas.test_validar -v` sale con código 0.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
    - `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sale con código 0
      e imprime `VÁLIDO`.
    - `python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml`
      imprime `VÁLIDO`.
    - `git diff src/validar.py` muestra exactamente 3 líneas añadidas y 0 eliminadas.
  </acceptance_criteria>
  <done>Dos metas con el mismo id producen un error de R2; los tres cursos existentes siguen validando igual que antes.</done>
</task>

</tasks>

<verification>
- `python -X utf8 -m unittest discover -s pruebas` en verde.
- Los dos cursos de control siguen imprimiendo `VÁLIDO` — su informe no cambió, así que la huella
  del plan 09-05 tampoco debería moverse por este plan.
- `git diff --stat src/` solo lista `src/validar.py` con 3 líneas añadidas.
</verification>

<success_criteria>
Con identificadores libres, una colisión de ids se reporta como error de R2 en vez de pasar
inadvertida y pisar un nodo del grafo. Las ocho reglas siguen atrapando lo que atrapaban.
</success_criteria>

<output>
Al terminar, crea `.planning/phases/09-valor-de-una-meta/09-03-SUMMARY.md`.
</output>
