---
phase: 09-valor-de-una-meta
plan: 05
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - cursos/2026-2/39056-big-data/curso.yaml
  - cursos/2026-2/39056-big-data/MANIFIESTO.yaml
  - cursos/2026-2/39062-patrones-de-comportamiento/MANIFIESTO.yaml
  - grafo/grafo.json
  - grafo/index.html
  - grafo/AUDITORIA.md
  - pruebas/huellas.yaml
autonomous: true
requirements: [REQ-42, REQ-48]

must_haves:
  truths:
    - "Tras abrir el contrato, las tres huellas de los cuatro documentos de control no cambiaron —texto, informe y forma del MANIFIESTO.yaml—: el rasgo nuevo no se activa si el curso.yaml no lo declara (REQ-48)"
    - "El encuadre de Big Data se llama 1.0 y el documento queda igual salvo esa cadena: ninguna función de src/ deducía el encuadre por su id (criterio 3)"
    - "El grafo sigue al curso.yaml: grafo/ no menciona una meta que ya no existe con ese id, y su número de nodos y aristas no cambia"
    - "El único cambio de huella del milestone queda medido y aceptado deliberadamente, con el diff como constancia"
  artifacts:
    - path: "cursos/2026-2/39056-big-data/curso.yaml"
      provides: "El encuadre declarado como 1.0"
      contains: "id: \"1.0\""
    - path: "pruebas/huellas.yaml"
      provides: "Línea base actualizada tras el renombrado, aceptada a propósito"
      contains: "documentos:"
  key_links:
    - from: "cursos/2026-2/39056-big-data/curso.yaml"
      to: "grafo/grafo.json"
      via: "python src/grafo.py tras el renombrado"
      pattern: "Meta 1\\.0\\."
---

<objective>
Ejecutar los pasos 3 a 7 de la secuencia obligatoria de la fase (D-15, ampliada por D-24): demostrar
que el contrato nuevo no contaminó nada, renombrar el encuadre `0` de Big Data a `1.0`, hacer que el
grafo siga al curso, medir el único cambio de huella del milestone y aceptarlo.

Propósito: el renombrado es la prueba de fuego de REQ-42. Si alguna función de `src/` dedujera el
encuadre por su identificador, el documento cambiaría en más de una cadena y se vería aquí. Y va al
final a propósito: gasta la primera excepción a REQ-48 **después** de que el instrumento haya
demostrado que sabe decir «nada cambió».

Salida: `curso.yaml` de Big Data con el encuadre `1.0`, `grafo/` regenerado, `pruebas/huellas.yaml`
con la línea base nueva, y el `git diff` como constancia de que solo cambió esa cadena.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-valor-de-una-meta/09-CONTEXT.md
@.planning/phases/09-valor-de-una-meta/09-VALIDATION.md
@src/huella.py

<interfaces>
<!-- La secuencia obligatoria. El planeador no puede reordenarla y el ejecutor tampoco. -->

D-15 + D-24, completa. Los pasos 1 y 2 los hicieron los planes 09-01 a 09-04; este plan hace
del 3 al 7:

```
1. huella registrar            # línea base del repo tal como está HOY      -> plan 09-01
2. ...contrato: unidad, total, componentes, conversion...                   -> planes 09-02..09-04
3. huella verificar            # OK, nada cambió: el instrumento demuestra algo
4. renombrar 0 -> 1.0 en Big Data
5. python src/grafo.py         # el grafo sigue al curso.yaml
6. huella verificar            # cambió 39056
   git diff                    # solo "Meta 0." -> "Meta 1.0." (+ grafo/)
7. huella registrar            # se acepta, con el diff como constancia
```

Estado actual del encuadre a renombrar (`cursos/2026-2/39056-big-data/curso.yaml:208-209`):
```yaml
metas:
  - id: "0"
    unidad: "I"
    tipo: encuadre
```

Tamaño actual del grafo, medido antes de esta fase (ningún plan anterior lo toca, así que debe
seguir igual al empezar): **377 nodos, 669 aristas**. Se comprueba con:
```
python -X utf8 -c "import json;d=json.load(open('grafo/grafo.json',encoding='utf-8'));print(len(d['nodos']),len(d['aristas']))"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Paso 3 — el contrato no contaminó nada</name>
  <files>ninguno (esta tarea solo mide)</files>
  <read_first>
    - `src/huella.py` — qué compara `verificar` y qué imprime cuando algo cambió.
    - `.planning/phases/09-valor-de-una-meta/09-CONTEXT.md` §D-15 — por qué este paso va antes del
      renombrado.
  </read_first>
  <action>
Este paso no modifica ningún archivo: mide.

1. Corre `python -X utf8 -m unittest discover -s pruebas`. Debe salir 0. Si falla, para: el
   contrato no está cerrado y no tiene sentido medir la huella.

2. Corre `python -X utf8 src/huella.py verificar`.

   **Resultado esperado: los cuatro documentos intactos en sus tres hashes, y código de salida 0.**
   Es el momento en que el instrumento demuestra que sirve: abrir el contrato en tres puntos
   —rubro en puntos, componentes, ids libres— no cambió ni un carácter de los cursos que no
   declaran nada de eso, ni la forma de su manifiesto.

3. Si `verificar` señala algún documento como cambiado, **para y reporta**. No sigas al renombrado
   y no corras `registrar`. El diagnóstico:
   - Si cambió «la forma del MANIFIESTO.yaml»: algún campo nuevo del modelo se está colando en
     `generar.py` — hoy su bloque de rubros filtra explícitamente a `{id, porcentaje}`, así que
     `unidad` y `total` no deberían llegar ahí. Es exactamente lo que D-27 quería poder ver.
   - Si cambió «el informe de validación» de algún curso: la comprobación de metas duplicadas del
     plan 09-03 está produciendo un hallazgo nuevo en un curso que antes no lo tenía, o el orden de
     los hallazgos cambió.
   - Si cambió «el texto del documento»: el cambio de `src/render_docx.py` del plan 09-04 no es
     neutro para un curso sin componentes — `_evidencias()` no está devolviendo la misma lista de
     siempre.
   - En ambos casos el arreglo pertenece al plan que lo introdujo, no a este.

4. Comprueba que `verificar` fue de solo lectura: `git status --porcelain cursos/` debe salir
   vacío (D-23).
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest discover -s pruebas && python -X utf8 src/huella.py verificar && git status --porcelain cursos/</automated>
  </verify>
  <acceptance_criteria>
    - `python -X utf8 src/huella.py verificar` sale con código 0 e imprime
      `Todo intacto. 4 documentos comparados.`
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
    - `git status --porcelain cursos/` sale vacío después de verificar.
    - `git diff --stat pruebas/huellas.yaml` no reporta cambios: verificar no escribe el registro.
  </acceptance_criteria>
  <done>Con el contrato ya abierto, la huella de los cuatro documentos de control es idéntica a la línea base. REQ-48 se cumple para los cambios de los planes 09-02, 09-03 y 09-04.</done>
</task>

<task type="auto">
  <name>Tarea 2: Pasos 4 y 5 — renombrar el encuadre y regenerar el grafo</name>
  <files>cursos/2026-2/39056-big-data/curso.yaml, grafo/grafo.json, grafo/index.html, grafo/AUDITORIA.md</files>
  <read_first>
    - `cursos/2026-2/39056-big-data/curso.yaml` líneas 205-240 — el bloque completo del encuadre,
      para cambiar exactamente una línea y ninguna más.
    - `.planning/phases/09-valor-de-una-meta/09-CONTEXT.md` §D-14 y §D-16 — qué alcanza el
      renombrado y qué no.
    - `src/grafo.py` líneas 290-325 — cómo se construyen las claves de nodo a partir del id, para
      saber qué se espera que cambie en `grafo/grafo.json`.
  </read_first>
  <action>
**Paso 4 — el renombrado.** En `cursos/2026-2/39056-big-data/curso.yaml`, cambia exactamente una
línea (la 209):

```yaml
  - id: "0"
```
pasa a
```yaml
  - id: "1.0"
```

Nada más de ese archivo. En concreto:
- **No** renombres `1.1`, `1.2` ni ninguna otra meta: la unidad 1 abre en `.0` y sigue en `.1`, que
  es exactamente el patrón del 531.
- **No** toques el recurso `M0_Foro de presentación` ni su mención dentro de los pasos. El criterio
  de éxito de la fase es que la única diferencia del documento sea `"Meta 0." -> "Meta 1.0."`, y
  cambiar el nombre del recurso metería una diferencia más. Que el recurso siga llamándose `M0_...`
  con la meta ya renombrada queda como un detalle a resolver por separado con el docente; anótalo
  en el SUMMARY.
- **No** toques `cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml`: es el documento de
  control con huella intacta durante toda la fase.
- **No** toques `cursos/2026-2/38985-contabilidad-financiera/curso.yaml`: es la Fase 14.

Confirma el alcance: `git diff --numstat cursos/2026-2/39056-big-data/curso.yaml` debe reportar
`1	1` (una línea añadida, una eliminada).

**Paso 5 — el grafo sigue al curso.** Antes de regenerar, anota el tamaño actual:

```
python -X utf8 -c "import json;d=json.load(open('grafo/grafo.json',encoding='utf-8'));print(len(d['nodos']),len(d['aristas']))"
```

Corre `python -X utf8 src/grafo.py`, que reescribe `grafo/grafo.json`, `grafo/index.html` y
`grafo/AUDITORIA.md` juntos. Vuelve a medir: **el número de nodos y de aristas debe ser idéntico**
(377 y 669, salvo que la medición previa dé otra cosa — en cuyo caso vale la medición previa, no
este número). Lo que cambia es el contenido de unas cuantas cadenas: la etiqueta
`"Meta 0. Encuadre del curso."` y las claves de nodo derivadas del id
(`meta:2026-2:39056:0` pasa a `meta:2026-2:39056:1.0`, y lo mismo sus evidencias y criterios).

Si el número de nodos o de aristas cambia, **para y reporta**: la forma del grafo debía quedar
intacta, y un conteo distinto significa que algo más se movió.

Revisa `grafo/AUDITORIA.md`: debe seguir cerrando en **0 huecos** y **0 anclas rotas**, salvo el
hueco ya conocido y correcto de «Contabilidad Financiera · 2026-2» (falta el PUA 38985). Si
aparece un hueco nuevo, para y reporta.

No corras `huella registrar` en esta tarea: eso es el paso 7, y va después de medir.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && git diff --numstat cursos/2026-2/39056-big-data/curso.yaml && python -X utf8 -c "import json;d=json.load(open('grafo/grafo.json',encoding='utf-8'));print(len(d['nodos']),len(d['aristas']))" && python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat cursos/2026-2/39056-big-data/curso.yaml` reporta exactamente `1	1`.
    - `grep -c 'id: "0"' cursos/2026-2/39056-big-data/curso.yaml` devuelve 0 y
      `grep -c 'id: "1.0"' cursos/2026-2/39056-big-data/curso.yaml` devuelve 1.
    - `grep -c 'M0_Foro de presentación' cursos/2026-2/39056-big-data/curso.yaml` devuelve el mismo
      número que antes del cambio (el recurso no se tocó).
    - `git status --porcelain cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` y
      `git status --porcelain cursos/2026-2/38985-contabilidad-financiera/curso.yaml` salen vacíos.
    - `grafo/grafo.json` tiene el mismo número de nodos y de aristas que antes de regenerar.
    - `grep -c "Meta 0\." grafo/grafo.json` devuelve 0 y `grep -c "Meta 1\.0\." grafo/grafo.json`
      devuelve 2.
    - `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` imprime `VÁLIDO`.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
  </acceptance_criteria>
  <done>El encuadre de Big Data se llama 1.0, el grafo lo refleja y su forma no cambió. Las ocho reglas siguen dando el mismo veredicto.</done>
</task>

<task type="auto">
  <name>Tarea 3: Pasos 6 y 7 — medir el cambio y aceptarlo</name>
  <files>pruebas/huellas.yaml, cursos/2026-2/39056-big-data/MANIFIESTO.yaml, cursos/2026-2/39062-patrones-de-comportamiento/MANIFIESTO.yaml</files>
  <read_first>
    - `src/huella.py` — la diferencia entre `verificar` (restaura los MANIFIESTO) y `registrar`
      (los deja escritos).
    - `.planning/phases/09-valor-de-una-meta/09-CONTEXT.md` §D-15, §D-21 y §D-23.
  </read_first>
  <action>
**Paso 6 — medir.** Corre `python -X utf8 src/huella.py verificar`. Ahora **debe fallar**, con
código de salida 1, y su salida debe cumplir las tres cosas:

- señala `39056 grupo 961` y `39056 grupo 962` como cambiados, **con dos líneas cada uno**: una
  por «el texto del documento» y otra por «la forma del MANIFIESTO.yaml». La segunda es esperada y
  correcta: el manifiesto registra el `sha256` del `curso.yaml`, y el `curso.yaml` acaba de cambiar.
  Si solo aparecieran las de texto, el hash del manifiesto no estaría incluyendo `curso.sha256` y
  habría que revisar `forma_del_manifiesto()`;
- deja `39062 grupo 971` y `39062 grupo 972` marcados como intactos — el documento de control no se
  movió;
- el mensaje dice qué cambió y qué mirar.

Si señalara también a 39062, **para y reporta**: el renombrado de un curso habría afectado a otro,
que es exactamente lo que REQ-48 existe para impedir.

Recoge la constancia del alcance del cambio. La forma más directa de comprobar que la única
diferencia del documento es esa cadena, sin generar dos veces, es comparar el texto extraído del
`.docx` regenerado contra el del `.docx` que dejó la corrida anterior. Como `verificar` compara
hashes y no textos, hazlo así:

```
python -X utf8 -c "
import sys; sys.path.insert(0, 'src')
import huella, generar
p = generar.paquete('cursos/2026-2/39056-big-data/curso.yaml', pdf=False, grupos=['961'])
nuevo = huella.extraer_texto(p.archivos[0])
viejo = nuevo.replace('Meta 1.0.', 'Meta 0.')
print('sha del texto con la cadena revertida:', huella.sha_texto(viejo))
"
```

y compara ese sha contra el `texto_docx` que `pruebas/huellas.yaml` tiene registrado hoy para
`2026-2:39056:961`. **Deben ser iguales.** Si no lo son, el renombrado cambió algo más que la
cadena `"Meta 0."` y hay que averiguar qué antes de aceptarlo: es exactamente el criterio 3 de la
fase el que está en juego.

Repite la comprobación para el grupo `962`.

Guarda en el SUMMARY la salida de `git diff --stat` completo: debe listar
`cursos/2026-2/39056-big-data/curso.yaml` (1 línea), `grafo/grafo.json`, `grafo/index.html`,
`grafo/AUDITORIA.md` y los `MANIFIESTO.yaml`. Nada más de `cursos/`.

**Paso 7 — aceptar.** Corre `python -X utf8 src/huella.py registrar`. Debe imprimir dos líneas
`!` (los dos grupos de 39056, con el hash de texto viejo y el nuevo) y dos líneas `=` (los dos de
39062, sin cambios). Ese es el registro deliberado del único cambio de huella del milestone; en
`git diff pruebas/huellas.yaml` deben verse cambiar seis valores —`texto_docx` y `manifiesto` de
961 y 962, más sus fechas de `registrado`— y ninguno de 39062.

Vuelve a correr `python -X utf8 src/huella.py verificar`: ahora debe salir 0 con los cuatro
documentos intactos, y `git status --porcelain cursos/` debe salir vacío después.

No edites `pruebas/huellas.yaml` a mano en ningún momento: se escribe solo con `registrar`.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 src/huella.py registrar && python -X utf8 src/huella.py verificar && git status --porcelain cursos/ && python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <acceptance_criteria>
    - Antes de `registrar`, `python -X utf8 src/huella.py verificar` sale con código 1, su salida
      de error menciona `39056` y **no** menciona `39062`, y trae 4 líneas de problema: dos de
      «el texto del documento» y dos de «la forma del MANIFIESTO.yaml».
    - Tras `registrar`, `git diff pruebas/huellas.yaml` no muestra ninguna línea cambiada bajo las
      claves `2026-2:39062:971` ni `2026-2:39062:972`.
    - El sha del texto de 39056-961 con `Meta 1.0.` revertido a `Meta 0.` coincide con el
      `texto_docx` registrado antes del renombrado, y lo mismo para 962.
    - Después de `registrar`, `python -X utf8 src/huella.py verificar` sale con código 0 e imprime
      `Todo intacto. 4 documentos comparados.`
    - `git status --porcelain cursos/` sale vacío tras ese `verificar` final.
    - `git diff --stat` no lista ningún archivo de `referencias/`, `ejemplos/` ni `plantillas/`.
    - `python -X utf8 src/plantillas.py verificar` sale con código 0.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
  </acceptance_criteria>
  <done>El cambio de huella quedó medido —solo 39056, solo esa cadena— y aceptado con el diff como constancia. El instrumento vuelve a estar en verde para las cinco fases siguientes.</done>
</task>

</tasks>

<verification>
- La secuencia de D-15/D-24 se ejecutó en su orden: verificar en verde, renombrado, grafo,
  verificar señalando solo 39056, registrar.
- `python -X utf8 -m unittest discover -s pruebas` en verde.
- `python -X utf8 src/huella.py verificar` en verde, con `git status --porcelain cursos/` vacío.
- `python -X utf8 src/plantillas.py verificar` sale 0.
- `git diff --stat` no lista nada de `referencias/`, `ejemplos/` ni `plantillas/`.
</verification>

<success_criteria>
Criterio 3 del ROADMAP §Fase 9 demostrado sobre el repositorio real: renombrar el encuadre de Big
Data deja el documento igual salvo esa cadena, y ninguna función de `src/` deducía el encuadre por
su id. Criterio 4 ejercido en sus dos modos: el instrumento supo decir «nada cambió» y supo señalar
exactamente qué cambió.
</success_criteria>

<output>
Al terminar, crea `.planning/phases/09-valor-de-una-meta/09-05-SUMMARY.md`.
</output>
