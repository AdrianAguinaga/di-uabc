---
phase: 09-valor-de-una-meta
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/render_docx.py
  - pruebas/test_render_docx.py
autonomous: true
requirements: [REQ-39]

must_haves:
  truths:
    - "La evidencia de un componente aparece en la columna de evidencias de la Sección 2, junto a las de su meta"
    - "Un curso sin componentes produce exactamente el mismo texto que antes: ni un carácter cambia"
    - "La columna Valor sigue imprimiendo el porcentaje tal como hoy: los dos puntos que imprimen el valor son de la Fase 13 y no se tocan"
  artifacts:
    - path: "src/render_docx.py"
      provides: "Concatenación de las evidencias de los componentes de una meta"
      contains: "def _evidencias"
    - path: "pruebas/test_render_docx.py"
      provides: "Regresión de que la ausencia de componentes no cambia nada"
      contains: "class EvidenciaDeComponente"
  key_links:
    - from: "src/render_docx.py:_filas_de_meta"
      to: "meta.componentes"
      via: "_evidencias(meta) en la celda de evidencias"
      pattern: "_evidencias\\(meta\\)"
---

<objective>
Hacer que la evidencia declarada por un componente de meta llegue al documento, concatenada a las
evidencias de su meta en la columna de evidencias de la Sección 2.

Propósito: es un desvío deliberado del reparto «Fase 9 = contrato, Fase 13 = documento», y está
acotado a propósito. Sin esto, una meta podría declarar el Examen I y el alumno no vería en ninguna
parte que tiene que entregarlo — que es exactamente la falla metodológica que este proyecto ya
corrigió una vez con la Práctica 3 de Big Data.

Salida: una función auxiliar de tres líneas en `src/render_docx.py`, la celda de evidencias que la
usa, y las pruebas de regresión.

**Fuera de este plan, explícitamente:** la columna Valor en puntos, el componente impreso en la
Sección 3 («Examen I, 15 %») y los dos niveles de la calificación. Los dos puntos de
`render_docx.py` que imprimen `f"{meta.valor:g}%"` **no se tocan**: son de la Fase 13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-valor-de-una-meta/09-CONTEXT.md
@.planning/phases/09-valor-de-una-meta/09-PATTERNS.md
@src/render_docx.py
@pruebas/test_render_docx.py

<interfaces>
<!-- La función completa a tocar, tal como está hoy. Solo cambia la celda de evidencias. -->

`src/render_docx.py:266-288`:
```python
def _filas_de_meta(meta: Meta, proto: list, cal, dividida: bool) -> list:
    """Las filas de la tabla que corresponden a una meta (dos si la entrega se divide)."""
    sesiones = meta.sesiones or []
    filas = []
    for n, plantilla_tr in enumerate(proto):
        tr = deepcopy(plantilla_tr)
        s = sesiones[n] if n < len(sesiones) else None
        ultima = n == len(proto) - 1

        _celda(tr, 0, [{"t": meta.etiqueta + ".", "enfasis": "meta"},
                       {"t": f" {meta.enunciado}"}] if n == 0 else "")
        _celda(tr, 1, str(min(meta.semanas)) if n == 0 else "")
        if dividida:
            _celda(tr, 2, "Presencial / Sincrónico" if n == 0 else "Virtual / Asincrónico")
        _celda(tr, 3 if dividida else 2,
               calendario.texto_fecha_corta(s.fecha) if s and s.fecha else "")
        _celda(tr, 4 if dividida else 3, s.actividad_tabla if s else "")
        _celda(tr, 5 if dividida else 4,
               ", ".join(e.nombre for e in meta.evidencias) if ultima else "")
        _celda(tr, 6 if dividida else 5,
               f"{meta.valor:g}%" if ultima else "")
        filas.append(tr)
    return filas
```

Contrato del modelo tras el plan 09-02:
```python
@dataclass
class Componente:
    rubro: str
    valor: float
    etiqueta: str
    tipo: str
    evidencia: Evidencia | None = None

# Meta gana: componentes: list[Componente] = field(default_factory=list)
```

Helper ya existente en `pruebas/test_render_docx.py:33-42`, que extrae todo el texto de un
documento (párrafos y celdas de tabla) y sirve directamente para comparar antes/después:
```python
def texto(doc) -> str: ...
```

Firma del renderizado usada por esas pruebas (`test_render_docx.py:56`):
`render_docx.generar(curso, grupo, destino)` — `cfg` es opcional.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: La regresión — con y sin componentes</name>
  <files>pruebas/test_render_docx.py</files>
  <read_first>
    - `pruebas/test_render_docx.py` líneas 1-80 — imports, el helper `texto()`, y el
      `setUpClass`/`tearDownClass` de `RenderizadoDeBigData`, que es el molde de la clase nueva.
    - `src/render_docx.py` líneas 266-290 — la función que se va a tocar.
    - `src/modelo.py` — `Componente` y `Evidencia`, para construirlos en memoria.
  </read_first>
  <action>
Añade una clase nueva al final de `pruebas/test_render_docx.py`. No modifiques ninguna clase ni
método existente.

La clase renderiza el curso real de Big Data (que **no** declara componentes) como línea base, y
luego vuelve a renderizarlo con un componente añadido **en memoria** — nunca escribiendo en
`cursos/2026-2/39056-big-data/curso.yaml`, que en este plan no se toca.

```python
class EvidenciaDeComponente(unittest.TestCase):
    """La evidencia de un componente tiene que llegar al documento; su ausencia, no cambiarlo.

    La segunda mitad es la que defiende REQ-48: Big Data no declara componentes y su
    documento debe salir carácter por carácter igual que antes de esta fase.
    """

    @classmethod
    def setUpClass(cls):
        cls.tmp = Path(tempfile.mkdtemp(prefix="di-componente-"))
        cls.base = cls.render(modelo.cargar(CURSO), "base")

    @classmethod
    def tearDownClass(cls):
        shutil.rmtree(cls.tmp, ignore_errors=True)

    @classmethod
    def render(cls, curso, nombre):
        destino = cls.tmp / f"{nombre}.docx"
        render_docx.generar(curso, curso.grupos[0], destino)
        return texto(docx.Document(str(destino)))

    def test_una_lista_de_componentes_vacia_no_cambia_ni_un_caracter(self):
        """Es la mitad de REQ-48 que se puede probar sin generar el paquete completo."""
        curso = modelo.cargar(CURSO)
        for m in curso.metas:
            m.componentes = []
        self.assertEqual(self.base, self.render(curso, "vacios"))

    def test_la_evidencia_del_componente_se_concatena_a_las_de_su_meta(self):
        curso = modelo.cargar(CURSO)
        meta = next(m for m in curso.metas if m.evidencias)
        meta.componentes = [
            modelo.Componente(
                rubro="examenes", valor=15, etiqueta="Examen I", tipo="examen_parcial",
                evidencia=modelo.Evidencia(nombre="Examen I resuelto", tipo="examen"),
            )
        ]
        salida = self.render(curso, "con-componente")
        self.assertNotIn("Examen I resuelto", self.base)
        self.assertIn(f"{meta.evidencias[-1].nombre}, Examen I resuelto", salida)

    def test_un_componente_sin_evidencia_no_agrega_nada(self):
        """Un componente puede existir solo para imputar valor a otro rubro."""
        curso = modelo.cargar(CURSO)
        curso.metas[1].componentes = [
            modelo.Componente(rubro="examenes", valor=15, etiqueta="Examen I",
                              tipo="examen_parcial")
        ]
        self.assertEqual(self.base, self.render(curso, "sin-evidencia"))

    def test_la_columna_valor_sigue_en_porcentaje(self):
        """Frontera con la Fase 13: imprimir el valor en puntos no es de esta fase."""
        self.assertIn("10%", self.base)
```

Comprueba que `shutil`, `tempfile`, `Path`, `docx` y `modelo` ya estén importados en la cabecera
del archivo (lo están, líneas 13-28). No añadas imports nuevos.

Si `"10%"` no apareciera en el documento de Big Data, sustitúyelo por el valor real de alguna de
sus metas leído de su `curso.yaml` — pero **no** cambies el renderizado para que aparezca.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest pruebas.test_render_docx.EvidenciaDeComponente -v</automated>
  </verify>
  <acceptance_criteria>
    - `pruebas/test_render_docx.py` contiene `class EvidenciaDeComponente` con los cuatro métodos.
    - `python -X utf8 -m unittest pruebas.test_render_docx.EvidenciaDeComponente -v` falla **solo**
      en `test_la_evidencia_del_componente_se_concatena_a_las_de_su_meta`; los otros tres pasan.
    - `python -X utf8 -m unittest pruebas.test_render_docx.RenderizadoDeBigData -v` sale con código 0.
    - `git diff pruebas/test_render_docx.py` no muestra líneas eliminadas ni modificadas.
    - `git status --porcelain cursos/` sale vacío.
  </acceptance_criteria>
  <done>Las pruebas describen las dos mitades —la evidencia llega, la ausencia no cambia nada— y solo falla la que espera la funcionalidad nueva.</done>
</task>

<task type="auto">
  <name>Tarea 2: La celda de evidencias incluye las de los componentes</name>
  <files>src/render_docx.py</files>
  <read_first>
    - `src/render_docx.py` líneas 255-300 — `_filas_de_meta` y su entorno, para colocar el helper
      justo antes.
    - `src/render_docx.py` líneas 395-415 — el bloque de la Sección 3, que contiene el segundo
      `f"{meta.valor:g}%"`. Se lee para **confirmar que no se toca**.
    - `pruebas/test_render_docx.py` clase `EvidenciaDeComponente` — el comportamiento exacto que
      hay que satisfacer.
  </read_first>
  <action>
Añade a `src/render_docx.py`, inmediatamente **antes** de `def _filas_de_meta` (línea 266), esta
función:

```python
def _evidencias(meta: Meta) -> list[str]:
    """Los nombres de evidencia que se imprimen para una meta: las suyas y las de sus
    componentes.

    Una meta sin componentes produce exactamente la lista de siempre — de eso depende que
    los cursos que no declaran el rasgo salgan carácter por carácter iguales.
    """
    return [e.nombre for e in meta.evidencias] + [
        c.evidencia.nombre for c in meta.componentes if c.evidencia
    ]
```

Y cambia **solo** la celda de evidencias dentro de `_filas_de_meta` (líneas 283-284):

```python
        _celda(tr, 5 if dividida else 4,
               ", ".join(_evidencias(meta)) if ultima else "")
```

Eso es todo el cambio de renderizado de esta fase. En concreto, **no** toques:
- La línea 285-286, `f"{meta.valor:g}%"` de la columna Valor — Fase 13.
- El `f"{meta.valor:g}%"` del bloque de la Sección 3 (cerca de la línea 408) — Fase 13.
- Ninguna plantilla, ningún estilo, ningún prototipo clonado. Este cambio es **contenido**, no
  formato: no se acerca a `referencias/` ni a `plantillas/`.
- `src/estilo.py`: la evidencia del componente se imprime con el mismo énfasis que las demás
  evidencias de la meta, porque es la misma celda.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <acceptance_criteria>
    - `src/render_docx.py` contiene `def _evidencias(meta: Meta) -> list[str]:` y
      `", ".join(_evidencias(meta)) if ultima else ""`.
    - `src/render_docx.py` sigue conteniendo exactamente 2 ocurrencias de `f"{meta.valor:g}%"`
      (comprobable con `grep -c`), las mismas de antes.
    - `python -X utf8 -m unittest pruebas.test_render_docx -v` sale con código 0.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
    - `python -X utf8 src/plantillas.py verificar` sale con código 0: renderizar no escribió sobre
      ninguna plantilla.
    - `git diff --stat src/` solo lista `src/render_docx.py` en este plan.
  </acceptance_criteria>
  <done>La evidencia de un componente aparece junto a las de su meta, y un curso sin componentes produce el mismo documento de siempre.</done>
</task>

</tasks>

<verification>
- `python -X utf8 -m unittest discover -s pruebas` en verde.
- `python -X utf8 src/plantillas.py verificar` sale 0.
- `git status --porcelain cursos/` vacío: ningún `curso.yaml` se tocó para probar esto.
</verification>

<success_criteria>
La evidencia de un componente llega al documento, y el documento de un curso que no declara
componentes no cambia. El plan 09-05 lo confirmará de extremo a extremo con `huella verificar`.
</success_criteria>

<output>
Al terminar, crea `.planning/phases/09-valor-de-una-meta/09-04-SUMMARY.md`.
</output>
