---
phase: 09-valor-de-una-meta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/huella.py
  - pruebas/test_huella.py
  - pruebas/huellas.yaml
  - cursos/2026-2/39056-big-data/MANIFIESTO.yaml
  - cursos/2026-2/39062-patrones-de-comportamiento/MANIFIESTO.yaml
autonomous: true
requirements: [REQ-48]

must_haves:
  truths:
    - "Existe un comando que regenera 39056 y 39062 y compara tres huellas por documento —texto del .docx, informe de validación y forma del MANIFIESTO.yaml— contra las registradas (criterio 4 de la fase)"
    - "La forma del MANIFIESTO.yaml está vigilada: qué claves emite y con qué estructura, no sus valores por corrida (D-27, la mitad de REQ-48 que faltaba)"
    - "La línea base queda registrada con el repositorio TAL COMO ESTÁ HOY, antes de que ninguna tarea toque el modelo (paso 1 de D-15)"
    - "`huella verificar` es de solo lectura sobre el repo y no invoca git: restaura los MANIFIESTO.yaml con los bytes que leyó antes de generar (D-28)"
    - "La suite unitaria sigue siendo rápida: ninguna prueba de pruebas/ invoca generar.paquete() (D-18)"
  artifacts:
    - path: "src/huella.py"
      provides: "CLI con subcomandos verificar/registrar, extraer_texto(), forma_del_manifiesto() y el registro"
      contains: "def forma_del_manifiesto"
      exports: ["extraer_texto", "forma_del_manifiesto", "sha_texto", "cargar", "guardar", "verificar", "registrar", "main"]
    - path: "pruebas/huellas.yaml"
      provides: "Registro versionado de las cuatro huellas de control, con tres hashes cada una"
      contains: "manifiesto:"
    - path: "pruebas/test_huella.py"
      provides: "Pruebas rápidas y aisladas de extraer_texto(), forma_del_manifiesto() y el registro"
      contains: "class"
  key_links:
    - from: "src/huella.py"
      to: "src/generar.py"
      via: "generar.paquete(ruta, pdf=False, grupos=[...])"
      pattern: "generar\\.paquete\\("
    - from: "src/huella.py"
      to: "pruebas/huellas.yaml"
      via: "cargar()/guardar() con yaml.safe_dump"
      pattern: "yaml\\.safe_dump"
    - from: "src/huella.py:forma_del_manifiesto"
      to: "cursos/**/MANIFIESTO.yaml"
      via: "sha256 del manifiesto con las claves volátiles fuera"
      pattern: "VOLATILES_MANIFIESTO"
---

<objective>
Construir `src/huella.py` —el instrumento de la no contaminación (REQ-48) que heredan las cinco
fases siguientes— y **registrar con él la línea base del repositorio antes de tocar nada más**.

Propósito: es el paso 1 obligatorio de D-15. Si la línea base se registrara después de los cambios
del contrato, el único cambio de huella del milestone (el renombrado de D-14) quedaría sin medir y
el instrumento no demostraría nada.

Salida: `src/huella.py`, `pruebas/test_huella.py`, y `pruebas/huellas.yaml` con las cuatro entradas
de control (39056-961, 39056-962, 39062-971, 39062-972), cada una con sus **tres** hashes: el
texto del `.docx`, el informe de validación y la forma del `MANIFIESTO.yaml` (D-27).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-valor-de-una-meta/09-CONTEXT.md
@.planning/phases/09-valor-de-una-meta/09-PATTERNS.md
@src/plantillas.py
@src/generar.py

<interfaces>
<!-- Contratos ya existentes que el ejecutor consume. No hay que buscarlos en el código. -->

De `src/generar.py` (firma verificada, líneas 52-57 y 221-226):
```python
@dataclass
class Paquete:
    curso: Curso                 # modelo.Curso — tiene .ciclo, .clave, .grupos
    informe: validar.Informe     # tiene .texto() -> str, .valido -> bool
    archivos: list[Path]         # los .docx generados (y .pdf si pdf=True)
    manifiesto: Path

class ErrorGenerar(Exception): ...

def paquete(
    ruta_curso: Path | str,
    pdf: bool = True,
    traza=None,
    grupos: list[str] | None = None,
) -> Paquete: ...
```

De `src/validar.py` (línea 88): `Informe.texto()` devuelve el informe completo como cadena.
Es **por curso**, no por grupo: su cabecera dice «grupos 961, 962».

De `src/plantillas.py` (línea 55): `sha256(ruta: Path) -> str` hashea **archivos**, no cadenas.
Para hashear texto en memoria se usa un one-liner propio, no se toca esa función.

Nombre de archivo de un documento (`modelo.Curso.nombre_archivo`, línea 234):
`DI-{ciclo}-{clave}-{grupo}.docx` → el grupo se recupera con `a.stem.rsplit("-", 1)[-1]`.

Grupos reales declarados en los `curso.yaml` de control (D-25): 39056 → `961`, `962`;
39062 → `971`, `972`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Escribir src/huella.py — extracción, registro y CLI</name>
  <files>src/huella.py</files>
  <read_first>
    - `src/plantillas.py` (completo) — es el molde exacto: `CABECERA`, `ErrorPlantilla`,
      `cargar()`/`guardar()`, `verificar()`, `registrar()`, `main(argv)` con códigos de salida.
    - `src/generar.py` líneas 40-60 y 218-300 — `Paquete`, `ErrorGenerar`, `paquete()`.
    - `src/validar.py` líneas 70-104 — `Informe.texto()`.
    - `pruebas/test_render_docx.py` líneas 33-42 — el recorrido OOXML crudo
      (`tc.iter(qn("w:t"))`) que ya se usa y funciona en este repositorio.
  </read_first>
  <action>
Crea `src/huella.py`, un módulo nuevo. No modifiques ningún otro archivo en esta tarea.

**Docstring del módulo** (mismo espíritu que `src/comprobar.py`: decir qué defiende y qué hacer):
explica que compara la huella de texto de los documentos de control contra `pruebas/huellas.yaml`,
que es el instrumento de REQ-48 para todo el milestone v2.0, y que se corre a mano al cerrar cada
fase — no cuelga del ciclo de pruebas unitarias.

**Imports y constantes** (los módulos de `src/` se importan por nombre suelto, como ya hacen
`generar.py` y `render_docx.py`):

```python
from __future__ import annotations

import hashlib
import sys
from datetime import date
from pathlib import Path

import yaml
from docx import Document
from docx.oxml.ns import qn

import generar

RAIZ = Path(__file__).resolve().parent.parent
REGISTRO = RAIZ / "pruebas" / "huellas.yaml"

# Los documentos de control son los de Adrian (D-22/D-25). 38985 no entra: es el curso
# que va a cambiar en la Fase 14.
CONTROL = (
    ("cursos/2026-2/39056-big-data/curso.yaml", ("961", "962")),
    ("cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml", ("971", "972")),
)

CABECERA = (
    "# Huellas de texto de los documentos de control (REQ-48). Lo genera\n"
    "# src/huella.py: no lo edites a mano.\n"
    "#\n"
    "#     python src/huella.py verificar    # compara contra este registro\n"
    "#     python src/huella.py registrar    # acepta el estado actual como línea base\n"
    "#\n"
    "# Tres hashes por documento:\n"
    "#   texto_docx  el texto extraído del .docx — no su binario, cuyo zip lleva marcas\n"
    "#               de tiempo y cambiaría en cada corrida.\n"
    "#   informe     la salida de validar.py. Es por curso, así que se repite entre los\n"
    "#               grupos de un mismo curso: es correcto, no está duplicado por error.\n"
    "#   manifiesto  la FORMA del MANIFIESTO.yaml: qué claves emite y con qué estructura,\n"
    "#               sin `generado`, `commit` ni el sha256/bytes de los archivos, que\n"
    "#               cambian en cada corrida por diseño. También es por curso.\n"
)


class ErrorHuella(Exception):
    """Falta el registro, o un documento de control no se pudo generar."""
```

**Extracción determinista del texto** (D-19; receta verificada empíricamente en 09-RESEARCH.md
contra un `.docx` real). Recorre `document.element.body` porque `doc.paragraphs` y `doc.tables`
son listas separadas y **no** conservan el orden del documento. No uses `row.cells[i].text`: en
celdas con `vMerge` devuelve el texto de la celda de origen duplicado en cada fila que abarca.

```python
def _texto_de(elemento) -> str:
    """Todo el texto (`<w:t>`) dentro de un elemento OOXML, en orden de aparición."""
    return "".join(t.text or "" for t in elemento.iter(qn("w:t")))


def extraer_texto(ruta_docx: Path | str) -> str:
    """El texto del documento en orden real: encabezado, cuerpo (párrafos y tablas), pie.

    Recorre el XML crudo en vez de `doc.paragraphs`/`doc.tables`, que son listas
    separadas y no conservan el orden, y en vez de `row.cells[i].text`, que resuelve
    las celdas con `vMerge` duplicando el texto de la celda de origen.
    """
    doc = Document(str(ruta_docx))
    bloques: list[str] = []
    for sec in doc.sections:          # hoy están vacíos; no cuesta nada dejarlo listo
        for p in sec.header.paragraphs:
            if p.text.strip():
                bloques.append(p.text)
    for hijo in doc.element.body.iterchildren():
        tag = hijo.tag.rsplit("}", 1)[-1]
        if tag == "p":
            bloques.append(_texto_de(hijo))
        elif tag == "tbl":
            for tr in hijo.iter(qn("w:tr")):
                bloques.append(" | ".join(_texto_de(tc) for tc in tr.findall(qn("w:tc"))))
    for sec in doc.sections:
        for p in sec.footer.paragraphs:
            if p.text.strip():
                bloques.append(p.text)
    return "\n".join(bloques)


def sha_texto(texto: str) -> str:
    return hashlib.sha256(texto.encode("utf-8")).hexdigest()
```

**Forma del `MANIFIESTO.yaml`** (D-27). REQ-48 pide que el manifiesto no cambie de **forma**, y
esta es la función que lo vigila. Se le quita exactamente lo que cambia en cada corrida por diseño:
`generado` (marca de tiempo), `commit` (con su sufijo `-sucio`) y el `sha256`/`bytes` de cada
archivo producido (el zip del `.docx` cambia siempre). Lo que **se queda dentro** incluye el
`sha256` del propio `curso.yaml`: ese solo cambia cuando el curso cambia de verdad, y que la huella
lo note es justo lo que se quiere.

```python
# Lo que cambia en cada corrida por diseño y no dice nada sobre la forma (D-27).
VOLATILES_MANIFIESTO = ("generado", "commit")
VOLATILES_ARCHIVO = ("sha256", "bytes")


def forma_del_manifiesto(ruta: Path | str) -> str:
    """sha256 del MANIFIESTO.yaml con sus claves volátiles fuera.

    Vigila qué claves emite y con qué estructura, no sus valores por corrida. Si una fase
    posterior añadiera un bloque al manifiesto —el segundo nivel de la calificación, la
    rúbrica— este hash lo denuncia, que es la mitad de REQ-48 que la huella de texto no ve.
    """
    datos = yaml.safe_load(Path(ruta).read_text(encoding="utf-8")) or {}
    for clave in VOLATILES_MANIFIESTO:
        datos.pop(clave, None)
    for archivo in datos.get("archivos", []):
        for clave in VOLATILES_ARCHIVO:
            archivo.pop(clave, None)
    return sha_texto(
        yaml.safe_dump(datos, allow_unicode=True, sort_keys=False, default_flow_style=False)
    )
```

**Lectura y escritura del registro.** Mismo estilo de YAML que todo el repositorio
(`plantillas.guardar`, `generar.escribir_manifiesto`), y claves ordenadas para que el `git diff`
sea estable:

```python
def cargar() -> dict[str, dict]:
    """Lee `pruebas/huellas.yaml`. Devuelve {} si aún no existe."""
    if not REGISTRO.exists():
        return {}
    datos = yaml.safe_load(REGISTRO.read_text(encoding="utf-8")) or {}
    return datos.get("documentos", {})


def guardar(documentos: dict[str, dict]) -> None:
    cuerpo = {"documentos": {k: documentos[k] for k in sorted(documentos)}}
    texto = yaml.safe_dump(cuerpo, allow_unicode=True, sort_keys=False, default_flow_style=False)
    REGISTRO.write_text(CABECERA + texto, encoding="utf-8")
```

**Generación de los cuatro documentos.** Llama a `generar.paquete()` — nunca reconstruyas
validar→renderizar a mano (anti-patrón explícito de 09-RESEARCH.md). Con `pdf=False` no se abre
Word, así que corre en cualquier máquina.

`generar.paquete()` reescribe `MANIFIESTO.yaml` con un `generado:` y un `commit:` nuevos en cada
corrida, y esos dos archivos están versionados. **`verificar` restaura los bytes que leyó antes de
generar** (D-28): consigue que `verificar` sea de solo lectura sobre el repo, no destruye una
edición sin commitear que ya estuviera en el archivo, y no obliga a `huella.py` a invocar git.

**El orden importa y no es intercambiable.** El manifiesto se lee **antes** de generar, para poder
restaurarlo; y se hashea **después** de generar. Lo que se hashea es el manifiesto **recién
producido**, no el que se restaura: si se hasheara el restaurado, la huella mediría el archivo
viejo y jamás detectaría un cambio de forma. Por eso `forma_del_manifiesto()` se llama dentro del
`try`, justo después de `paquete()`, y la restauración vive en el `finally`, que corre después.

```python
def _generar_control(restaurar_manifiesto: bool) -> dict[str, dict]:
    """Regenera los .docx de control y devuelve sus huellas, indexadas por ciclo:clave:grupo."""
    actuales: dict[str, dict] = {}
    for rel, grupos in CONTROL:
        ruta = RAIZ / rel
        if not ruta.exists():
            raise ErrorHuella(f"Falta el curso de control {rel}.")
        manifiesto = ruta.parent / "MANIFIESTO.yaml"
        previo = manifiesto.read_bytes() if manifiesto.exists() else None
        try:
            paq = generar.paquete(ruta, pdf=False, grupos=list(grupos))
            # El manifiesto RECIÉN generado, antes de que el finally restaure el anterior.
            forma = forma_del_manifiesto(paq.manifiesto)
        except generar.ErrorGenerar as e:
            raise ErrorHuella(f"{rel}: no se pudo generar — {e}") from e
        finally:
            if restaurar_manifiesto and previo is not None:
                manifiesto.write_bytes(previo)
        informe = sha_texto(paq.informe.texto())
        for a in paq.archivos:
            if a.suffix != ".docx":
                continue
            grupo = a.stem.rsplit("-", 1)[-1]
            actuales[f"{paq.curso.ciclo}:{paq.curso.clave}:{grupo}"] = {
                "ciclo": paq.curso.ciclo,
                "clave": paq.curso.clave,
                "grupo": grupo,
                "texto_docx": sha_texto(extraer_texto(a)),
                "informe": informe,
                "manifiesto": forma,
            }
    return actuales


def _rotulo(clave: str) -> str:
    _, clave_materia, grupo = clave.split(":")
    return f"{clave_materia} grupo {grupo}"
```

**`verificar()`** devuelve `(problemas, intactos)`. Los mensajes siguen el molde de
`plantillas.verificar()` (`plantillas.py:251-257`): dicen **qué** cambió y **qué hacer**.

```python
CAMPOS = ("texto_docx", "informe", "manifiesto")
QUE_CAMBIO = {
    "texto_docx": "el texto del documento",
    "informe": "el informe de validación",
    "manifiesto": "la forma del MANIFIESTO.yaml",
}


def verificar() -> tuple[list[str], list[str]]:
    """Compara el estado actual contra el registro. No escribe nada (D-28)."""
    registradas = cargar()
    if not registradas:
        raise ErrorHuella(
            "No hay huellas registradas todavía. Registra la línea base con "
            "`python src/huella.py registrar` antes de comparar."
        )
    actuales = _generar_control(restaurar_manifiesto=True)
    problemas: list[str] = []
    intactos: list[str] = []
    for clave in sorted(set(registradas) | set(actuales)):
        vieja, nueva = registradas.get(clave), actuales.get(clave)
        if nueva is None:
            problemas.append(
                f"{_rotulo(clave)}: está registrado pero ya no se generó. "
                f"¿Cambió la lista de grupos del curso? Acepta el cambio con "
                f"`python src/huella.py registrar`."
            )
            continue
        if vieja is None:
            problemas.append(
                f"{_rotulo(clave)}: documento nuevo, sin huella registrada. "
                f"Regístralo con `python src/huella.py registrar`."
            )
            continue
        cambios = [c for c in CAMPOS if vieja.get(c) != nueva[c]]
        if not cambios:
            intactos.append(clave)
            continue
        for c in cambios:
            que = QUE_CAMBIO[c]
            problemas.append(
                f"{_rotulo(clave)}: cambió {que} ({vieja.get(c, '—')[:12]} → {nueva[c][:12]}). "
                f"Mira qué lo movió con `git diff` y, si el cambio es deliberado, acéptalo con "
                f"`python src/huella.py registrar`."
            )
    return problemas, intactos
```

**`registrar()`** devuelve las líneas a imprimir. Conserva la fecha `registrado` cuando la huella
no cambió, para que registrar dos veces seguidas no produzca ningún diff:

```python
def registrar() -> list[str]:
    """Acepta el estado actual como línea base.

    A diferencia de `verificar`, **sí** deja escritos los MANIFIESTO.yaml de los cursos de
    control: aquí el cambio es deliberado y debe quedar en el commit (D-23/D-28).
    """
    previas = cargar()
    actuales = _generar_control(restaurar_manifiesto=False)
    hoy = date.today().isoformat()
    lineas: list[str] = []
    for clave in sorted(actuales):
        nueva, vieja = actuales[clave], previas.get(clave)
        igual = vieja is not None and all(vieja.get(c) == nueva[c] for c in CAMPOS)
        nueva["registrado"] = vieja["registrado"] if igual and vieja.get("registrado") else hoy
        if vieja is None:
            lineas.append(f"+ {_rotulo(clave)}   huella registrada ({nueva['texto_docx'][:12]})")
        elif igual:
            lineas.append(f"= {_rotulo(clave)}   sin cambios")
        else:
            lineas.append(
                f"! {_rotulo(clave)}   {vieja['texto_docx'][:12]} → {nueva['texto_docx'][:12]}"
            )
    for clave in sorted(set(previas) - set(actuales)):
        lineas.append(f"- {_rotulo(clave)}   ya no se genera; se retira del registro")
    guardar(actuales)
    return lineas
```

**`main(argv)`** calca `plantillas.main` (`plantillas.py:303-337`): `verificar` por omisión,
código 0 si todo bien, 1 si hay problemas o `ErrorHuella`, 2 si la orden es desconocida.

```python
def main(argv: list[str]) -> int:
    orden = argv[1] if len(argv) > 1 else "verificar"
    try:
        if orden == "registrar":
            for linea in registrar():
                print(f"  {linea}")
            print(f"\nRegistro escrito en {REGISTRO.relative_to(RAIZ).as_posix()}.")
            print("Los MANIFIESTO.yaml de los cursos de control quedaron reescritos: van en el commit.")
        elif orden == "verificar":
            problemas, intactos = verificar()
            for clave in intactos:
                print(f"  ✓ {_rotulo(clave):<20} huella intacta")
            if problemas:
                for p in problemas:
                    print(f"  ✗ {p}", file=sys.stderr)
                print(f"\n{len(problemas)} problema(s).", file=sys.stderr)
                return 1
            print(f"\nTodo intacto. {len(intactos)} documentos comparados.")
        else:
            print(f"Orden desconocida: {orden}", file=sys.stderr)
            print("Uso: python src/huella.py [verificar|registrar]", file=sys.stderr)
            return 2
    except ErrorHuella as e:
        print(f"Error: {e}", file=sys.stderr)
        return 1
    return 0


if __name__ == "__main__":
    raise SystemExit(main(sys.argv))
```

No añadas nada más: ni `--json`, ni confirmación interactiva (descartada en D-21), ni ganchos en
`src/comprobar.py` ni en `pruebas/` (D-18).
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -c "import sys; sys.path.insert(0,'src'); import huella; print(huella.REGISTRO.name, len(huella.CONTROL), huella.sha_texto('x')[:8])"</automated>
  </verify>
  <acceptance_criteria>
    - `src/huella.py` existe y contiene `def extraer_texto`, `def forma_del_manifiesto`,
      `def sha_texto`, `def cargar`, `def guardar`, `def verificar`, `def registrar`, `def main`,
      `class ErrorHuella`.
    - `src/huella.py` contiene `VOLATILES_MANIFIESTO = ("generado", "commit")` y
      `CAMPOS = ("texto_docx", "informe", "manifiesto")`.
    - En `_generar_control`, la llamada a `forma_del_manifiesto(paq.manifiesto)` está **dentro del
      `try`**, antes del `finally` que restaura: se hashea el manifiesto generado, no el restaurado.
    - `src/huella.py` **no** contiene `git checkout` ni `import subprocess` (D-28: no invoca git).
    - `src/huella.py` contiene `generar.paquete(` y **no** contiene `render_docx.generar(`
      ni `validar.validar(` (se reusa la cadena, no se reimplementa).
    - `src/huella.py` contiene `yaml.safe_dump(cuerpo, allow_unicode=True, sort_keys=False, default_flow_style=False)`.
    - `src/huella.py` **no** contiene `.cells[` (prohibido por el efecto de `vMerge`).
    - `src/huella.py` contiene las cuatro claves de grupo `"961"`, `"962"`, `"971"`, `"972"`.
    - `python -X utf8 src/huella.py orden-inventada` sale con código 2.
    - `python -X utf8 src/plantillas.py verificar` sigue saliendo con código 0.
  </acceptance_criteria>
  <done>El módulo importa sin errores, expone las siete funciones y el CLI responde a una orden desconocida con código 2.</done>
</task>

<task type="auto">
  <name>Tarea 2: pruebas/test_huella.py — extracción determinista, sin generación completa</name>
  <files>pruebas/test_huella.py</files>
  <read_first>
    - `src/huella.py` (el que acaba de escribirse) — para probar la firma real, no la supuesta.
    - `pruebas/test_plantillas.py` líneas 1-40 — la clase base `EnDirectorioTemporal` y el
      bootstrap de `sys.path`.
    - `pruebas/test_render_docx.py` líneas 11-42 — imports de `docx` y `qn`.
  </read_first>
  <action>
Crea `pruebas/test_huella.py`. **Ninguna prueba de este archivo puede invocar
`generar.paquete()`** (D-18: la suite unitaria corre hoy en ~13 s y no depende de las plantillas;
colgarle la generación de dos cursos completos la rompería). Todo se prueba contra un `.docx`
sintético armado en memoria con `docx.Document()`.

Docstring del módulo, en el tono de `pruebas/test_validar.py`: explica que estas pruebas defienden
que la huella sea **determinista** —si el texto extraído variara entre corridas, el instrumento de
REQ-48 daría falsos positivos y nadie volvería a mirarlo— y que deliberadamente no generan ningún
curso real.

Bootstrap idéntico al del resto de `pruebas/`:

```python
from __future__ import annotations

import shutil
import sys
import tempfile
import unittest
from pathlib import Path

import docx

RAIZ = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(RAIZ / "src"))

import huella  # noqa: E402
```

Helper que arma el documento sintético (párrafo, tabla con una celda fusionada verticalmente,
párrafo final) y lo guarda en un temporal:

```python
def documento_sintetico(destino: Path) -> Path:
    """Un .docx mínimo con lo único que complica la extracción: una celda fusionada."""
    doc = docx.Document()
    doc.add_paragraph("Antes de la tabla")
    tabla = doc.add_table(rows=2, cols=2)
    tabla.cell(0, 0).text = "Meta 0. Encuadre del curso."
    tabla.cell(0, 1).text = "Presencial"
    tabla.cell(1, 1).text = "Virtual"
    tabla.cell(0, 0).merge(tabla.cell(1, 0))   # vMerge: la fila 2 no tiene texto propio
    doc.add_paragraph("Después de la tabla")
    doc.save(str(destino))
    return destino
```

Clase base que aísla el registro real, calcando `EnDirectorioTemporal` de `test_plantillas.py`:

```python
class EnRegistroTemporal(unittest.TestCase):
    """Nunca toca pruebas/huellas.yaml de verdad."""

    def setUp(self):
        self.tmp = Path(tempfile.mkdtemp(prefix="di-huella-"))
        self._registro = huella.REGISTRO
        huella.REGISTRO = self.tmp / "huellas.yaml"

    def tearDown(self):
        huella.REGISTRO = self._registro
        shutil.rmtree(self.tmp, ignore_errors=True)
```

Pruebas exigidas, con nombre en español y docstring que dice **qué defiende** cada una:

1. `class ExtraccionDeTexto(EnRegistroTemporal)`
   - `test_el_texto_extraido_es_determinista` — extraer dos veces el mismo `.docx` da el mismo
     `sha_texto`. Defiende que el instrumento no dé falsos positivos.
   - `test_una_celda_fusionada_no_duplica_su_texto` — 
     `self.assertEqual(1, huella.extraer_texto(ruta).count("Meta 0. Encuadre del curso."))`.
     Defiende contra el efecto de `row.cells[i].text` documentado en 09-RESEARCH.md.
   - `test_el_texto_respeta_el_orden_del_documento` — el índice de `"Antes de la tabla"` es menor
     que el de `"Meta 0."`, y este menor que el de `"Después de la tabla"`. Defiende que se
     recorra `body` y no las listas separadas `paragraphs`/`tables`.
   - `test_el_texto_de_la_tabla_lleva_todas_sus_celdas` — `"Presencial"` y `"Virtual"` aparecen
     en el texto extraído.

2. `class RegistroDeHuellas(EnRegistroTemporal)`
   - `test_el_registro_va_y_vuelve` — `guardar({...})` y luego `cargar()` devuelve el mismo mapa.
   - `test_el_registro_lleva_su_cabecera_de_no_editar` — el archivo escrito empieza por `"#"` y
     su texto contiene `"no lo edites a mano"`.
   - `test_sin_registro_cargar_devuelve_vacio` — `cargar()` es `{}` si el archivo no existe.
   - `test_verificar_sin_registro_es_error_huella` — `assertRaises(huella.ErrorHuella)` y el
     mensaje contiene `"registrar"`. **Esta prueba debe fallar antes de generar nada**: con el
     registro temporal vacío, `verificar()` lanza `ErrorHuella` en su primera línea, sin llegar a
     `_generar_control`. Verifícalo tras escribirla: si la prueba tarda más de un segundo, el
     orden de las comprobaciones dentro de `verificar()` está mal.

3. `class FormaDelManifiesto(EnRegistroTemporal)` — D-27. Se prueba contra un YAML sintético
   escrito en el temporal, **nunca** contra el `MANIFIESTO.yaml` de un curso real:

   ```python
   MANIFIESTO_DE_PRUEBA = {
       "generado": "2026-08-05T10:00:00",
       "commit": "abc1234",
       "curso": {"archivo": "cursos/x/curso.yaml", "sha256": "aa" * 32},
       "evaluacion": {"esquema_id": "estandar-2026",
                      "rubros": [{"id": "tareas", "porcentaje": 40}]},
       "archivos": [{"ruta": "salida/DI.docx", "grupo": "961",
                     "sha256": "bb" * 32, "bytes": 233051}],
   }
   ```

   - `test_la_marca_de_tiempo_y_el_commit_no_cuentan` — escribe el manifiesto dos veces
     cambiando solo `generado` y `commit`, y comprueba que
     `forma_del_manifiesto` devuelve el mismo hash. Defiende que la huella no dé un falso
     positivo en cada corrida.
   - `test_el_sha256_y_los_bytes_de_los_archivos_no_cuentan` — cambia el `sha256` y los `bytes`
     de la entrada de `archivos`: mismo hash. Defiende contra el zip del `.docx`, que cambia
     siempre.
   - `test_una_clave_nueva_cambia_la_forma` — añade `{"nivel_2": {"promedio": 60,
     "ordinario": 40}}` al manifiesto y comprueba que el hash **cambia**. Es la prueba que da
     sentido a las otras dos: si esto no cambiara, la función no vigilaría nada.
   - `test_el_sha256_del_curso_si_cuenta` — cambia `curso.sha256`: el hash cambia. Un curso
     distinto no puede pasar por el mismo manifiesto.
   - `test_un_archivo_de_mas_cambia_la_forma` — añade una segunda entrada a `archivos`: el hash
     cambia, aunque sus `sha256`/`bytes` se hayan quitado. Se vigila la estructura, no los valores.

No escribas ninguna prueba de `_generar_control`, `registrar()` completo ni de los cursos reales.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest pruebas.test_huella -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -X utf8 -m unittest pruebas.test_huella -v` sale con código 0 y reporta al menos
      13 pruebas, repartidas en las tres clases.
    - `pruebas/test_huella.py` contiene `class FormaDelManifiesto` y
      `huella.forma_del_manifiesto(`.
    - `pruebas/test_huella.py` **no** contiene la cadena `generar.paquete` ni `import generar`.
    - `pruebas/test_huella.py` contiene `huella.REGISTRO = self.tmp` (el registro real queda aislado).
    - `git status --porcelain pruebas/huellas.yaml` sale vacío después de correr las pruebas
      (todavía no existe el registro real y las pruebas no lo crearon).
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y tarda menos de 25 s.
  </acceptance_criteria>
  <done>Las pruebas de `extraer_texto()`, de `forma_del_manifiesto()` y del registro pasan, y la suite completa sigue corriendo en su tiempo de siempre.</done>
</task>

<task type="auto">
  <name>Tarea 3: Registrar la línea base del repositorio (paso 1 de D-15)</name>
  <files>pruebas/huellas.yaml, cursos/2026-2/39056-big-data/MANIFIESTO.yaml, cursos/2026-2/39062-patrones-de-comportamiento/MANIFIESTO.yaml</files>
  <read_first>
    - `src/huella.py` — para saber exactamente qué escribe cada subcomando.
    - `.planning/phases/09-valor-de-una-meta/09-CONTEXT.md` §D-15 y §D-24 — el orden de los pasos
      de la fase, que esta tarea abre.
  </read_first>
  <action>
Este es el **paso 1 de la secuencia obligatoria de D-15**: la línea base se toma del repositorio
tal como está HOY, antes de que ninguna tarea toque `src/modelo.py`. No adelantes ni pospongas
este paso.

1. Confirma que el árbol de trabajo no trae cambios previos en los cursos de control:
   `git status --porcelain cursos/` debe salir vacío. Si no lo está, **para** y reporta: la línea
   base se estaría tomando sobre un estado que no es el del commit.

2. Corre `python -X utf8 src/huella.py registrar`.
   Debe imprimir cuatro líneas `+ … huella registrada`, una por cada documento de control:
   `39056 grupo 961`, `39056 grupo 962`, `39062 grupo 971`, `39062 grupo 972`.
   Si imprime menos de cuatro, o alguna clave distinta, **para**: la lista `CONTROL` no coincide
   con los grupos que declaran los `curso.yaml` (D-25).

3. Corre `python -X utf8 src/huella.py verificar`. Debe imprimir las cuatro líneas
   `✓ … huella intacta`, cerrar con `Todo intacto. 4 documentos comparados.` y salir con código 0.

4. Comprueba que `verificar` fue de solo lectura (D-23): tras el paso 3,
   `git status --porcelain cursos/` **no** debe listar ningún `MANIFIESTO.yaml`. Si los lista, la
   restauración de `_generar_control(restaurar_manifiesto=True)` no está funcionando — arréglala
   antes de seguir.

   Nota: los `MANIFIESTO.yaml` sí quedaron modificados por el paso 2 (`registrar` los deja
   escritos a propósito, D-23) — es correcto y van en el commit de este plan. Lo que el paso 4
   comprueba es que `verificar` no los vuelva a mover.

5. Corre `python -X utf8 src/huella.py registrar` una segunda vez y comprueba que
   `git diff --stat pruebas/huellas.yaml` no reporta cambios: la fecha `registrado` se conserva
   cuando la huella no cambió, así que registrar dos veces es idempotente.

6. Revisa a ojo `pruebas/huellas.yaml`: cabecera de comentarios presente, cuatro entradas bajo
   `documentos:`, cada una con `ciclo`, `clave`, `grupo`, `texto_docx`, `informe`, `manifiesto`,
   `registrado`. Los campos `informe` y `manifiesto` se repiten entre los dos grupos de un mismo
   curso — es correcto y esperado: tanto `Informe.texto()` como el `MANIFIESTO.yaml` son por curso,
   no por grupo, y la cabecera del archivo ya lo explica.

7. Un cambio esperado en el diff que conviene no confundir con un fallo: `huella registrar` genera
   con `pdf=False`, así que el `MANIFIESTO.yaml` que deja escrito lista **solo los `.docx`**,
   mientras que el commiteado hoy lista también los `.pdf` (se generó con Word). Es coherente con
   el diseño del manifiesto —«solo los de esta corrida»— y con que `salida/` esté en `.gitignore`.
   Desde aquí, la línea base y todas las verificaciones siguientes se miden siempre con `pdf=False`,
   así que la comparación es consistente. Anótalo en el SUMMARY para que nadie lo lea como pérdida
   de trazabilidad.

No toques `cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` ni
`cursos/2026-2/38985-contabilidad-financiera/curso.yaml` en ningún momento.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 src/huella.py verificar && git status --porcelain cursos/</automated>
  </verify>
  <acceptance_criteria>
    - `pruebas/huellas.yaml` existe, empieza por `#` y contiene `documentos:`.
    - `pruebas/huellas.yaml` contiene las cuatro claves `"2026-2:39056:961"`, `"2026-2:39056:962"`,
      `"2026-2:39062:971"`, `"2026-2:39062:972"`.
    - `pruebas/huellas.yaml` contiene exactamente 4 líneas `texto_docx:`, 4 `informe:` y
      4 `manifiesto:` — tres hashes por documento (D-27).
    - `python -X utf8 src/huella.py verificar` sale con código 0 e imprime
      `Todo intacto. 4 documentos comparados.`
    - `git status --porcelain cursos/` sale **vacío** inmediatamente después de `verificar`
      (D-23: verificar es de solo lectura).
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
    - `python -X utf8 src/plantillas.py verificar` sale con código 0 (nada escribió sobre una plantilla).
  </acceptance_criteria>
  <done>La línea base de las cuatro huellas está registrada y versionada, y `verificar` corre en verde sin ensuciar el árbol.</done>
</task>

</tasks>

<verification>
- `python -X utf8 -m unittest discover -s pruebas` en verde (179 anteriores + las de `test_huella`).
- `python -X utf8 src/huella.py verificar` sale 0 con los cuatro documentos intactos.
- `git status --porcelain cursos/` vacío tras `verificar`.
- `python -X utf8 src/plantillas.py verificar` sale 0.
- Ningún archivo de `referencias/` ni de `ejemplos/` aparece en `git status`.
</verification>

<success_criteria>
Existe el comando que regenera 39056 y 39062 y compara su huella contra la registrada, corre en
verde, y la línea base quedó tomada **antes** de que la fase toque el modelo (criterio 4 del
ROADMAP §Fase 9; paso 1 de D-15).
</success_criteria>

<output>
Al terminar, crea `.planning/phases/09-valor-de-una-meta/09-01-SUMMARY.md`.
</output>

