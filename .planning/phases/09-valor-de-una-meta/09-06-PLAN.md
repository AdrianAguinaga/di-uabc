---
phase: 09-valor-de-una-meta
plan: 06
type: execute
wave: 5
depends_on: ["09-05"]
files_modified: []
autonomous: false
requirements: [REQ-48]

must_haves:
  truths:
    - "El .docx de 39056 renombrado abre en Word sin pedir reparar el archivo"
    - "El diff completo de la fase lo revisó una persona y solo contiene lo que la fase prometía"
    - "El grafo regenerado dice lo que debe decir: la auditoría cierra igual que antes del renombrado"
  artifacts: []
  key_links: []
---

<objective>
Cerrar la fase con las comprobaciones que ninguna prueba automatizada puede hacer: que el documento
renombrado abre en Word, que dice «Meta 1.0.» donde decía «Meta 0.», y que el diff completo de la
fase es exactamente lo que la fase prometía y nada más.

Propósito: la Fase 10 hereda el instrumento y el contrato tal como queden aquí. El `.docx` es un
entregable con valor legal; que abra en Word no lo puede afirmar el agente.

La pregunta que este plan traía sobre la forma del `MANIFIESTO.yaml` ya está resuelta: D-27 la
cerró —sí entra en la huella, como tercer hash— y su implementación vive en el plan 09-01. Aquí no
queda nada que decidir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-valor-de-una-meta/09-CONTEXT.md
@.planning/phases/09-valor-de-una-meta/09-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 1: El documento renombrado abre en Word y el diff es el que se prometió</name>
  <files>ninguno</files>
  <read_first>
    - `.planning/phases/09-valor-de-una-meta/09-VALIDATION.md` §Verificaciones solo manuales.
    - El SUMMARY del plan 09-05, con el `git diff --stat` que dejó registrado.
  </read_first>
  <what-built>
    Los cinco planes anteriores abrieron el contrato `curso.yaml` en tres puntos (rubros en puntos,
    componentes de meta, identificadores libres), construyeron `src/huella.py` con su registro
    versionado, renombraron el encuadre de Big Data a `1.0` y regeneraron el grafo. Todo lo
    automatizable ya corrió en verde: la suite completa de pruebas, `huella verificar`,
    `plantillas verificar` y `validar.py` sobre los tres cursos.
  </what-built>
  <how-to-verify>
    1. Regenera el documento de Big Data si no está en disco:
       `python -X utf8 src/generar.py cursos/2026-2/39056-big-data/curso.yaml --sin-pdf`
    2. Abre `cursos/2026-2/39056-big-data/salida/DI-2026-2-39056-961.docx` en Word.
       **Word no debe pedir reparar el archivo.** Si lo pide, algo del XML quedó mal formado y hay
       que parar aquí.
    3. En la tabla de la Sección 2, la primera fila de meta debe decir **«Meta 1.0. Encuadre del
       curso.»** — antes decía «Meta 0.». El resto de la tabla debe verse exactamente igual que
       antes: mismas fechas, mismas actividades, mismas evidencias, mismos porcentajes.
    4. En la Sección 3, el primer bloque debe encabezarse igual, con «Meta 1.0.».
    5. Revisa el diff completo de la fase con `git diff --stat` y `git diff cursos/`.
       Lo único que debe aparecer bajo `cursos/` es **una** línea de
       `39056-big-data/curso.yaml` (`id: "0"` → `id: "1.0"`) y los dos `MANIFIESTO.yaml`
       regenerados. Si aparece cualquier otra cosa —sobre todo si aparece
       `39062-patrones-de-comportamiento/curso.yaml` o
       `38985-contabilidad-financiera/curso.yaml`— hay que parar.
    6. Confirma que `referencias/`, `ejemplos/` y `plantillas/` no aparecen en el diff.
    7. Abre `grafo/AUDITORIA.md` y confirma que cierra igual que antes del renombrado: 0 anclas
       rotas y el único hueco conocido, «Contabilidad Financiera · 2026-2» (falta el PUA 38985).
       Y `grafo/index.html` en el navegador, buscando «Meta 1.0.»: no debe quedar ninguna
       «Meta 0.» del curso 39056.
  </how-to-verify>
  <acceptance_criteria>
    - El `.docx` abre en Word sin diálogo de reparación.
    - La Sección 2 y la Sección 3 dicen «Meta 1.0.» donde antes decían «Meta 0.».
    - `git diff --numstat cursos/2026-2/39056-big-data/curso.yaml` reporta `1	1`.
    - `git status --porcelain cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` sale vacío.
    - `git diff --stat` no lista nada de `referencias/`, `ejemplos/` ni `plantillas/`.
    - `grafo/AUDITORIA.md` no reporta anclas rotas ni huecos nuevos.
  </acceptance_criteria>
  <action>
Antes de convocar al usuario, deja el terreno preparado: corre
`python -X utf8 src/generar.py cursos/2026-2/39056-big-data/curso.yaml --sin-pdf` para que el
`.docx` esté en disco, y ten a mano la salida de `git diff --stat` y de `git diff cursos/`.
Después presenta el punto de verificación y **espera** la respuesta del usuario: abrir Word y
juzgar el diff completo de la fase no lo puede hacer el agente.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && git diff --numstat cursos/2026-2/39056-big-data/curso.yaml && git status --porcelain cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml</automated>
  </verify>
  <done>El usuario confirmó que el documento abre en Word, que dice «Meta 1.0.» y que el diff de la fase no contiene nada más de lo prometido.</done>
  <resume-signal>Escribe «aprobado» o describe qué encontraste.</resume-signal>
</task>

</tasks>

<verification>
- Word abrió el documento sin pedir reparación.
- El diff de la fase contiene solo lo que la fase prometía.
- `grafo/AUDITORIA.md` cierra igual que antes del renombrado.
</verification>

<success_criteria>
Los cinco criterios del ROADMAP §Fase 9 quedan verificados: los cuatro primeros por los planes
anteriores, el quinto (`unittest discover`) en cada uno de ellos, y el documento entregable
comprobado por una persona en la aplicación en la que se va a abrir de verdad.
</success_criteria>

<output>
Al terminar, crea `.planning/phases/09-valor-de-una-meta/09-06-SUMMARY.md`.
</output>
