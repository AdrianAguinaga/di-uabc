---
phase: 09-valor-de-una-meta
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/modelo.py
  - pruebas/test_modelo.py
autonomous: true
requirements: [REQ-38, REQ-39, REQ-42]

must_haves:
  truths:
    - "Un curso.yaml que declare «Actividades 30 %» con valores en puntos y total 150, junto a «Exámenes 50 %» en porcentaje, carga sin ErrorModelo (criterio 1)"
    - "Una meta con un componente adicional sigue siendo una sola meta con una sola semana: ni len(curso.metas) ni sus semanas cambian (criterio 2)"
    - "Metas con id 1.0, 2.0 y 6.0 cargan y conservan el orden declarado en el YAML (criterio 3)"
    - "Una unidad de rubro desconocida, un rubro en puntos sin total, y un tipo de componente fuera del vocabulario son ErrorModelo al cargar"
    - "La conversión de puntos a porcentaje vive en un solo sitio del modelo y las Fases 10 y 13 la consumen en vez de reinventarla"
    - "Los tres curso.yaml existentes siguen cargando sin tocarse: todos sus rubros quedan en unidad porcentaje y todas sus metas sin componentes"
  artifacts:
    - path: "src/modelo.py"
      provides: "Rubro.unidad/total/a_porcentaje, dataclass Componente, Meta.componentes"
      contains: "TIPOS_COMPONENTE"
      exports: ["Componente", "TIPOS_COMPONENTE", "UNIDADES_RUBRO"]
    - path: "pruebas/test_modelo.py"
      provides: "Pruebas de esquema del contrato nuevo (Ola 0 de 09-VALIDATION.md)"
      contains: "class RubroEnPuntos"
  key_links:
    - from: "src/modelo.py:_construir_meta"
      to: "src/modelo.py:_construir_componente"
      via: "d.pop('componentes', [])"
      pattern: "d\\.pop\\(\"componentes\""
    - from: "src/modelo.py:Rubro.a_porcentaje"
      to: "Rubro.porcentaje y Rubro.total"
      via: "valor / base * porcentaje"
      pattern: "def a_porcentaje"
---

<objective>
Abrir el contrato `curso.yaml` en los tres puntos de la fase: rubros en puntos (REQ-38),
componentes de meta (REQ-39) e identificadores libres (REQ-42). Todo dentro de `src/modelo.py`.

Propósito: `curso.yaml` es el contrato entre planeación y renderizado. Mientras el modelo no sepa
decir «10 pts de 150» ni «esta meta también aporta 15 % a exámenes», un DI redactado al estilo de
Zurisaddai no puede existir sin traducirse.

Salida: `src/modelo.py` extendido (nunca reemplazado) y `pruebas/test_modelo.py`, archivo nuevo
que hoy no existe — las pruebas de carga vivían implícitamente dentro de `test_validar.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-valor-de-una-meta/09-CONTEXT.md
@.planning/phases/09-valor-de-una-meta/09-PATTERNS.md
@src/modelo.py
@pruebas/test_validar.py

<interfaces>
<!-- Estado literal del código a extender. Verificado por lectura directa. -->

`src/modelo.py:30-32` — el bloque de vocabularios cerrados donde entran los nuevos:
```python
MODALIDADES = ("semipresencial", "escolarizada", "a_distancia")
TIPOS_META = ("encuadre", "aprendizaje", "examen_parcial", "cierre")
AMBIENTES = ("presencial", "virtual")
```

`src/modelo.py:91-95` — `Evidencia`, el dataclass que el componente reusa tal cual:
```python
@dataclass
class Evidencia:
    nombre: str
    tipo: str = ""
    recurso: str = ""  # p. ej. "M1.1_Mapa conceptual"
```

`src/modelo.py:158-165` — `Rubro` HOY (se extiende, no se reemplaza):
```python
@dataclass
class Rubro:
    id: str
    etiqueta: str
    porcentaje: float
    detalle: str = ""
    parciales: int = 0
```

`src/modelo.py:241-250` — `_construir_meta` HOY:
```python
def _construir_meta(d: dict) -> Meta:
    sesiones = [Sesion(**s) for s in d.pop("sesiones", [])]
    evidencias = [
        Evidencia(**e) if isinstance(e, dict) else Evidencia(nombre=e)
        for e in d.pop("evidencias", [])
    ]
    semanas = d.pop("semanas", None)
    if semanas is None and (s := d.pop("semana", None)) is not None:
        semanas = [s]
    return Meta(semanas=semanas or [], sesiones=sesiones, evidencias=evidencias, **d)
```

`src/modelo.py:276` — `desde_dict` construye los rubros con `Rubro(**r)`; cualquier clave nueva
del YAML sin campo en el dataclass revienta con `TypeError`, capturado en `cargar()` (`:322-323`).
**No hay que tocar `desde_dict`.**

`src/modelo.py:311-323` — `cargar(ruta)` no necesita `Config`: solo lee YAML y llama `desde_dict`.
Las pruebas pueden cargar los `curso.yaml` reales sin montar nada.

Helpers ya existentes en `pruebas/test_validar.py` (líneas 26-116) que `test_modelo.py` puede
imitar o importar: `_meta(id_, unidad, semanas, valor, rubro, tipo="aprendizaje")`,
el diccionario `CURSO_VALIDO`, y `curso(**cambios)` que hace `deepcopy` + `modelo.desde_dict`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: pruebas/test_modelo.py — la Ola 0 del contrato nuevo</name>
  <files>pruebas/test_modelo.py</files>
  <read_first>
    - `pruebas/test_validar.py` líneas 1-120 — el bootstrap de `sys.path`, el helper `_meta`,
      el diccionario `CURSO_VALIDO` y el helper `curso(**cambios)`.
    - `src/modelo.py` líneas 88-165 y 238-324 — el estado actual de los dataclasses y de la carga,
      para que las pruebas apunten a la API real.
    - `.planning/phases/09-valor-de-una-meta/09-VALIDATION.md` §Mapa de verificación — la lista
      exacta de comportamientos que este archivo debe cubrir.
  </read_first>
  <action>
Crea `pruebas/test_modelo.py`. **Se escribe antes que la implementación**: al terminar esta tarea
las pruebas deben fallar señalando exactamente los campos que faltan, y las tareas 2 y 3 las ponen
en verde. Ese fallo es el resultado esperado de esta tarea, no un error.

Docstring del módulo, en el tono de `pruebas/test_validar.py`:

```python
"""Pruebas del esquema de `curso.yaml`.

`validar.py` comprueba que las cantidades cuadren; esto comprueba que el contrato
**cargue**. Son cosas distintas y fallan distinto: un rubro cuya unidad no existe es un
defecto de esquema y revienta al cargar; un rubro cuyas metas no suman lo declarado carga
bien y lo reporta una regla.

La mitad de la no contaminación (REQ-48) se defiende aquí: los tres `curso.yaml` que ya
existen deben seguir cargando sin declarar ninguna clave nueva.
"""
```

Bootstrap idéntico al del resto de `pruebas/` (`from __future__ import annotations`, `copy`,
`sys`, `unittest`, `Path`, `RAIZ`, `sys.path.insert(0, str(RAIZ / "src"))`, `import modelo`).
Importa además los helpers de la fixture existente para no duplicar 80 líneas de curso válido:

```python
from test_validar import CURSO_VALIDO, _meta  # noqa: E402
```

Si ese import fallara al correr con `unittest discover` (el directorio `pruebas/` sí está en el
path del descubrimiento, así que debería funcionar), usa `from pruebas.test_validar import ...`
como alternativa; no copies la fixture.

Helper local para armar un curso con la evaluación cambiada:

```python
def curso_con(rubros, metas=None):
    """Copia del curso válido con otros rubros (y, si se dan, otras metas)."""
    d = copy.deepcopy(CURSO_VALIDO)
    d["evaluacion"]["rubros"] = rubros
    if metas is not None:
        d["metas"] = metas
    return modelo.desde_dict(d)
```

Escribe estas cuatro clases, con los nombres de método en español y un docstring de una línea que
diga **qué defecto real evita** cada prueba:

**1. `class RubroEnPuntos(unittest.TestCase)`** — REQ-38, criterio 1:
- `test_puntos_y_porcentaje_conviven_en_el_mismo_curso` — construye los dos rubros literales del
  531 y comprueba que cargan y que `unidad`/`total` llegan al objeto:
  ```python
  rubros = [
      {"id": "actividades", "etiqueta": "Entrega de actividades en clases y tareas",
       "porcentaje": 30, "unidad": "puntos", "total": 150},
      {"id": "examenes", "etiqueta": "Exámenes", "porcentaje": 50},
      {"id": "proyecto", "etiqueta": "Proyecto final", "porcentaje": 20},
  ]
  ```
  Verifica `c.rubro("actividades").unidad == "puntos"`, `c.rubro("actividades").total == 150`,
  y `c.rubro("examenes").unidad == ""` (ausente significa porcentaje).
- `test_unidad_desconocida_es_error_modelo` — `"unidad": "creditos"` → `assertRaises(modelo.ErrorModelo)`,
  y el mensaje contiene `"creditos"` y `"puntos"` (dice qué está mal y qué vale).
- `test_puntos_sin_total_es_error_modelo` — `"unidad": "puntos"` sin `total` →
  `ErrorModelo` cuyo mensaje contiene `"total"`.
- `test_el_total_no_se_infiere_de_la_suma_de_las_metas` — el mismo caso anterior, con metas que
  suman 140 imputadas a ese rubro: sigue siendo `ErrorModelo`. Docstring: inferirlo haría
  invisible el defecto real del 531 —150 declarados contra 140 sumados— que la Fase 10 debe atrapar.
- `test_convierte_puntos_a_porcentaje` — con `porcentaje: 30, total: 150`,
  `assertAlmostEqual(2.0, rubro.a_porcentaje(10))`.
- `test_un_rubro_en_porcentaje_se_convierte_en_si_mismo` — con `porcentaje: 50` y sin `unidad`,
  `assertAlmostEqual(15, rubro.a_porcentaje(15))`. Docstring: es la identidad que permite a las
  Fases 10 y 13 llamar siempre a la misma función sin preguntar la unidad.

**2. `class ComponentesDeMeta(unittest.TestCase)`** — REQ-39, criterio 2:
- `test_una_meta_con_componente_sigue_siendo_una_meta_con_una_semana` — la prueba del criterio 2.
  Carga el curso válido dos veces: una tal cual y otra añadiendo a la meta `2.1` el componente
  literal del 531, y compara:
  ```python
  componente = {
      "rubro": "examenes", "valor": 15, "etiqueta": "Examen I", "tipo": "examen_parcial",
      "evidencia": {"nombre": "Examen I resuelto", "tipo": "examen",
                    "recurso": "M2.4_Examen I"},
  }
  ```
  `assertEqual(len(sin.metas), len(con.metas))` y
  `assertEqual(sin.metas[4].semanas, con.metas[4].semanas)` (usa el índice o busca la meta por id,
  pero compara semanas, no solo longitudes).
- `test_el_componente_conserva_sus_cinco_campos` — `rubro`, `valor`, `etiqueta`, `tipo` y
  `evidencia.nombre` llegan íntegros al objeto.
- `test_la_evidencia_del_componente_acepta_la_forma_corta` — `"evidencia": "Examen I resuelto"`
  (una cadena) produce un `modelo.Evidencia` con ese `nombre`.
- `test_un_componente_sin_evidencia_carga` — sin la clave, `comp.evidencia is None`.
- `test_tipo_de_componente_fuera_del_vocabulario_es_error_modelo` — `"tipo": "examen_partial"` →
  `ErrorModelo` cuyo mensaje contiene `"examen_partial"` y `"examen_parcial"`.
- `test_componente_sin_tipo_es_error_modelo` — sin la clave `tipo` → `ErrorModelo` cuyo mensaje
  contiene `"tipo"`. Docstring: no hay valor por omisión a propósito; un default silencioso
  convertiría un typo en un componente que nadie cuenta.
- `test_el_valor_del_componente_se_lee_en_la_unidad_de_su_propio_rubro` — meta imputada a un rubro
  en puntos (`valor: 10` = 10 pts) con un componente en un rubro en porcentaje (`valor: 15` = 15 %);
  comprueba con `a_porcentaje` de cada rubro que 10 pts → 2 % y 15 % → 15 %.

**3. `class IdentificadoresLibres(unittest.TestCase)`** — REQ-42, criterio 3:
- `test_ids_terminados_en_punto_cero_cargan` — metas con id `"1.0"`, `"2.0"`, `"6.0"` cargan.
- `test_se_conserva_el_orden_declarado` — declara las metas en un orden que **no** es el numérico
  (por ejemplo `"6.0"`, `"1.0"`, `"2.0"`) y comprueba
  `assertEqual(["6.0", "1.0", "2.0"], [m.id for m in c.metas])`. Docstring: nada en `src/` ordena
  las metas y esta prueba lo fija; si alguien introduce un `sorted()`, se entera aquí.
- `test_metas_de_unidad_conserva_el_orden` — `c.metas_de("I")` devuelve los ids en el orden
  declarado, no reordenados.
- `test_el_encuadre_no_se_deduce_del_id` — una meta de `tipo: "encuadre"` con id `"1.0"` carga y
  `m.tipo == "encuadre"`, mientras que una meta con id `"0"` y `tipo: "aprendizaje"` carga como
  aprendizaje. Docstring: el encuadre se distingue por su `tipo`, nunca por su identificador.

**4. `class LosCursosExistentesNoCambian(unittest.TestCase)`** — REQ-48 por construcción:
- `test_los_tres_cursos_cargan_sin_declarar_nada_nuevo` — recorre las tres rutas reales:
  ```python
  RUTAS = [
      RAIZ / "cursos/2026-2/39056-big-data/curso.yaml",
      RAIZ / "cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml",
      RAIZ / "cursos/2026-2/38985-contabilidad-financiera/curso.yaml",
  ]
  ```
  Para cada una: `modelo.cargar(ruta)` no lanza, todos sus rubros tienen `unidad == ""` y
  `total is None`, y todas sus metas tienen `componentes == []`. Docstring: es la mitad de REQ-48
  resuelta por construcción — si algún campo nuevo dejara de tener default, esta prueba lo delata.
  Usa `subTest` por ruta para que el fallo diga cuál curso rompió.
  **No modifiques ninguno de esos tres archivos.**
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest pruebas.test_modelo -v; echo "salida esperada en esta tarea: fallos que nombran unidad/total/componentes"</automated>
  </verify>
  <acceptance_criteria>
    - `pruebas/test_modelo.py` existe y contiene las cuatro clases `RubroEnPuntos`,
      `ComponentesDeMeta`, `IdentificadoresLibres`, `LosCursosExistentesNoCambian`.
    - `python -X utf8 -m unittest pruebas.test_modelo -v` reporta al menos 18 pruebas.
    - En este punto la corrida **falla**, y los mensajes de fallo mencionan `unidad`, `total` o
      `componentes` — es decir, fallan por lo que falta implementar, no por un error de la prueba.
    - `python -X utf8 -m unittest pruebas.test_validar -v` sigue saliendo con código 0
      (importar `CURSO_VALIDO` desde ahí no rompió nada).
    - `git status --porcelain cursos/` sale vacío (ningún `curso.yaml` fue tocado).
  </acceptance_criteria>
  <done>El archivo de pruebas existe, describe los tres puntos del contrato nuevo y falla señalando exactamente lo que falta.</done>
</task>

<task type="auto">
  <name>Tarea 2: Rubro en puntos y la conversión a porcentaje (REQ-38)</name>
  <files>src/modelo.py</files>
  <read_first>
    - `src/modelo.py` líneas 27-35 (constantes y `ErrorModelo`), 98-165 (`Sesion`, `Meta`, `Rubro`)
      y 260-286 (`desde_dict`).
    - `pruebas/test_modelo.py` clase `RubroEnPuntos` — la API exacta que hay que satisfacer.
    - `src/validar.py` líneas 124-207 — R1 y R2. **No se tocan en esta fase** (son de la Fase 10);
      se leen para confirmar que siguen sumando `r.porcentaje` y `m.valor` sin enterarse de nada.
  </read_first>
  <action>
Extiende `src/modelo.py`. No reemplaces nada de lo que ya está.

**1. Vocabulario cerrado nuevo**, junto a `MODALIDADES`/`TIPOS_META`/`AMBIENTES` (línea 30-32):

```python
UNIDADES_RUBRO = ("puntos",)  # ausente = porcentaje
```

**2. `Rubro` gana dos campos con default y su validación.** El `porcentaje` sigue existiendo
siempre y sigue siendo lo que cuenta contra el 100 del esquema — no se toca ni se renombra:

```python
@dataclass
class Rubro:
    id: str
    etiqueta: str
    porcentaje: float
    detalle: str = ""
    parciales: int = 0
    unidad: str = ""            # ausente = porcentaje; "puntos" = los valores de sus metas son pts
    total: float | None = None  # los puntos que reparte el rubro. Obligatorio si unidad == "puntos"

    def __post_init__(self) -> None:
        if self.unidad and self.unidad not in UNIDADES_RUBRO:
            raise ErrorModelo(
                f"Rubro {self.id}: unidad inválida {self.unidad!r}. "
                f"Válidas: {', '.join(UNIDADES_RUBRO)} (o ausente, para porcentaje)."
            )
        if self.unidad == "puntos":
            if self.total is None:
                raise ErrorModelo(
                    f"Rubro {self.id}: declara unidad «puntos» sin `total`. "
                    f"Un rubro en puntos debe declarar su total (p. ej. `total: 150`). "
                    f"No se infiere de la suma de sus metas: esa diferencia es justo lo que "
                    f"las reglas tienen que poder ver."
                )
            if self.total <= 0:
                raise ErrorModelo(
                    f"Rubro {self.id}: `total` debe ser mayor que 0, no {self.total:g}."
                )

    @property
    def base(self) -> float:
        """El total contra el que se leen los valores de sus metas."""
        return float(self.total) if self.unidad == "puntos" else float(self.porcentaje)

    def a_porcentaje(self, valor: float) -> float:
        """Convierte un valor escrito en la unidad de este rubro a % de la calificación final.

        Para un rubro en porcentaje es la identidad, así que quien compara nunca tiene que
        preguntar en qué unidad está: llama siempre a esto. No redondea — el redondeo es
        decisión de quien compara.
        """
        return 0.0 if not self.base else valor / self.base * self.porcentaje
```

**3. Actualiza el comentario de `Meta.valor`** (`modelo.py:123`), que hoy dice
`# porcentaje de la calificación final` y deja de ser literalmente cierto:

```python
    valor: float = 0.0  # en la unidad de su rubro: porcentaje, o puntos si el rubro los declara
```

**No** añadas `unidad:` a `Meta`: la unidad se declara en un solo sitio —el rubro— para que meta y
rubro no puedan contradecirse.

**No** toques `desde_dict`: `Rubro(**r)` ya pasa las claves nuevas, y las dos claves llevan default
para que los tres `curso.yaml` existentes sigan cargando sin tocarse.

**No** toques `src/validar.py` en esta tarea: que R1 y R2 sigan sumando en porcentaje es correcto
hasta la Fase 10.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest pruebas.test_modelo.RubroEnPuntos pruebas.test_modelo.LosCursosExistentesNoCambian -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/modelo.py` contiene `UNIDADES_RUBRO = ("puntos",)`, `def a_porcentaje` y
      `unidad: str = ""` dentro de `class Rubro`.
    - `src/modelo.py` **no** contiene `unidad` como campo nuevo de `class Meta`
      (la meta hereda la unidad de su rubro; `Meta.unidad` sigue siendo la unidad temática del PUA).
    - `python -X utf8 -m unittest pruebas.test_modelo.RubroEnPuntos -v` sale con código 0.
    - `python -X utf8 -m unittest pruebas.test_modelo.LosCursosExistentesNoCambian -v` sale con código 0.
    - `python -X utf8 -m unittest pruebas.test_validar -v` sale con código 0: las 8 reglas siguen
      intactas y `test_detecta_el_defecto_del_ejemplo_961` no se tocó.
    - `git diff --stat src/validar.py` no reporta cambios.
  </acceptance_criteria>
  <done>Un rubro en puntos con su total carga junto a rubros en porcentaje, la unidad inválida y el total faltante son ErrorModelo, y la conversión vive en un solo sitio.</done>
</task>

<task type="auto">
  <name>Tarea 3: Componentes de meta e identificadores libres (REQ-39, REQ-42)</name>
  <files>src/modelo.py</files>
  <read_first>
    - `src/modelo.py` líneas 88-150 (`Evidencia`, `Sesion`, `Meta`) y 238-258 (`_construir_meta`,
      `_construir_grupo`) — el estado tras la tarea 2.
    - `pruebas/test_modelo.py` clases `ComponentesDeMeta` e `IdentificadoresLibres` — la API exacta.
    - `src/grafo.py` líneas 290-325 — los nodos de meta se construyen con `m.id`. Se lee para
      **confirmar que no hay que tocarlo**: los componentes no entran al grafo en esta fase, y eso
      se cumple por omisión con no añadir ningún bucle sobre `m.componentes` ahí.
  </read_first>
  <action>
Sigue extendiendo `src/modelo.py`.

**1. Vocabulario cerrado del componente**, junto a `UNIDADES_RUBRO`:

```python
# Vocabulario propio, distinto de TIPOS_META: un componente no es una meta.
TIPOS_COMPONENTE = ("examen_parcial", "examen_ordinario", "actividad", "proyecto")
```

**2. Dataclass `Componente`**, colocado justo después de `Evidencia` (para que `Evidencia` ya esté
definida) y antes de `Meta`:

```python
@dataclass
class Componente:
    """Un aporte adicional de una meta a otro rubro.

    No es una meta aparte: la meta sigue teniendo una sola semana y un solo enunciado. Es
    lo que hace la meta 2.4 del 531 —10 pts de actividades y, en la misma sesión, el
    Examen I que vale 15 % de exámenes—.

    `valor` se lee en la unidad del rubro al que se imputa, que puede no ser el de su meta.
    """

    rubro: str
    valor: float
    etiqueta: str
    tipo: str                       # sin valor por omisión: quien declara un componente dice de qué tipo es
    evidencia: Evidencia | None = None

    def __post_init__(self) -> None:
        if self.tipo not in TIPOS_COMPONENTE:
            raise ErrorModelo(
                f"Componente «{self.etiqueta}»: tipo inválido {self.tipo!r}. "
                f"Válidos: {', '.join(TIPOS_COMPONENTE)}."
            )
```

**3. `Meta` gana la lista**, justo después de `evidencias` (`modelo.py:128`):

```python
    componentes: list[Componente] = field(default_factory=list)
```

**4. Constructor del componente y enganche en `_construir_meta`.** Replica la rama de forma corta
que `_construir_meta` ya usa para las evidencias (línea 244), para que
`evidencia: "Examen I resuelto"` funcione igual que el mapa completo:

```python
def _construir_componente(c: dict) -> Componente:
    c = dict(c)
    if "tipo" not in c:
        raise ErrorModelo(
            f"Componente «{c.get('etiqueta', '?')}»: falta `tipo`. "
            f"Válidos: {', '.join(TIPOS_COMPONENTE)}. No hay valor por omisión a propósito: "
            f"un tipo supuesto convertiría un examen mal escrito en un componente que nadie cuenta."
        )
    ev = c.pop("evidencia", None)
    evidencia = (
        Evidencia(**ev) if isinstance(ev, dict)
        else Evidencia(nombre=ev) if ev
        else None
    )
    return Componente(evidencia=evidencia, **c)


def _construir_meta(d: dict) -> Meta:
    sesiones = [Sesion(**s) for s in d.pop("sesiones", [])]
    evidencias = [
        Evidencia(**e) if isinstance(e, dict) else Evidencia(nombre=e)
        for e in d.pop("evidencias", [])
    ]
    componentes = [_construir_componente(c) for c in d.pop("componentes", [])]
    semanas = d.pop("semanas", None)
    if semanas is None and (s := d.pop("semana", None)) is not None:
        semanas = [s]
    return Meta(
        semanas=semanas or [],
        sesiones=sesiones,
        evidencias=evidencias,
        componentes=componentes,
        **d,
    )
```

**Sobre REQ-42 no hay nada que implementar.** La auditoría de `src/` está hecha y confirmada: no
hay `sorted()` ni `.sort()` sobre `curso.metas` en ningún módulo, `metas_de()` es una comprensión
que preserva el orden, y la única distinción del encuadre (`validar.py:394`) usa `m.tipo`, no
`m.id`. Las pruebas de la clase `IdentificadoresLibres` fijan ese comportamiento como regresión.
**Lo que sí es obligatorio en esta tarea: no introducir la suposición.** Ninguna línea nueva puede
ordenar metas, comparar `m.id` contra `"0"`, partirlo con `.split(".")` ni deducir nada de su forma.

Tampoco toques `src/grafo.py`: los componentes no entran al grafo en esta fase, y eso se consigue
no añadiendo nada ahí.
  </action>
  <verify>
    <automated>cd "E:/GeneradorDiseñoInstruccional" && python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
  <acceptance_criteria>
    - `src/modelo.py` contiene `TIPOS_COMPONENTE = (`, `class Componente:`,
      `componentes: list[Componente] = field(default_factory=list)` y `def _construir_componente`.
    - `src/modelo.py` contiene `d.pop("componentes", [])`.
    - `src/modelo.py` **no** contiene ninguna de estas cadenas: `sorted(`, `.sort(`,
      `id.split(`, `id ==  "0"`, `startswith(` aplicadas a un id de meta.
    - `python -X utf8 -m unittest pruebas.test_modelo -v` sale con código 0 y las 18+ pruebas pasan.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y reporta al menos
      197 pruebas (179 anteriores + `test_huella` + `test_modelo`), sin fallos ni errores.
    - `git diff --stat src/grafo.py src/validar.py src/render_docx.py` no reporta cambios.
    - `git status --porcelain cursos/` sale vacío.
  </acceptance_criteria>
  <done>Una meta declara componentes con su rubro, valor, etiqueta, tipo y evidencia; sigue teniendo una sola semana; y los ids libres cargan en el orden declarado.</done>
</task>

</tasks>

<verification>
- `python -X utf8 -m unittest discover -s pruebas` en verde, sin que ninguna prueba anterior se
  haya modificado (`git diff pruebas/test_validar.py` solo puede mostrar cambios hechos por el
  plan 09-03, no por este).
- Los tres `curso.yaml` existentes cargan sin tocarse: `git status --porcelain cursos/` vacío.
- `python -X utf8 src/plantillas.py verificar` sale 0.
</verification>

<success_criteria>
Criterios 1, 2 y 3 del ROADMAP §Fase 9 quedan satisfechos a nivel de contrato: el rubro en puntos
carga junto al rubro en porcentaje, la meta con componente sigue siendo una meta con una semana, y
los identificadores libres conservan su orden. La conversión de puntos a porcentaje existe en un
solo sitio, lista para que las Fases 10 y 13 la consuman.
</success_criteria>

<output>
Al terminar, crea `.planning/phases/09-valor-de-una-meta/09-02-SUMMARY.md`.
</output>
