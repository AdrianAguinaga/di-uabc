---
phase: 10-reglas-en-la-unidad-declarada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modelo.py
  - pruebas/test_modelo.py
autonomous: true
requirements: [REQ-40, REQ-45]

must_haves:
  truths:
    - "Una sola función del modelo enumera todo lo que aporta valor a un rubro: la meta y sus componentes."
    - "Cada aporte sale en la unidad cruda del rubro al que se imputa, sin convertir."
    - "Desde un aporte se puede llegar a la meta que lo declaró (lo necesita la Fase 13)."
    - "Un curso sin `componentes:` produce exactamente un aporte por meta, en el orden declarado."
  artifacts:
    - path: "src/modelo.py"
      provides: "dataclass `Aporte` y accesor `Curso.aportes()`"
      contains: "def aportes(self)"
    - path: "pruebas/test_modelo.py"
      provides: "pruebas del accesor plano"
      contains: "class Aportes"
  key_links:
    - from: "src/modelo.py::Curso.aportes"
      to: "Meta.componentes"
      via: "bucle anidado que emite meta y luego sus componentes"
      pattern: "for c in m\\.componentes"
---

<objective>
Construir el accesor único de aportes en el modelo: el dataclass `Aporte` y el generador
plano `Curso.aportes()`, que emite lo que la meta aporta a su rubro y lo que aporta cada
uno de sus componentes.

Propósito: hoy `componentes:` existe en el contrato desde la Fase 9 pero **ninguna regla lo
lee** — fuera del propio dataclass solo aparece en `render_docx.py:274`. R2 deriva su suma en
línea (`validar.py:197`) mirando solo `m.valor`, así que un componente imputado a otro rubro no
cuenta nunca. Este plan escribe **una sola vez** la definición de «lo que cuenta para un rubro»;
los dos planes siguientes la consumen (R2 filtra por `rubro`, R3 por `tipo`) y la Fase 13 la
consumirá por `meta`.

Salida: `Aporte` y `Curso.aportes()` en `src/modelo.py`, con sus pruebas en
`pruebas/test_modelo.py`. **Ninguna regla cambia en este plan** — la suite queda igual de verde
que antes, con pruebas de más.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reglas-en-la-unidad-declarada/10-CONTEXT.md

Decisiones que gobiernan este plan: **D-01** (la agregación vive una sola vez, en el modelo,
como generador plano), **D-02** (valor en la unidad cruda; convertir es cosa de
`Rubro.a_porcentaje()`), **D-03** (R3 consume el mismo accesor filtrando por `tipo`).

<interfaces>
Contratos que ya existen en `src/modelo.py` y que este plan usa tal cual — no se reimplementan:

```python
TIPOS_META = ("encuadre", "aprendizaje", "examen_parcial", "cierre")          # :31
TIPOS_COMPONENTE = ("examen_parcial", "examen_ordinario", "actividad", "proyecto")  # :35

@dataclass
class Componente:            # :101-123
    rubro: str
    valor: float
    etiqueta: str
    tipo: str
    evidencia: Evidencia | None = None

@dataclass
class Meta:                  # :144-175
    id: str
    rubro: str = "tareas"
    valor: float = 0.0
    tipo: str = "aprendizaje"
    componentes: list[Componente] = field(default_factory=list)   # :157
    @property
    def etiqueta(self) -> str: return f"Meta {self.id}"           # :173-175

@dataclass
class Curso:                 # :249-299
    metas: list[Meta]
    rubros: list[Rubro]
    def unidad(self, numero): ...        # :289
    def rubro(self, id_): ...            # :292
    def metas_de(self, unidad): ...      # :295
    def nombre_archivo(self, grupo, ext): ...  # :298
```

`src/modelo.py:19` ya trae `from __future__ import annotations`.
`src/modelo.py:23` importa `from typing import Any`; **no** hay `Iterator` importado todavía.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: dataclass `Aporte` y accesor `Curso.aportes()`</name>

  <files>src/modelo.py</files>

  <read_first>
    - `src/modelo.py` completo, con atención a estas líneas antes de escribir nada:
      - `:19-23` — el bloque de imports (`from __future__ import annotations`, `from typing import Any`).
      - `:101-123` — `Componente`: sus campos son `rubro`, `valor`, `etiqueta`, `tipo`, `evidencia`.
      - `:144-175` — `Meta`: `rubro`, `valor`, `tipo`, `componentes` y la propiedad `etiqueta`.
      - `:241-247` — `Grupo`, el dataclass que precede a `Curso`. Ahí va `Aporte`.
      - `:285-299` — los accesores de `Curso` (`presencial`, `unidad`, `rubro`, `metas_de`,
        `nombre_archivo`). `aportes()` se escribe con ese mismo estilo.
    - `.planning/phases/10-reglas-en-la-unidad-declarada/10-CONTEXT.md` §D-01 y §D-02.
  </read_first>

  <action>
Dos añadidos a `src/modelo.py`. **Se extiende, nunca se reemplaza**: ninguna línea existente
cambia salvo el import de la línea 23.

**1. Import.** En el bloque de imports, junto a `from dataclasses import dataclass, field`
(línea 20), añade:

```python
from collections.abc import Iterator
```

Colócalo antes de `from dataclasses import ...` para respetar el orden alfabético de la
biblioteca estándar que el archivo ya sigue (`collections.abc` < `dataclasses` < `datetime` <
`pathlib` < `typing`).

**2. El dataclass `Aporte`.** Va **después** del dataclass `Grupo` (que termina en la línea 247)
y **antes** de `@dataclass class Curso` (línea 249), separado por dos líneas en blanco. Tiene que
ir después de `Meta` porque la referencia como tipo. Texto literal:

```python
@dataclass(frozen=True)
class Aporte:
    """Lo que una meta aporta a un rubro: ella misma, o uno de sus componentes.

    `valor` va en la unidad **cruda** del rubro al que se imputa: `10` son 10 pts o 10 %
    según lo que declare ese rubro, y no según el rubro de la meta. Convertir es cosa de
    `Rubro.a_porcentaje()`, y quien compara decide cuándo. Llevar aquí los dos valores
    sería un derivado que puede desincronizarse.

    `meta` es la meta entera, no su id: la Fase 13 tiene que poder llegar desde un aporte
    a la semana, las sesiones y las evidencias de quien lo declaró.
    """

    meta: Meta
    rubro: str
    valor: float
    etiqueta: str
    tipo: str
    es_componente: bool
```

Nota que hay que respetar y **no** resolver: `Aporte` es `frozen=True` pero **no es hashable en
la práctica**, porque `Meta` es un dataclass mutable y por tanto sin `__hash__`. No metas un
`Aporte` en un `set` ni lo uses como clave de diccionario. Filtrar y sumar es todo lo que hace
falta.

**3. El accesor.** Dentro de `Curso`, entre `metas_de()` (termina en la línea 296) y
`nombre_archivo()` (línea 298). Texto literal:

```python
    def aportes(self) -> Iterator[Aporte]:
        """Todo lo que aporta valor a un rubro, en orden: cada meta y luego sus componentes.

        Es la única definición de «lo que cuenta para un rubro» del proyecto. R2 filtra por
        `rubro`, R3 por `tipo` y la Fase 13 por `meta`; ninguna de las tres deriva la suya.
        Se devuelve plano —no agrupado por rubro— porque agrupar solo le sirve a R2.
        """
        for m in self.metas:
            yield Aporte(m, m.rubro, m.valor, m.etiqueta, m.tipo, False)
            for c in m.componentes:
                yield Aporte(m, c.rubro, c.valor, c.etiqueta, c.tipo, True)
```

**No toques nada más.** En particular: no cambies `Meta`, `Componente`, `Rubro` ni
`_construir_meta`; no añadas ningún campo al contrato; no toques `src/validar.py` (es el plan
10-02) ni `src/render_docx.py`.
  </action>

  <acceptance_criteria>
    - `src/modelo.py` contiene la línea exacta `from collections.abc import Iterator`.
    - `src/modelo.py` contiene `class Aporte:` precedido de `@dataclass(frozen=True)`.
    - `src/modelo.py` contiene `    def aportes(self) -> Iterator[Aporte]:` y la línea
      `                yield Aporte(m, c.rubro, c.valor, c.etiqueta, c.tipo, True)`.
    - `Aporte` declara exactamente seis campos, en este orden: `meta`, `rubro`, `valor`,
      `etiqueta`, `tipo`, `es_componente`.
    - `git diff --stat src/modelo.py` reporta **solo inserciones**, cero borrados salvo el
      reordenamiento del bloque de imports.
    - `git diff src/validar.py src/render_docx.py` sale vacío.
    - `python -X utf8 -c "import sys; sys.path.insert(0,'src'); import modelo; c=modelo.cargar('cursos/2026-2/39056-big-data/curso.yaml'); a=list(c.aportes()); print(len(a), len(c.metas))"`
      imprime dos números **iguales** (39056 no declara `componentes:`, así que hay un aporte
      por meta).
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
`Aporte` y `Curso.aportes()` existen en `src/modelo.py`; la suite completa sigue en verde y
`src/validar.py` no se tocó.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: pruebas del accesor plano</name>

  <files>pruebas/test_modelo.py</files>

  <read_first>
    - `pruebas/test_modelo.py` completo. Fíjate en:
      - `:1-36` — el encabezado, los imports y el helper `curso_con(rubros, metas=None)`.
      - `:105-121` — `ComponentesDeMeta.COMPONENTE` y `_cursos_con_y_sin_componente()`: el
        componente de ejemplo ya escrito, que estas pruebas reutilizan.
      - `:191-216` — `test_el_valor_del_componente_se_lee_en_la_unidad_de_su_propio_rubro`:
        el curso de dos rubros (uno en puntos, otro en porcentaje) que este plan replica.
      - `:265-280` — `LosCursosExistentesNoCambian`, para el estilo de las pruebas que leen
        los `curso.yaml` reales.
    - `pruebas/test_validar.py:26-50` — el helper `_meta(id_, unidad, semanas, valor, rubro, tipo)`
      que `test_modelo.py` ya importa.
    - `src/modelo.py` — el `Aporte` y el `aportes()` recién escritos.
  </read_first>

  <action>
Añade al final de `pruebas/test_modelo.py`, **antes** de la clase `LosCursosExistentesNoCambian`
(línea 265) para que esa quede de cierre, una clase nueva:

```python
class Aportes(unittest.TestCase):
    """El accesor plano que R2, R3 y la Fase 13 consumen (Fase 10, D-01 a D-03)."""

    def test_un_curso_sin_componentes_da_un_aporte_por_meta(self):
        """La mitad de la no contaminación: sin `componentes:` nada cambia de forma."""
        c = modelo.desde_dict(copy.deepcopy(CURSO_VALIDO))
        aportes = list(c.aportes())
        self.assertEqual(len(c.metas), len(aportes))
        self.assertEqual([m.id for m in c.metas], [a.meta.id for a in aportes])
        self.assertFalse(any(a.es_componente for a in aportes))

    def test_el_aporte_de_una_meta_copia_su_rubro_su_valor_y_su_tipo(self):
        c = modelo.desde_dict(copy.deepcopy(CURSO_VALIDO))
        a = next(a for a in c.aportes() if a.meta.id == "P1")
        self.assertEqual("examenes", a.rubro)
        self.assertEqual(10, a.valor)
        self.assertEqual("examen_parcial", a.tipo)
        self.assertEqual("Meta P1", a.etiqueta)
        self.assertFalse(a.es_componente)

    def test_el_componente_emite_su_propio_aporte_con_su_rubro(self):
        """El caso de la meta 2.4 del 531: 10 pts en actividades y el Examen I en exámenes."""
        d = copy.deepcopy(CURSO_VALIDO)
        for m in d["metas"]:
            if m["id"] == "2.1":
                m["componentes"] = [{
                    "rubro": "examenes", "valor": 15, "etiqueta": "Examen I",
                    "tipo": "examen_parcial",
                }]
        c = modelo.desde_dict(d)
        aportes = list(c.aportes())
        self.assertEqual(len(c.metas) + 1, len(aportes))
        comp = next(a for a in aportes if a.es_componente)
        self.assertEqual("examenes", comp.rubro)
        self.assertEqual(15, comp.valor)
        self.assertEqual("Examen I", comp.etiqueta)
        self.assertEqual("examen_parcial", comp.tipo)

    def test_el_componente_va_justo_detras_del_aporte_de_su_meta(self):
        """El orden importa: los mensajes de R2 enumeran aportes y deben leerse en orden."""
        d = copy.deepcopy(CURSO_VALIDO)
        for m in d["metas"]:
            if m["id"] == "2.1":
                m["componentes"] = [{
                    "rubro": "examenes", "valor": 15, "etiqueta": "Examen I",
                    "tipo": "examen_parcial",
                }]
        etiquetas = [a.etiqueta for a in modelo.desde_dict(d).aportes()]
        self.assertEqual(etiquetas.index("Meta 2.1") + 1, etiquetas.index("Examen I"))

    def test_desde_el_aporte_se_llega_a_la_meta_que_lo_declaro(self):
        """Condición de la Fase 13: el documento imprime cada aporte en la fila de su meta."""
        d = copy.deepcopy(CURSO_VALIDO)
        for m in d["metas"]:
            if m["id"] == "2.1":
                m["componentes"] = [{
                    "rubro": "examenes", "valor": 15, "etiqueta": "Examen I",
                    "tipo": "examen_parcial",
                }]
        c = modelo.desde_dict(d)
        comp = next(a for a in c.aportes() if a.es_componente)
        self.assertEqual("2.1", comp.meta.id)
        self.assertEqual([6, 7], comp.meta.semanas)

    def test_el_valor_del_aporte_sale_crudo_sin_convertir(self):
        """D-02: `10` en un rubro de 150 pts son 10 pts, no 2 %. Convertir es de quien compara."""
        rubros = [
            {"id": "actividades", "etiqueta": "Actividades", "porcentaje": 30,
             "unidad": "puntos", "total": 150},
            {"id": "examenes", "etiqueta": "Exámenes", "porcentaje": 50},
            {"id": "proyecto", "etiqueta": "Proyecto final", "porcentaje": 20},
        ]
        metas = [
            _meta("0", "I", [1], 0, "actividades", tipo="encuadre"),
            _meta("2.4", "II", [7], 10, "actividades"),
        ]
        c = curso_con(rubros, metas)
        a = next(a for a in c.aportes() if a.meta.id == "2.4")
        self.assertEqual(10, a.valor)
        self.assertAlmostEqual(2.0, c.rubro("actividades").a_porcentaje(a.valor))
```

Si `curso_con` no acepta metas con `componentes` tal cual, **no lo modifiques**: usa
`modelo.desde_dict` directamente como hacen las pruebas de arriba.

**No toques** ninguna prueba existente de `test_modelo.py` ni ningún archivo de `src/`.
  </action>

  <acceptance_criteria>
    - `pruebas/test_modelo.py` contiene `class Aportes(unittest.TestCase):` con **seis** métodos
      `def test_`.
    - `pruebas/test_modelo.py` contiene la cadena `a.meta.semanas` (prueba del enlace a la meta,
      condición de la Fase 13).
    - `python -X utf8 -m unittest discover -s pruebas -p "test_modelo.py"` sale con código 0 y
      reporta **6 pruebas más** que antes del plan (24 en total si antes eran 18).
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
    - `git diff --stat pruebas/test_modelo.py` reporta cero borrados.
    - `git diff src/` no muestra cambios respecto al final de la Tarea 1.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
Seis pruebas nuevas fijan el accesor: un aporte por meta sin componentes, el aporte de la meta,
el aporte del componente con su propio rubro, el orden, el enlace de vuelta a la meta y el valor
crudo. Suite completa en verde.
  </done>
</task>

</tasks>

<verification>
1. `python -X utf8 -m unittest discover -s pruebas` sale 0, con seis pruebas más que al empezar.
2. `git diff --name-only` lista exactamente `src/modelo.py` y `pruebas/test_modelo.py`.
3. `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sale 0 y su informe es
   idéntico al de antes del plan (ninguna regla se tocó).
4. `python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml`
   sale 0.
</verification>

<success_criteria>
- `Curso.aportes()` es la única definición de «lo que cuenta para un rubro» en el proyecto.
- Un aporte lleva la meta entera, así que la Fase 13 puede llegar desde él a su semana.
- El valor sale en la unidad cruda del rubro al que se imputa, sin conversión.
- Un curso sin `componentes:` produce exactamente los mismos aportes que metas, en orden.
- Ninguna regla de validación cambió de comportamiento en este plan.
</success_criteria>

<output>
Al terminar, escribe `.planning/phases/10-reglas-en-la-unidad-declarada/10-01-SUMMARY.md`.
</output>
