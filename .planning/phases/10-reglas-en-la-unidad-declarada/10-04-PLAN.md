---
phase: 10-reglas-en-la-unidad-declarada
plan: 04
type: execute
wave: 4
depends_on: ["10-02", "10-03"]
files_modified:
  - pruebas/test_validar.py
autonomous: true
requirements: [REQ-45, REQ-48]

must_haves:
  truths:
    - "R1 no lee ningún valor de meta ni ninguna clave de unidad: queda fijado con pruebas, no supuesto."
    - "Un curso que no declara ni `componentes:` ni `unidad:` sigue sin producir un solo hallazgo de R2 o R3."
    - "`python src/validar.py` sobre el curso en puntos reporta el error en puntos y sale con código 1."
    - "La huella de texto de 39056 y 39062 no cambió."
  artifacts:
    - path: "pruebas/test_validar.py"
      provides: "auditoría de R1 fijada, prueba de no contaminación y ejercicio del criterio 1 por la CLI"
      contains: "class Regla1EsInsensibleALaUnidad"
  key_links:
    - from: "pruebas/test_validar.py"
      to: "cursos/2026-2/39056-big-data/curso.yaml"
      via: "carga y validación del curso de control real, sin renderizar"
      pattern: "39056-big-data"
    - from: "pruebas/test_validar.py"
      to: "src/validar.py::main"
      via: "ejercicio de la CLI sobre un curso volcado a mkdtemp"
      pattern: "validar\\.main"
---

<objective>
Cerrar la fase: fijar con pruebas la afirmación de que **R1 es insensible a la unidad**, fijar
con una prueba rápida que los cursos que no declaran nada nuevo **siguen sin producir un solo
hallazgo de R2 o R3**, ejercer el criterio 1 del roadmap literalmente por la CLI, y comprobar a
mano que la huella de texto de los dos cursos de control no cambió.

Propósito: REQ-45 nombra a R1 además de a R2, pero la conclusión de que R1 no necesita cambio
alguno es **provisional** y se escribió sin auditar. Este plan la audita y la fija al estilo de
D-12/D-13 de la Fase 9 —afirmaciones sobre el código que se prueban en vez de implementarse—. Y
REQ-48, el criterio de cierre del milestone, se comprueba en dos niveles (D-14): una prueba
unitaria en el ciclo rápido, y `python src/huella.py verificar` a mano, que **no** entra en la
suite para no revertir D-18 de la Fase 9.

Salida: tres bloques de prueba en `pruebas/test_validar.py` y la corrida manual de la huella.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reglas-en-la-unidad-declarada/10-CONTEXT.md
@.planning/phases/10-reglas-en-la-unidad-declarada/10-02-SUMMARY.md
@.planning/phases/10-reglas-en-la-unidad-declarada/10-03-SUMMARY.md

Decisiones que gobiernan este plan: **D-12** (si el criterio 1 se ejerce por CLI, se usa el
patrón `mkdtemp` + `yaml.safe_dump` de `test_generar.py:38-39`), **D-13** (**38985 no se toca**:
es la Fase 14), **D-14** (el cierre de REQ-48 es `huella verificar` a mano **más** una prueba
unitaria), **D-15** (la línea base medida: los cuatro documentos de control validan limpios, con
cabecera, cinco recordatorios IEDI y `VÁLIDO`; **cero hallazgos de R2 y cero de R3**).

<interfaces>
```python
# src/validar.py — la CLI, ya existente
def main(argv: list[str]) -> int:
    """argv[1] es la ruta al curso.yaml. Imprime el informe y devuelve 0 si válido, 1 si no,
    2 si el curso ni siquiera carga."""

# src/validar.py:60-103 — el informe
class Informe:
    hallazgos: list[Hallazgo]      # Hallazgo(regla, nivel, mensaje)
    errores: list[Hallazgo]
    valido: bool
    def de(self, nivel) -> list[Hallazgo]: ...
    def texto(self) -> str: ...

# pruebas/test_validar.py — helpers ya disponibles al llegar aquí
CURSO_VALIDO, curso(), informe(), reglas_con_error()          # de siempre
CURSO_EN_PUNTOS, curso_en_puntos(), informe_en_puntos()       # plan 10-02
CURSO_CON_EXAMENES_EN_COMPONENTES, informe_con_examenes()     # plan 10-03
```

Los dos cursos de control, con su nombre de directorio exacto:
`cursos/2026-2/39056-big-data/curso.yaml` y
`cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml`.
El tercero, `cursos/2026-2/38985-contabilidad-financiera/`, **queda fuera de todo** (D-13).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: auditar R1 y fijar el resultado con pruebas</name>

  <files>pruebas/test_validar.py</files>

  <read_first>
    - `src/validar.py:126-169` — `regla_1` entera. **Léela línea a línea antes de escribir
      nada**: es la auditoría, no un trámite. Las cuatro comprobaciones son (a) la suma de
      `r.porcentaje` contra `suma_exacta`, (b) los ids de rubro duplicados vía `Counter`,
      (c) `self.c.exencion_ordinario` contra `[exencion_minima, exencion_maxima]`, y (d) el
      contraste de `{r.id: r.porcentaje}` contra el catálogo de `esquema_id`.
    - `src/modelo.py:187-228` — `Rubro`: `porcentaje` existe **siempre**, también en un rubro en
      puntos; `unidad`, `total`, `base` y `a_porcentaje` son lo que R1 no toca.
    - `pruebas/test_validar.py:138-166` — la clase `Regla1Porcentajes`, junto a la que va la
      clase nueva.
    - `pruebas/test_validar.py` — los helpers `informe_en_puntos()` y `_meta_de()` del plan 10-02.
  </read_first>

  <action>
**Primero audita, después escribe.** Recorre `src/validar.py:126-169` y confirma que ninguna de
sus cuatro comprobaciones lee `Meta.valor`, `Rubro.unidad`, `Rubro.total`, `Rubro.base` ni
`Rubro.a_porcentaje`. Si encuentras algo sensible a la unidad, **para y levántalo en el SUMMARY
como hallazgo**: no lo resuelvas por tu cuenta, no está decidido.

Confirmado eso, añade a `pruebas/test_validar.py`, inmediatamente después de la clase
`Regla1Porcentajes` (termina en la línea 166) y antes de `class Regla2Metas`:

```python
class Regla1EsInsensibleALaUnidad(unittest.TestCase):
    """REQ-45 nombra a R1 además de a R2, pero R1 no necesitó cambio alguno.

    Sus cuatro comprobaciones (`validar.py:126-169`) miran la suma de `r.porcentaje` contra
    100, los ids de rubro duplicados, el umbral de exención y el catálogo de esquemas. Ninguna
    toca un valor de meta ni ninguna clave de unidad, y `porcentaje` existe igual en un rubro
    en puntos. Se fija en vez de implementarse, como D-12 y D-13 de la Fase 9.
    """

    def test_el_curso_en_puntos_no_produce_ningun_hallazgo_de_r1(self):
        """El curso defectuoso de la Fase 10 —150 declarados, 140 sumados— es problema de R2."""
        inf = informe_en_puntos()
        self.assertEqual([], [h for h in inf.hallazgos if h.regla == "R1"])

    def test_r1_calla_aunque_los_valores_de_las_metas_esten_del_todo_mal(self):
        """Duplicar el valor de todas las metas rompe R2 y deja R1 en silencio."""
        metas = copy.deepcopy(CURSO_VALIDO["metas"])
        for m in metas:
            m["valor"] = m["valor"] * 2
        inf = informe(metas=metas)
        self.assertIn("R2", reglas_con_error(inf))
        self.assertNotIn("R1", reglas_con_error(inf))

    def test_poner_un_rubro_en_puntos_no_altera_lo_que_r1_comprueba(self):
        """`porcentaje` sigue siendo lo que cuenta contra el 100, también en puntos."""
        sin = [h.mensaje for h in informe().hallazgos if h.regla == "R1"]
        con = [h.mensaje for h in informe_en_puntos().hallazgos if h.regla == "R1"]
        self.assertEqual(sin, con)

    def test_el_codigo_de_r1_no_menciona_la_unidad_de_ningun_rubro(self):
        """La afirmación, escrita como prueba: si alguien mete aritmética de unidades en R1,
        esto se rompe y le obliga a decidirlo en vez de colarlo."""
        fuente = inspect.getsource(validar._Validador.regla_1)
        for termino in (".base", "a_porcentaje", ".unidad", ".total", ".valor"):
            self.assertNotIn(termino, fuente, f"R1 pasó a leer «{termino}»")
```

La última prueba necesita `import inspect` en el bloque de imports de la biblioteca estándar de
`pruebas/test_validar.py` (junto a `copy`, `sys`, `unittest`, línea 13-15). Añádelo en orden
alfabético: `copy`, `inspect`, `sys`, `unittest`.

Cuidado con un falso positivo al escribir esa última prueba: `regla_1` **sí** contiene la
palabra `total` como variable local (`total = round(sum(r.porcentaje …))`). Por eso los términos
llevan punto delante — `.total`, `.valor` — y no se busca `total` a secas. No cambies esa lista
sin volver a leer la fuente.

**No toques** `src/validar.py` en este plan. Si una de estas cuatro pruebas falla, es hallazgo
para el SUMMARY, no material para arreglar la regla.
  </action>

  <acceptance_criteria>
    - `pruebas/test_validar.py` contiene `import inspect` y
      `class Regla1EsInsensibleALaUnidad(unittest.TestCase):` con **cuatro** métodos `def test_`.
    - `pruebas/test_validar.py` contiene `inspect.getsource(validar._Validador.regla_1)`.
    - `python -X utf8 -m unittest discover -s pruebas -p "test_validar.py"` sale con código 0.
    - `git diff src/` sale vacío.
    - El SUMMARY del plan recoge el resultado de la auditoría en una frase explícita: «R1 es
      insensible a la unidad» o el hallazgo concreto en contra.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas -p "test_validar.py"</automated>
  </verify>

  <done>
La parte de REQ-45 que toca a R1 queda cubierta por auditoría y fijada por cuatro pruebas, sin
haber cambiado una línea de `regla_1`.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: la no contaminación en el ciclo rápido y el criterio 1 por la CLI</name>

  <files>pruebas/test_validar.py</files>

  <read_first>
    - `pruebas/test_validar.py:1-24` — el encabezado, `RAIZ` y los imports.
    - `pruebas/test_modelo.py:265-280` — `LosCursosExistentesNoCambian`: el estilo de las pruebas
      que leen los `curso.yaml` reales del repositorio.
    - `pruebas/test_generar.py:33-40` — `copia_del_curso`: el patrón `mkdtemp` + `yaml.safe_dump`
      que D-12 manda reutilizar.
    - `src/validar.py` — el final del archivo: la función `main(argv)`, sus tres códigos de
      salida y el hecho de que imprime con `print`.
    - `.planning/phases/10-reglas-en-la-unidad-declarada/10-CONTEXT.md` §D-15 — la línea base
      medida: cabecera, cinco recordatorios IEDI y `VÁLIDO`; cero hallazgos de R2 y de R3.
  </read_first>

  <action>
Añade al final de `pruebas/test_validar.py` una clase nueva. Necesita tres imports más en el
bloque de la biblioteca estándar: `contextlib`, `io`, `tempfile`, y uno de tercero: `yaml`
(va con los imports de tercero, si el archivo no lo tiene ya; si no hay bloque de tercero,
colócalo detrás de la biblioteca estándar y antes de `import modelo`).

```python
CURSOS_DE_CONTROL = (
    RAIZ / "cursos" / "2026-2" / "39056-big-data" / "curso.yaml",
    RAIZ / "cursos" / "2026-2" / "39062-patrones-de-comportamiento" / "curso.yaml",
)


class NoContaminacion(unittest.TestCase):
    """REQ-48 en el ciclo rápido. La otra mitad —la huella del `.docx`— se comprueba a mano
    con `python src/huella.py verificar`, que no entra aquí para no atar la suite a las
    plantillas (D-18 de la Fase 9, conservada por D-14 de la Fase 10).

    38985 queda fuera a propósito: declara sus valores en porcentaje y su reescritura es la
    Fase 14 (D-13).
    """

    def test_los_cursos_de_control_no_declaran_nada_de_la_v2(self):
        """Si alguno empezara a declarar `componentes:` o `unidad:`, las dos pruebas de abajo
        dejarían de significar lo que dicen."""
        for ruta in CURSOS_DE_CONTROL:
            texto = ruta.read_text(encoding="utf-8")
            with self.subTest(curso=ruta.parent.name):
                self.assertNotIn("\n      componentes:", texto)
                self.assertNotIn("unidad: puntos", texto)

    def test_los_cursos_de_control_no_emiten_un_solo_hallazgo_de_r2_ni_de_r3(self):
        """La línea base medida antes de planear la fase (D-15): cero hallazgos de esas dos
        reglas, de cualquier nivel. Lo que REQ-48 exige preservar es ese silencio."""
        for ruta in CURSOS_DE_CONTROL:
            inf = validar.validar(modelo.cargar(ruta))
            with self.subTest(curso=ruta.parent.name):
                self.assertEqual(
                    [],
                    [str(h) for h in inf.hallazgos if h.regla in ("R2", "R3")],
                )

    def test_los_cursos_de_control_siguen_siendo_validos(self):
        for ruta in CURSOS_DE_CONTROL:
            inf = validar.validar(modelo.cargar(ruta))
            with self.subTest(curso=ruta.parent.name):
                self.assertTrue(inf.valido, "\n".join(str(h) for h in inf.errores))


class CriterioUnoPorLaLinea(unittest.TestCase):
    """Criterio 1 del roadmap, ejercido tal como está escrito: `python src/validar.py` sobre un
    curso cuyo rubro en puntos declara 150 y cuyas metas suman 140.

    El curso se vuelca a un directorio temporal con el patrón de `test_generar.py:38-39`: en
    `cursos/` solo viven materias reales, y el grafo recogería como materia cualquier cosa que
    se dejara ahí (D-12).
    """

    def test_la_cli_reporta_el_faltante_en_puntos_y_sale_con_uno(self):
        tmp = Path(tempfile.mkdtemp(prefix="di-r2-puntos-"))
        ruta = tmp / "curso.yaml"
        ruta.write_text(
            yaml.safe_dump(CURSO_EN_PUNTOS, allow_unicode=True, sort_keys=False),
            encoding="utf-8",
        )
        salida = io.StringIO()
        with contextlib.redirect_stdout(salida):
            codigo = validar.main(["validar.py", str(ruta)])
        texto = salida.getvalue()
        self.assertEqual(1, codigo)
        self.assertIn("R2", texto)
        self.assertIn("140 pts", texto)
        self.assertIn("150 pts", texto)
        self.assertNotIn("NO VÁLIDO — 0", texto)
```

Dos cosas a comprobar mientras escribes, no después:

- **Los nombres de directorio son literales y ya verificados**: `39056-big-data` y
  `39062-patrones-de-comportamiento`. Si `ruta.exists()` diera `False`, la prueba fallaría con un
  error críptico de carga; compruébalo con `ls cursos/2026-2/` antes de dar la tarea por buena.
- **`CURSO_EN_PUNTOS` es un diccionario Python de tipos simples** (str, int, float, list, dict),
  así que `yaml.safe_dump` lo serializa sin más. Si `safe_dump` protestara por algún tipo, el
  fixture del plan 10-02 se desvió de lo planeado: revísalo antes de tocar esta prueba.
- **No metas `python src/huella.py verificar` en la suite** (D-14). Es la Tarea 3, a mano.
  </action>

  <acceptance_criteria>
    - `pruebas/test_validar.py` contiene `CURSOS_DE_CONTROL = (`, `class NoContaminacion(` con
      tres métodos `def test_`, y `class CriterioUnoPorLaLinea(` con uno.
    - `pruebas/test_validar.py` contiene `39062-patrones-de-comportamiento` y **no** contiene
      `38985`.
    - `pruebas/test_validar.py` contiene `validar.main([` y `tempfile.mkdtemp(`.
    - `pruebas/test_validar.py` **no** contiene `huella`.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0.
    - `git status --porcelain cursos/` sale vacío: la prueba de la CLI escribe en `mkdtemp`, no
      en el repositorio.
    - `git diff src/` sale vacío.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
El silencio de R2 y R3 sobre los dos cursos de control está fijado en el ciclo rápido, y el
criterio 1 del roadmap se ejerce literalmente por la línea de órdenes sin ensuciar `cursos/`.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: cierre de REQ-48 — la huella, a mano</name>

  <files>(ninguno: esta tarea no modifica archivos)</files>

  <read_first>
    - `src/huella.py` — los subcomandos `verificar` y `registrar`, y qué compara cada uno (texto
      del `.docx`, informe de validación y forma del `MANIFIESTO.yaml`, D-27 de la Fase 9).
    - `pruebas/huellas.yaml` — la línea base de los cuatro documentos de control
      (39056/961, 39056/962, 39062/971, 39062/972).
    - `.planning/phases/09-valor-de-una-meta/09-VERIFICATION.md` — cómo se cerró REQ-48 en la
      fase anterior. Este cierre es el mismo, con una diferencia: **esta fase no gasta ninguna
      excepción**, porque ningún curso de control declara `componentes:` ni `unidad:`.
  </read_first>

  <action>
Corre, en este orden, y **anota la salida literal de cada uno en el SUMMARY**:

```
python -X utf8 -m unittest discover -s pruebas
python -X utf8 src/huella.py verificar
python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml
python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml
git status --porcelain
```

Lo que tiene que salir:

1. La suite completa en verde, con el número total de pruebas apuntado en el SUMMARY (la Fase 9
   la dejó en 216; esta fase suma las de los cuatro planes).
2. `huella verificar` **sin una sola diferencia** y con código de salida 0. Los tres hashes de
   los cuatro documentos —texto del `.docx`, informe y forma del `MANIFIESTO.yaml`— coinciden con
   `pruebas/huellas.yaml`.
3. Los dos informes de validación salen con código 0 y sin ninguna línea de `R2` ni de `R3`.
4. `git status --porcelain` **no lista `cursos/`**: `huella verificar` es de solo lectura y
   restaura los bytes que leyó (D-23/D-28 de la Fase 9).

**No corras `python src/huella.py registrar`.** Esta fase no cambia el documento: si la huella
difiere, algo se rompió y hay que averiguar qué, no aceptar el cambio. Registrar borraría la
evidencia.

**Si `huella verificar` reporta cualquier diferencia: para.** No arregles nada por tu cuenta.
Escribe en el SUMMARY qué documento difiere, en cuál de los tres hashes, y qué plan de la fase
lo introdujo; devuelve eso como bloqueo. REQ-48 es el criterio de aceptación del milestone
entero y una diferencia aquí es una decisión del usuario, no un defecto que se parchea.

Si `huella verificar` fallara por una causa de entorno —falta Word, faltan plantillas, falta un
paquete— déjalo dicho igual en el SUMMARY, distinguiendo con claridad «no se pudo comprobar» de
«se comprobó y difiere». Son cosas distintas y solo una de las dos es un bloqueo de la fase.
  </action>

  <acceptance_criteria>
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0; el SUMMARY apunta el
      número total de pruebas.
    - `python -X utf8 src/huella.py verificar` sale con código 0 y su salida **no** contiene las
      subcadenas `difiere` ni `cambió`.
    - `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sale 0; su salida
      no contiene `R2` ni `R3`. Ídem para `39062-patrones-de-comportamiento`.
    - `git status --porcelain` no lista ningún archivo bajo `cursos/`.
    - `git diff --name-only` para la fase entera lista exactamente cuatro archivos:
      `src/modelo.py`, `src/validar.py`, `pruebas/test_modelo.py`, `pruebas/test_validar.py`,
      más `AGENTS.md`.
    - El SUMMARY transcribe la salida literal de `huella verificar`, no una paráfrasis.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 src/huella.py verificar</automated>
  </verify>

  <done>
REQ-48 cerrado para la Fase 10: los cuatro documentos de control conservan su huella de texto,
sus informes de validación no cambiaron ni un hallazgo, y no se gastó ninguna excepción.
  </done>
</task>

</tasks>

<verification>
1. `python -X utf8 -m unittest discover -s pruebas` sale 0.
2. `python -X utf8 src/huella.py verificar` sale 0 y sin diferencias.
3. `grep -c "38985" pruebas/test_validar.py` devuelve 0 — D-13 respetado.
4. `git status --porcelain cursos/` sale vacío.
5. `git diff --name-only` no lista ningún archivo de `referencias/` ni de `ejemplos/`.
</verification>

<success_criteria>
- La parte de REQ-45 que toca a R1 está auditada y su conclusión fijada con cuatro pruebas, sin
  haber cambiado `regla_1`.
- El criterio 1 del roadmap está ejercido por la CLI, con el curso en un directorio temporal.
- El criterio 5 del roadmap (REQ-48) está comprobado: huella de 39056 y 39062 intacta e informes
  sin cambios.
- Ni `cursos/` ni `referencias/` ni `ejemplos/` cambiaron; 38985 no se tocó.
</success_criteria>

<output>
Al terminar, escribe `.planning/phases/10-reglas-en-la-unidad-declarada/10-04-SUMMARY.md`,
incluyendo el resultado explícito de la auditoría de R1 y la salida literal de
`huella verificar`.
</output>
