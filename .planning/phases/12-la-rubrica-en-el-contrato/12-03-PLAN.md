---
phase: 12
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/generar.py
  - pruebas/test_generar.py
  - .planning/phases/12-la-rubrica-en-el-contrato/12-VALIDATION.md
autonomous: true
requirements:
  - REQ-26
  - REQ-48
---

# Plan 12-03 — Trazabilidad y cierre de no contaminación

<goal>
Registrar una rúbrica declarada sin alterar la forma de manifiestos existentes y cerrar la fase
con sus verificaciones mecánicas.
</goal>

<tasks>
<task type="auto">
  <name>Tarea 1: registrar la rúbrica declarada en el manifiesto</name>
  <files>src/generar.py</files>
  <read_first>
    - src/generar.py: manifiesto y bloque evaluacion.
    - 12-CONTEXT.md, D-08 a D-10.
  </read_first>
  <action>
Dentro de `evaluacion` en `manifiesto()`, añade condicionalmente `rubrica` sólo si
`curso.rubrica is not None`. Copia `meta` o `rubro`, `total` y cada fila con
`concepto`, `puntos`, `descripcion`, en el orden declarado. No incluyas un bloque vacío,
defaults calculados ni textos producidos por código: para un curso existente, las claves y el
orden del manifiesto deben ser exactamente los mismos.
  </action>
  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
</task>

<task type="auto">
  <name>Tarea 2: probar manifiesto condicional y literalidad</name>
  <files>pruebas/test_generar.py</files>
  <read_first>
    - pruebas/test_generar.py: ManifiestoDelSegundoNivel.
    - src/generar.py tras la tarea 1.
    - pruebas/test_validar.py: CURSO_VALIDO.
  </read_first>
  <action>
Añade una clase de manifiesto de rúbrica que llame `generar.manifiesto()` directamente desde un
curso temporal. Prueba que, sin la clave, el orden de `evaluacion` no incorpora `rubrica`; con
una rúbrica, el manifiesto conserva exactamente destino, total, concepto, puntos y descripción.
No renderices documentos en esas pruebas.
  </action>
  <verify>
    <automated>python -X utf8 -m unittest pruebas.test_generar -v</automated>
  </verify>
</task>

<task type="auto">
  <name>Tarea 3: cerrar validación y escribir el resumen</name>
  <files>.planning/phases/12-la-rubrica-en-el-contrato/12-VALIDATION.md</files>
  <read_first>
    - 12-VALIDATION.md.
    - Salida de toda la suite y verificaciones manuales.
  </read_first>
  <action>
Marca la estrategia Nyquist como planeada/cumplida con los planes asignados. Ejecuta, en orden,
la suite completa, `python -X utf8 src/huella.py verificar`, `python src/plantillas.py verificar`
y `git diff --check`. Confirma que no hay cambios en `render_docx.py`, `grafo/`,
`referencias/`, `ejemplos/` ni `cursos/`. Escribe 12-03-SUMMARY.md con resultados y
pendientes explícitos para Fases 13 y 14.
  </action>
  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>
</task>
</tasks>

<verification>
- Manifiesto sin rúbrica conserva su forma.
- Manifiesto con rúbrica es una copia trazable de la declaración.
- Suite, huellas y custodia de plantillas pasan.
</verification>
