---
phase: 11-segundo-nivel-de-la-calificacion
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/generar.py
  - pruebas/test_generar.py
  - pruebas/test_validar.py
autonomous: true
requirements: [REQ-48, REQ-26]

must_haves:
  truths:
    - "El `MANIFIESTO.yaml` de un curso que declara el segundo nivel registra `segundo_nivel` y `exencion_contra` en su bloque `evaluacion:`."
    - "El `MANIFIESTO.yaml` de un curso que no los declara conserva su bloque `evaluacion:` con exactamente las tres claves de siempre, en el mismo orden: ni un byte de diferencia."
    - "39056 y 39062 no emiten un solo hallazgo de R1, de ningún nivel, y siguen siendo válidos."
    - "Ningún `curso.yaml` de control declara `segundo_nivel:` ni `exencion_contra:` en su texto crudo."
    - "`python src/huella.py verificar` pasa sin una sola diferencia en los cuatro documentos de control."
    - "38985 pasó de 9 a 10 hallazgos, el nuevo es un aviso de R1, y sigue siendo válido: la deuda que la Fase 14 cierra queda anotada, no arreglada."
  artifacts:
    - path: "src/generar.py"
      provides: "el bloque `evaluacion:` del manifiesto con las dos claves nuevas, incluidas solo si el curso las declara"
      contains: "if curso.segundo_nivel is not None"
    - path: "pruebas/test_generar.py"
      provides: "la forma del bloque `evaluacion:` del manifiesto en los dos sentidos"
      contains: "class ManifiestoDelSegundoNivel"
    - path: "pruebas/test_validar.py"
      provides: "el silencio de R1 sobre los cursos de control, fijado en el ciclo rápido"
      contains: "no_emiten_un_solo_hallazgo_de_r1"
  key_links:
    - from: "src/generar.py::manifiesto"
      to: "src/modelo.py::Curso.segundo_nivel"
      via: "inclusión condicional con **({...} if curso.segundo_nivel is not None else {})"
      pattern: "curso\\.segundo_nivel"
    - from: "pruebas/test_generar.py"
      to: "src/generar.py::manifiesto"
      via: "llamada directa a generar.manifiesto(), sin renderizar ningún documento"
      pattern: "generar\\.manifiesto\\("
    - from: "pruebas/test_validar.py::NoContaminacion"
      to: "cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml"
      via: "carga y validación de los cursos de control reales, sin renderizar"
      pattern: "39062-patrones-de-comportamiento"
---

<objective>
Cerrar la fase: registrar el segundo nivel en el `MANIFIESTO.yaml` **solo cuando el curso lo
declara**, fijar en el ciclo rápido que los cursos de control siguen sin recibir un solo hallazgo
de R1 y sin declarar ninguna de las dos claves nuevas, y comprobar a mano —fuera de la suite— que
la huella de los cuatro documentos de control no cambió.

Propósito: REQ-48, el criterio de aceptación del milestone entero, y la regla invariable 7 (todo
dato es trazable). Un DI de 38985 generado con dos niveles no debe dejar de rastro de que se
calificó a dos niveles; y a la vez, la forma del manifiesto de un curso que no los declara entra
en la huella (D-27 de la Fase 9), así que la inclusión tiene que ser **condicional** y no
«opcional con valor por omisión».

Las dos rutas de verificación de REQ-48 no se solapan y este plan corre **las dos, en este orden**:
la suite completa —que es la única que corre en cada commit y atrapa una regresión de R1— y
`python src/huella.py verificar` —que es la única que ve el `.docx` y la **forma** del manifiesto,
y por tanto la única que atraparía una regresión de D-16—.

Salida: seis líneas en `src/generar.py`, dos clases/pruebas nuevas, la ampliación de
`NoContaminacion`, y las corridas manuales de cierre transcritas en el SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-CONTEXT.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-RESEARCH.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-VALIDATION.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-01-SUMMARY.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-02-SUMMARY.md

Decisiones que gobiernan este plan:

- **D-16** — el `MANIFIESTO.yaml` registra `segundo_nivel` y `exencion_contra` en su bloque
  `evaluacion:` (`generar.py:177-181`) **solo cuando el curso los declara**. Los dos cursos de
  control no los declaran, así que su manifiesto no cambia ni un byte y D-27 de la Fase 9 —la
  forma del manifiesto entra en la huella— queda respetada.
- **D-17** — la clase `NoContaminacion` (`test_validar.py:691-724`) **se extiende, no se
  duplica**: `test_los_cursos_de_control_no_declaran_nada_de_la_v2` añade las dos claves nuevas a
  su lista de ausencias, y el silencio de **R1** se fija igual que ya se fija el de R2 y R3.
- **D-18** — 38985 **no se toca** y su aviso nuevo se acepta: 9 → 10 hallazgos, sigue válido. Su
  huella no se vigila (D-22 de la Fase 9).
- **D-20** — el cierre de REQ-48 es `python src/huella.py verificar` **a mano**, fuera del ciclo
  unitario, conservando D-18 de la Fase 9 y D-14 de la Fase 10.
- **D-21 (medido)** — `src/grafo.py` **no se toca y no se regenera**: no abre el bloque
  `evaluacion:` en ningún punto (solo `m.rubro` y `m.valor`, `:299`). La parte de REQ-48 que habla
  de la forma de `grafo/` se cumple por construcción, no por cuidado.

**Línea base medida, para que ninguna prueba se escriba a ojo** (remedida en `11-RESEARCH.md`
corriendo `validar.py` de verdad, y reconfirmada tras el plan 11-02):

| Curso | Hallazgos antes de la fase | De R1 antes | Después de la fase | De R1 después |
|---|---|---|---|---|
| 39056 Big Data | 5 | 0 | 5 | **0** |
| 39062 Patrones | 5 | 0 | 5 | **0** |
| 38985 Contabilidad | 9 | 0 | **10** | **1 aviso** (D-18) |

Los cinco hallazgos de 39056 y 39062 son los recordatorios IEDI 2.4/3.1/3.2/3.5/4.1. La suite
estaba en **245 pruebas** al empezar la fase (no las 179 del ROADMAP, que es la línea base de la
v1.0) y llega a este plan con **259**.

**Discrepancia detectada en el contrato de validación, y cómo se resuelve.** `11-VALIDATION.md`
enumera B13 como «39056 / 39062 / **38985** siguen emitiendo cero hallazgos de R1». Eso es
incompatible con D-18, que **mide y acepta** que 38985 gane un aviso de R1 en esta misma fase.
Resolución, que es también lo que dice la sección «Verificaciones solo manuales» del propio
`11-VALIDATION.md`: la prueba automatizada cubre **39056 y 39062** —los `CURSOS_DE_CONTROL` que
D-13 de la Fase 10 delimita— y el comportamiento de 38985 lo cubre la comprobación **manual** de
la Tarea 3, porque automatizar un 10 congelaría un número que la Fase 14 va a cambiar. Si escribes
la prueba con los tres cursos, **falla**.

<interfaces>
```python
# src/generar.py:136-181 — manifiesto() construye UN ÚNICO return {...} literal. No hay
# variable intermedia ni helper que lo componga por partes, así que la inclusión condicional
# de D-16 es una adición localizada, no un refactor. Firma:
def manifiesto(
    curso: Curso,
    ruta_curso: Path,
    informe: validar.Informe,
    archivos: list[Path],
    cfg: Config,
    cal: calendario.Calendario,
) -> dict: ...
#   `archivos=[]` es válido: la lista solo alimenta el bloque "archivos".
#   El bloque a modificar, hoy (:177-181):
#       "evaluacion": {
#           "esquema_id": curso.esquema_id,
#           "exencion_ordinario": curso.exencion_ordinario,
#           "rubros": [{"id": r.id, "porcentaje": r.porcentaje} for r in curso.rubros],
#       },

# src/huella.py:103-118 — forma_del_manifiesto() hashea el manifiesto completo salvo
# `generado`, `commit`, `sha256` y `bytes`. Con la inclusión condicional, un curso que no
# declara segundo nivel produce el mismo dict —sin las claves nuevas— y su hash de forma no
# cambia. Es la comprobación mecánica de que D-16 no rompe D-27.

# pruebas/test_generar.py:30-40 — el patrón que ya existe para no ensuciar cursos/
CURSO = RAIZ / "cursos" / "2026-2" / "39056-big-data" / "curso.yaml"
def copia_del_curso(destino: Path, retoque=None) -> Path: ...
#   Importa hoy: shutil, sys, tempfile, unittest, Path, yaml, calendario, generar, plantillas.
#   NO importa todavía `modelo` ni `validar`: la Tarea 1 los añade.

# pruebas/test_validar.py:685-724 — CURSOS_DE_CONTROL (39056 y 39062) y la clase
# NoContaminacion con sus tres pruebas actuales.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: el manifiesto registra el segundo nivel solo si el curso lo declara</name>

  <files>src/generar.py, pruebas/test_generar.py</files>

  <read_first>
    - `src/generar.py:136-209` — `manifiesto()` **entera**: el `return {...}` literal, el bloque
      `evaluacion:` (`:177-181`) y los bloques `grupos`, `validacion` y `archivos` que lo siguen,
      para no romper el orden de claves.
    - `pruebas/test_generar.py` **completo (202 líneas)**. Es lectura obligada de este plan:
      `11-VALIDATION.md` §Ola 0 dice explícitamente que hay que abrirlo **antes** de escribir esta
      tarea, porque la investigación no lo abrió. Fíjate en tres cosas: `copia_del_curso`
      (`:33-40`), que `PaqueteDeBigData` genera documentos de verdad en `setUpClass` (`:46-54`) y
      es **caro**, y que el archivo **no importa `modelo` ni `validar`** todavía.
    - `src/huella.py:103-118` — `forma_del_manifiesto()`: qué claves excluye del hash y por qué la
      inclusión condicional no cambia la huella de un curso que no declara nada.
    - `.planning/phases/11-segundo-nivel-de-la-calificacion/11-RESEARCH.md` §«El MANIFIESTO y
      D-16» — el bloque ya escrito, con su `**({...} if ... else {})`.
  </read_first>

  <behavior>
    Lo que las pruebas de esta misma tarea van a fijar:

    - Con el `curso.yaml` real de 39056 tal cual: `manifiesto(...)["evaluacion"]` tiene exactamente
      las claves `["esquema_id", "exencion_ordinario", "rubros"]`, **en ese orden**, y ninguna más.
    - Con ese mismo curso al que se le añade `segundo_nivel:` 60/40 y `exencion_contra: promedio`:
      el bloque `evaluacion:` gana `segundo_nivel` —con `porcentaje` y `etiqueta` de cada nivel— y
      `exencion_contra: "promedio"`.
    - Ninguna de las dos pruebas renderiza un `.docx`.
  </behavior>

  <action>
**1. `src/generar.py`.** Sustituye el bloque `evaluacion:` (`:177-181`) por:

```python
        "evaluacion": {
            "esquema_id": curso.esquema_id,
            "exencion_ordinario": curso.exencion_ordinario,
            "rubros": [{"id": r.id, "porcentaje": r.porcentaje} for r in curso.rubros],
            # Solo si el curso los declara (D-16): la forma del manifiesto entra en la huella
            # (D-27 de la Fase 9), así que un curso a un solo nivel tiene que producir
            # exactamente el mismo dict que antes de esta fase.
            **(
                {
                    "segundo_nivel": {
                        "promedio": {
                            "porcentaje": curso.segundo_nivel.promedio.porcentaje,
                            "etiqueta": curso.segundo_nivel.promedio.etiqueta,
                        },
                        "ordinario": {
                            "porcentaje": curso.segundo_nivel.ordinario.porcentaje,
                            "etiqueta": curso.segundo_nivel.ordinario.etiqueta,
                        },
                    },
                    "exencion_contra": curso.exencion_contra or "promedio",
                }
                if curso.segundo_nivel is not None
                else {}
            ),
        },
```

Tres cosas deliberadas:

- **A mano, no con `dataclasses.asdict()`.** Construir el dict explícitamente deja escrito qué dos
  claves entran, que es justo lo que D-16 pide; `asdict()` volcaría de más el día que `Nivel` gane
  un campo, y eso cambiaría la forma del manifiesto sin que nadie lo decidiera.
- **`curso.exencion_contra or "promedio"`.** El manifiesto registra el valor **efectivo**, no la
  cadena vacía. Dentro de este `if` el curso declara segundo nivel, así que por D-07 la clave
  existe y el `or` es un cinturón, no la lógica.
- **La inclusión va al final del dict.** Así las tres claves de siempre conservan su orden y su
  posición, y el hash de forma de un curso sin segundo nivel es byte por byte el mismo.

**No toques ningún otro punto de `src/generar.py`.** `manifiesto()` es la única función que arma
este bloque y ningún llamador la envuelve. Y **no toques `src/huella.py`**: esta fase solo lo
corre, no lo cambia.

**2. `pruebas/test_generar.py`.** Añade `import modelo` y `import validar` al bloque de imports de
`src` (junto a `calendario`, `generar`, `plantillas`, líneas 26-28, en orden alfabético:
`calendario`, `generar`, `modelo`, `plantillas`, `validar`), y una clase nueva **al final del
archivo, después de `class Panel` y antes del `if __name__ == "__main__":`**:

```python
class ManifiestoDelSegundoNivel(unittest.TestCase):
    """D-16: las dos claves nuevas entran en el manifiesto **solo** si el curso las declara.

    Se llama a `generar.manifiesto()` directamente y no a `generar.paquete()`: la forma del
    bloque `evaluacion:` no depende de renderizar nada, y renderizar dos documentos por prueba
    ataría esta comprobación a las plantillas y a Word (D-18 de la Fase 9). La otra mitad —que
    el manifiesto de los cursos de control no cambie de forma— la ve `huella verificar`, a mano.
    """

    def setUp(self):
        self.tmp = Path(tempfile.mkdtemp(prefix="di-manifiesto-nivel-"))
        self.addCleanup(shutil.rmtree, self.tmp, True)
        self.cfg = modelo.Config()
        self.cal = calendario.cargar("2026-2")

    def _evaluacion(self, retoque=None) -> dict:
        ruta = copia_del_curso(self.tmp, retoque)
        curso = modelo.cargar(ruta)
        inf = validar.validar(curso, self.cfg, self.cal)
        datos = generar.manifiesto(curso, ruta, inf, [], self.cfg, self.cal)
        return datos["evaluacion"]

    def test_un_curso_sin_segundo_nivel_no_registra_ninguna_clave_nueva(self):
        """La forma del manifiesto entra en la huella (D-27): esta es la mitad de REQ-48 que se
        puede comprobar sin generar documentos."""
        ev = self._evaluacion()
        self.assertEqual(["esquema_id", "exencion_ordinario", "rubros"], list(ev))

    def test_un_curso_con_segundo_nivel_lo_registra_con_sus_dos_etiquetas(self):
        """Sin esto, un DI de 38985 generado no dejaría rastro de que se calificó a dos
        niveles, y la regla invariable 7 dice que todo dato es trazable."""

        def declarar(d):
            d["evaluacion"]["segundo_nivel"] = {
                "promedio": {
                    "porcentaje": 60,
                    "etiqueta": "Valor del promedio antes del Examen Ordinario",
                },
                "ordinario": {"porcentaje": 40, "etiqueta": "Valor del examen Ordinario"},
            }
            d["evaluacion"]["exencion_contra"] = "promedio"

        ev = self._evaluacion(declarar)
        self.assertEqual(60, ev["segundo_nivel"]["promedio"]["porcentaje"])
        self.assertEqual(
            "Valor del examen Ordinario", ev["segundo_nivel"]["ordinario"]["etiqueta"]
        )
        self.assertEqual("promedio", ev["exencion_contra"])
```

Dos avisos:

- **La segunda prueba deja el informe con un aviso de R1** —39056 declara `esquema_id:
  estandar-2026`, que no declara segundo nivel, así que el contraste de D-14 diverge—. Da igual:
  la prueba no afirma nada sobre la validez, solo sobre la forma del manifiesto. **No le quites el
  `esquema_id` al fixture para «dejarlo limpio»**: la prueba dejaría de correr sobre el curso real.
- **La primera prueba compara la lista de claves, no solo su presencia.** Es a propósito: `assertNotIn`
  pasaría también si alguien reordenara el dict, y el orden es parte de la forma que la huella
  hashea.
  </action>

  <acceptance_criteria>
    - `src/generar.py` contiene `if curso.segundo_nivel is not None` y
      `"exencion_contra": curso.exencion_contra or "promedio"`.
    - `src/generar.py` **no** contiene `asdict`.
    - `pruebas/test_generar.py` contiene `import modelo`, `import validar`,
      `class ManifiestoDelSegundoNivel(unittest.TestCase):` con **dos** métodos `def test_`, y
      `generar.manifiesto(curso, ruta, inf, [], self.cfg, self.cal)`.
    - `pruebas/test_generar.py` **no** contiene `generar.paquete` dentro de la clase nueva.
    - `python -X utf8 -m unittest pruebas.test_generar -v` sale con código 0.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y reporta **261** pruebas
      (259 + 2).
    - `git status --porcelain cursos/` sale vacío: la clase nueva escribe en `mkdtemp`.
    - `git diff --name-only` lista exactamente `src/generar.py` y `pruebas/test_generar.py`.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
Un DI generado con dos niveles deja constancia de ello en su `MANIFIESTO.yaml`, y el de un curso a
un solo nivel conserva su bloque `evaluacion:` con las tres claves de siempre en el mismo orden.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: la no contaminación de R1 en el ciclo rápido</name>

  <files>pruebas/test_validar.py</files>

  <read_first>
    - `pruebas/test_validar.py:685-724` — `CURSOS_DE_CONTROL` y la clase `NoContaminacion`
      completa, con sus tres pruebas actuales: la de texto crudo, la del silencio de R2/R3 y la de
      validez. La nueva es hermana de la segunda y se escribe con la misma forma.
    - `.planning/phases/11-segundo-nivel-de-la-calificacion/11-CONTEXT.md` §D-17 y §D-18 — la
      línea base medida y por qué 38985 no entra en la prueba de silencio de R1.
    - `.planning/phases/11-segundo-nivel-de-la-calificacion/11-VALIDATION.md` §«Mapa de
      verificación» B13/B14 y §«Verificaciones solo manuales» — incluida la discrepancia de B13
      explicada en el `<context>` de este plan.
    - `cursos/2026-2/39056-big-data/curso.yaml` y
      `cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml` — su bloque `evaluacion:`, para
      confirmar con los ojos que ninguno declara las dos claves nuevas antes de escribir una
      prueba que lo afirme.
  </read_first>

  <action>
**1. Amplía `test_los_cursos_de_control_no_declaran_nada_de_la_v2`** (`:700-707`) con las dos
claves nuevas, dentro del mismo `subTest`:

```python
    def test_los_cursos_de_control_no_declaran_nada_de_la_v2(self):
        """Si alguno empezara a declarar `componentes:` o `unidad:`, las dos pruebas de abajo
        dejarían de significar lo que dicen. Desde la Fase 11 la lista incluye las dos claves
        del segundo nivel: es lo que hace que el silencio de R1 signifique algo."""
        for ruta in CURSOS_DE_CONTROL:
            texto = ruta.read_text(encoding="utf-8")
            with self.subTest(curso=ruta.parent.name):
                self.assertNotIn("\n      componentes:", texto)
                self.assertNotIn("unidad: puntos", texto)
                self.assertNotIn("segundo_nivel:", texto)
                self.assertNotIn("exencion_contra:", texto)
```

**2. Añade la prueba hermana del silencio de R1**, inmediatamente después de
`test_los_cursos_de_control_no_emiten_un_solo_hallazgo_de_r2_ni_de_r3` y antes de
`test_los_cursos_de_control_siguen_siendo_validos`:

```python
    def test_los_cursos_de_control_no_emiten_un_solo_hallazgo_de_r1(self):
        """La línea base medida antes de planear la Fase 11 (D-17): cero hallazgos de R1, de
        cualquier nivel, en los dos cursos de Adrian. Lo que REQ-48 exige preservar es ese
        silencio, y R1 es la única regla que esta fase toca.

        38985 queda fuera **a propósito** y no por herencia de D-13: al declarar el catálogo su
        segundo nivel, ese curso —que declara `esquema_id: zra-contabilidad` y no declara
        segundo nivel— gana un aviso de R1 y pasa de 9 a 10 hallazgos. Está medido y aceptado
        (D-18); se comprueba a mano, porque automatizar ese 10 congelaría un número que la Fase
        14 va a cambiar.
        """
        for ruta in CURSOS_DE_CONTROL:
            inf = validar.validar(modelo.cargar(ruta))
            with self.subTest(curso=ruta.parent.name):
                self.assertEqual([], [str(h) for h in inf.hallazgos if h.regla == "R1"])
```

**No toques el docstring de la clase `NoContaminacion` más de lo necesario** y **no dupliques la
clase**: D-17 dice extenderla. Tampoco añadas 38985 a `CURSOS_DE_CONTROL` —esa tupla es la línea
base de la huella y el 531 no está en ella (D-22 de la Fase 9)— ni pongas `huella` en ningún
punto de la suite (D-20: la huella es la Tarea 3, a mano).
  </action>

  <acceptance_criteria>
    - `pruebas/test_validar.py` contiene
      `def test_los_cursos_de_control_no_emiten_un_solo_hallazgo_de_r1(self):` dentro de la clase
      `NoContaminacion`, y esa clase tiene ahora **cuatro** métodos `def test_`.
    - `pruebas/test_validar.py` contiene `self.assertNotIn("segundo_nivel:", texto)` y
      `self.assertNotIn("exencion_contra:", texto)`.
    - `pruebas/test_validar.py` **no** contiene `38985` ni la cadena `huella`, y
      `CURSOS_DE_CONTROL` sigue teniendo **dos** entradas.
    - `python -X utf8 -m unittest pruebas.test_validar.NoContaminacion -v` sale con código 0 con
      cuatro pruebas.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y reporta **262** pruebas
      (261 + 1).
    - `git diff --name-only` en esta tarea lista exactamente `pruebas/test_validar.py`.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
El silencio de R1 sobre los dos cursos de control queda fijado en el ciclo rápido, igual que ya
estaba el de R2 y R3, y la prueba de texto crudo garantiza que ese silencio significa lo que dice.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: cierre de REQ-48 — la huella y el 38985, a mano</name>

  <files>(ninguno: esta tarea no modifica archivos)</files>

  <read_first>
    - `src/huella.py` — los subcomandos `verificar` y `registrar`, y qué compara cada uno: los tres
      hashes por documento (texto del `.docx`, informe de validación y **forma** del
      `MANIFIESTO.yaml`, D-27 de la Fase 9).
    - `pruebas/huellas.yaml` — la línea base de los cuatro documentos de control (39056/961,
      39056/962, 39062/971, 39062/972).
    - `.planning/phases/10-reglas-en-la-unidad-declarada/10-04-PLAN.md` §Tarea 3 — cómo se cerró
      REQ-48 en la fase anterior. Este cierre es el mismo, con una diferencia: **esta fase no
      gasta ninguna excepción**, porque ningún curso de control declara las claves nuevas.
    - `.planning/phases/11-segundo-nivel-de-la-calificacion/11-VALIDATION.md` §«Verificaciones
      solo manuales» — las dos filas: `huella verificar` y el 9 → 10 de 38985.
  </read_first>

  <action>
Corre, **en este orden**, y **transcribe la salida literal de cada uno en el SUMMARY** (no una
paráfrasis):

```
python -X utf8 -m unittest discover -s pruebas
python -X utf8 src/huella.py verificar
python src/plantillas.py verificar
python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml
python -X utf8 src/validar.py cursos/2026-2/39062-patrones-de-comportamiento/curso.yaml
python -X utf8 src/validar.py cursos/2026-2/38985-contabilidad-financiera/curso.yaml
git status --porcelain
git diff --name-only
```

Lo que tiene que salir:

1. **La suite completa en verde con 262 pruebas** (245 al empezar la fase, +17). Apunta el número
   en el SUMMARY.
2. **`huella verificar` sin una sola diferencia y con código 0.** Los tres hashes de los cuatro
   documentos coinciden con `pruebas/huellas.yaml`. Es la única ruta que ve el `.docx` y la forma
   del `MANIFIESTO.yaml`, así que es la única que atraparía una regresión de D-16.
3. **`plantillas verificar` en verde**: ninguna plantilla de `referencias/` se tocó (regla
   invariable 4).
4. **39056 y 39062 con 5 hallazgos cada uno, `VÁLIDO`, y ninguna línea de `R1`.**
5. **38985 con 10 hallazgos, uno de ellos un aviso de `R1` que menciona `zra-contabilidad`, y
   `VÁLIDO`.** Confirma las tres cosas por separado y déjalas escritas: eran 9, ahora son 10, el
   nuevo es de R1, es aviso y el curso sigue siendo válido. **Es una consecuencia aceptada a
   propósito, no un defecto**: ese `curso.yaml` está blindado hasta la Fase 14 (D-13 de la Fase
   10), su huella no se vigila (D-22 de la Fase 9) y el aviso **dice la verdad** —le falta el
   segundo nivel que su DI real sí tiene—. **No lo arregles, no lo toques.**
6. **`git status --porcelain` no lista nada bajo `cursos/`**: `huella verificar` es de solo lectura
   y restaura los bytes que leyó (D-23/D-28 de la Fase 9).
7. **`git diff --name-only` lista exactamente los nueve archivos de la fase**: `src/modelo.py`,
   `src/validar.py`, `src/generar.py`, `pruebas/test_modelo.py`, `pruebas/test_validar.py`,
   `pruebas/test_generar.py`, `config/esquemas-evaluacion.yaml`, `AGENTS.md` y
   `.claude/skills/di-validar/SKILL.md`. **Ni `src/render_docx.py`, ni `src/grafo.py`, ni
   `src/huella.py`, ni `config/politicas.yaml`, ni nada de `cursos/`, `referencias/` o
   `ejemplos/`.**

**No corras `python src/huella.py registrar`.** Esta fase no cambia ningún documento: si la huella
difiere, algo se rompió y hay que averiguar qué, no aceptar el cambio. `registrar` borraría la
evidencia.

**No regeneres `grafo/`.** D-21 está medido: `src/grafo.py` no abre el bloque `evaluacion:` en
ningún punto —solo lee `m.rubro` y `m.valor` por meta (`:299`)— así que la parte de REQ-48 que
habla de la forma de `grafo/` se cumple por construcción. Compruébalo, en vez de regenerar, con
`grep -c "segundo_nivel\|exencion" src/grafo.py`, que tiene que dar 0.

**Si `huella verificar` reporta cualquier diferencia: para.** No arregles nada por tu cuenta.
Escribe en el SUMMARY qué documento difiere, en cuál de los tres hashes y qué plan de la fase lo
introdujo, y devuélvelo como bloqueo. REQ-48 es el criterio de aceptación del milestone entero y
una diferencia aquí es una decisión del usuario, no un defecto que se parchea.

Si algo fallara por causa de entorno —falta Word, faltan plantillas, falta un paquete— déjalo
dicho igual en el SUMMARY distinguiendo con claridad «no se pudo comprobar» de «se comprobó y
difiere». Son cosas distintas y solo una de las dos bloquea la fase.
  </action>

  <acceptance_criteria>
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0; el SUMMARY apunta el
      total (262) y el delta contra las 245 del inicio de la fase.
    - `python -X utf8 src/huella.py verificar` sale con código 0 y su salida **no** contiene las
      subcadenas `difiere` ni `cambió`; el SUMMARY la transcribe literal.
    - `python src/plantillas.py verificar` sale con código 0.
    - `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sale 0 y su salida no
      contiene `R1`. Ídem `39062-patrones-de-comportamiento`.
    - La salida de `python -X utf8 src/validar.py
      cursos/2026-2/38985-contabilidad-financiera/curso.yaml` contiene `10 hallazgos`, contiene
      `zra-contabilidad`, contiene `VÁLIDO` y **no** contiene `NO VÁLIDO`; el SUMMARY deja escrito
      que eran 9 y que el hallazgo nuevo es de R1 y de nivel aviso.
    - `grep -c "segundo_nivel\|exencion" src/grafo.py` devuelve 0.
    - `git status --porcelain` no lista ningún archivo bajo `cursos/`, `grafo/`, `referencias/` ni
      `ejemplos/`.
    - `git diff --name-only` lista exactamente los nueve archivos de la fase enumerados en el
      `<action>`, sin ninguno más.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 src/huella.py verificar</automated>
  </verify>

  <done>
REQ-48 cerrado para la Fase 11 por las dos rutas que no se solapan: la suite completa en verde y
la huella de los cuatro documentos de control intacta, sin gastar ninguna excepción. El 9 → 10 de
38985 queda registrado como la deuda medida que la Fase 14 cierra.
  </done>
</task>

</tasks>

<verification>
1. `python -X utf8 -m unittest discover -s pruebas` sale 0 con 262 pruebas.
2. `python -X utf8 src/huella.py verificar` sale 0 y sin diferencias.
3. `python src/plantillas.py verificar` sale 0.
4. `python -X utf8 -m unittest pruebas.test_validar.NoContaminacion -v` sale 0 con cuatro pruebas.
5. `python -X utf8 -m unittest pruebas.test_generar.ManifiestoDelSegundoNivel -v` sale 0 con dos.
6. 38985: 10 hallazgos, uno de R1 y de nivel aviso, `VÁLIDO`. Su `curso.yaml` sin tocar.
7. `grep -c "segundo_nivel\|exencion" src/grafo.py` devuelve 0; `grafo/` sin regenerar.
8. `git status --porcelain cursos/` sale vacío; `git diff --name-only` lista los nueve archivos de
   la fase y ninguno más.
</verification>

<success_criteria>
- Criterio 4 del roadmap: la huella de 39056 y 39062 no cambió, y `grafo/` y `MANIFIESTO.yaml`
  conservan su forma —el primero por construcción (D-21), el segundo por inclusión condicional
  (D-16) comprobada en los dos sentidos.
- Criterio 2 del roadmap: un curso que no declara segundo nivel produce el mismo informe y el mismo
  documento, carácter por carácter.
- REQ-48 sostenido **sin gastar ninguna excepción**: ningún curso de control declara las claves
  nuevas, así que ningún rasgo nuevo se enciende.
- REQ-26 respetado en el manifiesto: registra lo que el curso declara, sin inferir ni rellenar.
- La consecuencia aceptada de D-18 —38985 de 9 a 10 hallazgos, siguiendo válido— está medida y
  escrita, y su archivo no se tocó.
</success_criteria>

<output>
Al terminar, escribe
`.planning/phases/11-segundo-nivel-de-la-calificacion/11-03-SUMMARY.md` con: el total de pruebas
al cerrar la fase y su delta contra 245, la salida literal de `huella verificar`, la salida literal
de la validación de los tres cursos con sus conteos (5 / 5 / 10), la confirmación explícita de que
38985 sigue siendo válido y no se tocó, y la lista de `git diff --name-only`.
</output>
