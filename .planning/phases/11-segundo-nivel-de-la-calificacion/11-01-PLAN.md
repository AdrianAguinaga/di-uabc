---
phase: 11-segundo-nivel-de-la-calificacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modelo.py
  - pruebas/test_modelo.py
  - config/esquemas-evaluacion.yaml
  - AGENTS.md
autonomous: true
requirements: [REQ-41, REQ-26]

must_haves:
  truths:
    - "Un `curso.yaml` que declara `segundo_nivel:` 60/40 con sus dos etiquetas carga sin `ErrorModelo`, y `Curso.segundo_nivel` deja de ser None."
    - "Un curso con `segundo_nivel:` y sin `exencion_contra:` no carga: es `ErrorModelo` con un mensaje que dice qué falta y por qué no hay valor por omisión."
    - "Un `exencion_contra:` fuera de `promedio | calificacion_final` no carga: es `ErrorModelo`."
    - "`exencion_contra: calificacion_final` **sí** carga: es un valor del vocabulario y su rechazo es cosa de R1, no del modelo."
    - "Los tres `curso.yaml` reales siguen cargando con `segundo_nivel is None` y `exencion_contra == \"\"`."
    - "El catálogo declara el segundo nivel de `zra-contabilidad` con los dos rótulos literales del DI de origen, capitalización irregular incluida."
  artifacts:
    - path: "src/modelo.py"
      provides: "los dataclasses `Nivel` y `SegundoNivel`, el vocabulario `EXENCION_CONTRA`, los dos campos nuevos de `Curso` y su carga desde el YAML"
      contains: "class SegundoNivel"
    - path: "config/esquemas-evaluacion.yaml"
      provides: "el segundo nivel 60/40 de `zra-contabilidad` con sus rótulos literales"
      contains: "segundo_nivel:"
    - path: "pruebas/test_modelo.py"
      provides: "el contrato del segundo nivel fijado: carga, obligatoriedad condicional y vocabulario cerrado"
      contains: "class ContratoDelSegundoNivel"
    - path: "AGENTS.md"
      provides: "§«Contrato de `curso.yaml`» con las dos claves nuevas"
      contains: "exencion_contra"
  key_links:
    - from: "src/modelo.py::desde_dict"
      to: "src/modelo.py::SegundoNivel"
      via: "_construir_segundo_nivel(evaluacion.get(\"segundo_nivel\"))"
      pattern: "_construir_segundo_nivel"
    - from: "config/esquemas-evaluacion.yaml"
      to: "conocimiento/ejemplos/531-contabilidad-financiera-2026-1.md:61-65"
      via: "los dos rótulos copiados carácter por carácter de la tabla 1 del DI de origen"
      pattern: "Valor del promedio antes del Examen Ordinario"
---

<objective>
Abrir el contrato de `curso.yaml` al segundo nivel de la calificación: los dataclasses `Nivel` y
`SegundoNivel` en `src/modelo.py`, los dos campos nuevos de `Curso` (`segundo_nivel` y
`exencion_contra`) con su vocabulario cerrado y su obligatoriedad condicional, su carga desde el
bloque `evaluacion:`, la declaración del 60/40 de `zra-contabilidad` en el catálogo con los
rótulos **literales** del DI de origen, y la actualización de `AGENTS.md` §«Contrato de
`curso.yaml`».

Propósito: REQ-41. Hoy `curso.yaml` supone que los rubros suman 100 y ahí termina la
calificación. Este plan le da forma a la frase «y todo esto vale el 60 % de la calificación
final», sin que ninguna regla la lea todavía —eso es el plan 11-02—. Todo el rasgo cuelga de
`Curso.segundo_nivel is None` (D-03), así que un curso que no lo declare se comporta
**exactamente** como hoy y eso se puede comprobar de un vistazo.

Salida: `src/modelo.py` extendido (nunca reemplazado), el catálogo con su segundo nivel, una
clase de pruebas nueva en `pruebas/test_modelo.py` y §«Contrato de `curso.yaml`» al día.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-CONTEXT.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-RESEARCH.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-VALIDATION.md

Decisiones que gobiernan este plan, todas de `11-CONTEXT.md`:

- **D-01** — el segundo nivel es un **par fijo con claves nombradas** (`promedio` / `ordinario`),
  no una lista. No hay tercer sumando.
- **D-02** — la `etiqueta:` de cada nivel es **obligatoria y del contrato**, como
  `Rubro.etiqueta`. El renderizador imprime, no redacta (REQ-26).
- **D-03** — dataclasses propios `Nivel` y `SegundoNivel`; `Curso.segundo_nivel: SegundoNivel |
  None = None`. `None` es «no declarado».
- **D-04** — **los dos porcentajes se declaran; ninguno se deriva del otro.** Si el ordinario se
  calculara como `100 − promedio`, el curso «60 y 30» sería inexpresable y el criterio 1 del
  roadmap no tendría cómo probarse.
- **D-05** — el catálogo también lo declara, con los rótulos **literales**, y el comentario de
  `esquemas-evaluacion.yaml:66-68` se corrige porque esta fase lo vuelve falso.
- **D-06** — `exencion_contra:` es hermana de `exencion_ordinario:` dentro de `evaluacion:`, con
  vocabulario cerrado `promedio | calificacion_final`. **El valor malo está en el vocabulario a
  propósito**, para que R1 pueda explicarlo en el plan 11-02.
- **D-07** — obligatoria cuando hay segundo nivel (su ausencia entonces es `ErrorModelo`);
  opcional sin él, y ausente significa `promedio`. **Por eso el campo no lleva default
  `"promedio"`**: lo llevaría y «ausente» sería indistinguible de «declarado promedio».
- **D-19** — los fixtures son variantes de `CURSO_VALIDO` por `deepcopy`. Nada de esto vive en
  `cursos/` (D-12 de la Fase 10).

Fuera de este plan, y no por descuido: **R1 no se toca aquí** (plan 11-02), el
`MANIFIESTO.yaml` tampoco (plan 11-03), y `src/render_docx.py`, `src/grafo.py`,
`config/politicas.yaml` y `cursos/2026-2/38985-.../curso.yaml` **no se tocan en toda la fase**
(D-10, D-21, D-18).

<interfaces>
```python
# src/modelo.py:31-36 — los vocabularios cerrados, tal como están hoy
MODALIDADES = ("semipresencial", "escolarizada", "a_distancia")
TIPOS_META = ("encuadre", "aprendizaje", "examen_parcial", "cierre")
AMBIENTES = ("presencial", "virtual")
UNIDADES_RUBRO = ("puntos",)  # ausente = porcentaje
TIPOS_COMPONENTE = ("examen_parcial", "examen_ordinario", "actividad", "proyecto")

# src/modelo.py:188-197 — el molde de dataclass del contrato: campos sin default = obligatorios
@dataclass
class Rubro:
    id: str
    etiqueta: str
    porcentaje: float
    detalle: str = ""
    ...

# src/modelo.py:299-305 — el molde de vocabulario cerrado a nivel de Curso
def __post_init__(self) -> None:
    if self.modalidad not in MODALIDADES:
        raise ErrorModelo(
            f"Modalidad inválida: {self.modalidad!r}. Válidas: {', '.join(MODALIDADES)}"
        )
    if not self.grupos:
        raise ErrorModelo("El curso debe tener al menos un grupo.")

# src/modelo.py:382-408 — desde_dict(): `evaluacion` se saca en :385 y se leen dos claves
#   sueltas al construir Curso(...)
evaluacion = d.pop("evaluacion", {})
...
    exencion_ordinario=evaluacion.get("exencion_ordinario", 80),
    esquema_id=evaluacion.get("esquema_id", ""),

# src/modelo.py:433-446 — cargar() atrapa TypeError y lo traduce:
#   ErrorModelo(f"{ruta}: campo inesperado o faltante — {e}")
#   Es lo que ya protege a Rubro(**r): un `segundo_nivel:` mal formado cae ahí con mensaje
#   genérico, y eso es aceptable (patrón existente). No prometas mensaje específico para
#   cualquier forma mal escrita: solo para los dos casos de D-06 y D-07.
```

Los helpers de prueba disponibles: `pruebas/test_modelo.py` importa `CURSO_VALIDO` y `_meta` de
`test_validar` y tiene `curso_con(rubros, metas=None)` (`:30-36`), que reemplaza los **rubros**.
Para este plan hace falta tocar otras claves de `evaluacion:`, así que la clase nueva lleva su
propio helper (ver la Tarea 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: `Nivel`, `SegundoNivel` y las dos claves nuevas de `Curso`</name>

  <files>src/modelo.py</files>

  <read_first>
    - `src/modelo.py:31-41` — los cinco vocabularios cerrados y `ErrorModelo`. Ahí entra
      `EXENCION_CONTRA`.
    - `src/modelo.py:188-229` — `Rubro` entero: el molde de dataclass del contrato (campos sin
      default = obligatorios) y su `__post_init__` con mensajes que explican qué falta y por qué.
      **Fíjate en que `Rubro.etiqueta` no tiene default y tampoco tiene comprobación de cadena
      vacía**: `Nivel` hace lo mismo, ni más ni menos.
    - `src/modelo.py:271-333` — `Curso`: dónde viven `exencion_ordinario` (`:288`) y `rubros`
      (`:285`), y el `__post_init__` (`:299-305`) que gana dos comprobaciones.
    - `src/modelo.py:339-408` — `_construir_componente`, `_construir_meta`, `_construir_grupo` y
      `desde_dict`. Los tres primeros son el molde de la función constructora que hace falta
      aquí; `desde_dict` es donde se leen las dos claves nuevas.
    - `src/modelo.py:433-446` — `cargar()` y cómo traduce `TypeError` a `ErrorModelo`.
  </read_first>

  <behavior>
    Lo que la Tarea 2 va a fijar con pruebas. Escribe el código contra esta lista:

    - Un `evaluacion:` con `segundo_nivel: {promedio: {porcentaje: 60, etiqueta: "A"},
      ordinario: {porcentaje: 40, etiqueta: "B"}}` y `exencion_contra: promedio` carga:
      `c.segundo_nivel.promedio.porcentaje == 60`, `c.segundo_nivel.ordinario.etiqueta == "B"`,
      `isinstance(c.segundo_nivel, modelo.SegundoNivel)`.
    - Un `evaluacion:` sin ninguna de las dos claves carga con `c.segundo_nivel is None` y
      `c.exencion_contra == ""`.
    - `segundo_nivel:` presente y `exencion_contra:` ausente → `ErrorModelo`, y el mensaje
      contiene `exencion_contra` y `segundo_nivel`.
    - `exencion_contra: promedio_del_curso` (fuera del vocabulario) → `ErrorModelo`, y el mensaje
      contiene `promedio_del_curso`, `promedio` y `calificacion_final`.
    - `exencion_contra: calificacion_final` **con** segundo nivel → **carga sin excepción**. Es un
      valor del vocabulario; su rechazo es de R1 (D-08), no del modelo.
    - `exencion_contra: calificacion_final` **sin** segundo nivel → carga sin excepción.
  </behavior>

  <action>
**1. El vocabulario cerrado.** Añade a `src/modelo.py`, junto a los otros cinco (después de
`TIPOS_COMPONENTE`, línea 36):

```python
# El segundo valor es válido en el contrato y R1 lo rechaza (D-06/D-08): tiene que poder
# escribirse para que la regla pueda explicar por qué está mal.
EXENCION_CONTRA = ("promedio", "calificacion_final")
```

**2. Los dos dataclasses.** Van **inmediatamente después de `Rubro`** (que termina en la línea
229, con `a_porcentaje`) y **antes de `@dataclass class Horario`** (línea 232):

```python
@dataclass
class Nivel:
    """Uno de los dos sumandos de la calificación final: el promedio, o el ordinario.

    `etiqueta` es del contrato y no tiene valor por omisión: los rótulos del DI de origen
    —«Valor del promedio antes del Examen Ordinario»— son redacción de la docente, no
    vocabulario del proyecto. El renderizador los imprime, no los redacta (REQ-26).
    """

    porcentaje: float
    etiqueta: str


@dataclass
class SegundoNivel:
    """Cómo se combina el promedio del curso con el examen ordinario.

    Par fijo con claves nombradas y no lista: la exención se mide contra **el promedio**, una
    fila concreta, no contra «una del montón». Con una lista genérica R1 tendría que
    identificar por id cuál de las filas es el promedio antes de poder comprobar nada (D-01).
    El Estatuto no contempla un tercer sumando.
    """

    promedio: Nivel
    ordinario: Nivel
```

**Ninguno de los dos lleva `__post_init__`.** `Nivel` no tiene nada que comprobar que el tipo no
garantice ya: los dos campos son obligatorios por no tener default, y rechazar un porcentaje
negativo o una etiqueta vacía no lo pide ningún criterio del roadmap. Es el mismo trato que
`Rubro.etiqueta`. Que los dos porcentajes sumen 100 es aritmética, y la aritmética es de R1
(plan 11-02), nunca del modelo.

**3. Los dos campos de `Curso`.** En el dataclass `Curso` (`:271-297`), dos inserciones:

```python
    rubros: list[Rubro] = field(default_factory=list)
    segundo_nivel: SegundoNivel | None = None   # None = un solo nivel: el promedio ES la nota
    grupos: list[Grupo] = field(default_factory=list)
```

```python
    exencion_ordinario: int = 80
    exencion_contra: str = ""   # ausente = promedio; obligatoria si hay segundo nivel (D-07)
```

**El default de `exencion_contra` es la cadena vacía, no `"promedio"`.** Con `"promedio"` de
default, «ausente» y «declarado promedio» serían indistinguibles y la obligatoriedad de D-07 no
se podría comprobar. Quien consuma el campo lee `exencion_contra or "promedio"`.

**4. Las dos comprobaciones de `__post_init__`.** Al final del `__post_init__` de `Curso`
(después de la comprobación de `grupos`, línea 305):

```python
        if self.exencion_contra and self.exencion_contra not in EXENCION_CONTRA:
            raise ErrorModelo(
                f"`exencion_contra` inválido: {self.exencion_contra!r}. "
                f"Válidos: {', '.join(EXENCION_CONTRA)}. «promedio» mide el umbral contra el "
                f"promedio del curso; «calificacion_final» contra la calificación ya combinada "
                f"con el examen ordinario."
            )
        if self.segundo_nivel is not None and not self.exencion_contra:
            raise ErrorModelo(
                "El curso declara `segundo_nivel` pero no `exencion_contra`. Con dos niveles "
                "hay que decir contra cuál se mide el umbral de exención: `promedio` o "
                "`calificacion_final`. No hay valor por omisión a propósito: suponerlo "
                "convertiría una decisión del docente en una suposición del generador."
            )
```

El orden importa poco pero no es indiferente: primero el vocabulario, después la obligatoriedad,
para que un curso que escribe mal el valor **y** no tiene segundo nivel reciba el mensaje del
vocabulario, que es el que le sirve.

**5. La función constructora y la carga.** Junto a `_construir_grupo` (`:375-379`), antes de
`desde_dict`:

```python
def _construir_segundo_nivel(sn: dict | None) -> SegundoNivel | None:
    if sn is None:
        return None
    return SegundoNivel(
        promedio=Nivel(**sn["promedio"]),
        ordinario=Nivel(**sn["ordinario"]),
    )
```

Y dentro de la llamada a `Curso(...)` de `desde_dict` (`:388-408`), junto a las dos claves de
`evaluacion:` que ya se leen ahí:

```python
        rubros=[Rubro(**r) for r in evaluacion.get("rubros", [])],
        segundo_nivel=_construir_segundo_nivel(evaluacion.get("segundo_nivel")),
        grupos=[_construir_grupo(g) for g in d.pop("grupos", [])],
        exencion_ordinario=evaluacion.get("exencion_ordinario", 80),
        exencion_contra=evaluacion.get("exencion_contra", ""),
        esquema_id=evaluacion.get("esquema_id", ""),
```

Hace falta la función y no una línea suelta porque hay que convertir **dos sub-dicts anidados**
en dos `Nivel` antes de envolverlos en `SegundoNivel`. Un `segundo_nivel:` mal formado —por
ejemplo `promedio:` sin `porcentaje:`— levanta `TypeError` dentro de `Nivel(**...)` y
`cargar()` lo traduce a `ErrorModelo(f"{ruta}: campo inesperado o faltante — {e}")`, igual que
ya hace con `Rubro(**r)`. Es el patrón existente y basta: **no añadas comprobaciones de forma
propias** más allá de las dos de D-06/D-07.

**No toques `src/validar.py`, `src/generar.py`, `src/render_docx.py` ni `src/grafo.py` en esta
tarea.** Tras ella la suite tiene que seguir con las 245 pruebas en verde, sin una sola nueva:
abrir el contrato no cambia el comportamiento de nadie.
  </action>

  <acceptance_criteria>
    - `src/modelo.py` contiene `EXENCION_CONTRA = ("promedio", "calificacion_final")`,
      `class Nivel:`, `class SegundoNivel:` y `def _construir_segundo_nivel(`.
    - `src/modelo.py` contiene `segundo_nivel: SegundoNivel | None = None` y
      `exencion_contra: str = ""`. **No** contiene `exencion_contra: str = "promedio"`.
    - `src/modelo.py` contiene `_construir_segundo_nivel(evaluacion.get("segundo_nivel"))` y
      `exencion_contra=evaluacion.get("exencion_contra", "")`.
    - Ni `class Nivel` ni `class SegundoNivel` declaran `__post_init__`.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y reporta **245**
      pruebas: esta tarea no añade ninguna y no rompe ninguna.
    - Este comando imprime exactamente `60 B 'promedio'`:
      `python -X utf8 -c "import sys,copy;sys.path.insert(0,'src');sys.path.insert(0,'pruebas');import modelo;from test_validar import CURSO_VALIDO as V;d=copy.deepcopy(V);d['evaluacion']['segundo_nivel']={'promedio':{'porcentaje':60,'etiqueta':'A'},'ordinario':{'porcentaje':40,'etiqueta':'B'}};d['evaluacion']['exencion_contra']='promedio';c=modelo.desde_dict(d);print(c.segundo_nivel.promedio.porcentaje,c.segundo_nivel.ordinario.etiqueta,repr(c.exencion_contra))"`
    - Este comando imprime `None ''`:
      `python -X utf8 -c "import sys;sys.path.insert(0,'src');import modelo;c=modelo.cargar('cursos/2026-2/39056-big-data/curso.yaml');print(c.segundo_nivel,repr(c.exencion_contra))"`
    - `git diff --name-only` lista exactamente `src/modelo.py`.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
El contrato sabe decir «el promedio vale 60 y el ordinario 40, y la exención se mide contra el
promedio». Ningún curso existente cambia de comportamiento: los tres siguen cargando con
`segundo_nivel is None`.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: el contrato del segundo nivel, fijado con pruebas</name>

  <files>pruebas/test_modelo.py</files>

  <read_first>
    - `pruebas/test_modelo.py` **completo** (370 líneas). Importa: el encabezado y el import de
      `CURSO_VALIDO`/`_meta` (`:12-27`), el helper `curso_con` (`:30-36`), la clase
      `RubroEnPuntos` (`:39-102`) como molde de las pruebas de `ErrorModelo` con
      `assertRaises` + `assertIn` sobre el mensaje, y `LosCursosExistentesNoCambian`
      (`:347-366`), que se amplía en esta misma tarea.
    - `src/modelo.py` tras la Tarea 1 — los dos dataclasses, el vocabulario y las dos
      comprobaciones del `__post_init__`, para que las pruebas asserten sobre los mensajes que
      de verdad se escribieron.
    - `pruebas/test_validar.py:60-118` — `CURSO_VALIDO` y los helpers `curso()`/`informe()`.
      **Ojo: hoy viven en `:110-118`, no en `:104-112` como cita `11-CONTEXT.md`** (drift de 6
      líneas detectado en la investigación; el contenido citado es correcto).
  </read_first>

  <action>
Añade a `pruebas/test_modelo.py` una clase nueva **inmediatamente después de `ComponentesDeMeta`
y antes de `class IdentificadoresLibres`** (línea 219). Los fixtures son variantes de
`CURSO_VALIDO` por `deepcopy` (D-19); el helper propio de la clase existe porque `curso_con()`
solo reemplaza los rubros y aquí hay que tocar otras claves de `evaluacion:`.

```python
class ContratoDelSegundoNivel(unittest.TestCase):
    """REQ-41: la calificación puede tener dos niveles, y el contrato dice contra cuál se mide
    la exención.

    Aquí solo se comprueba que **cargue**. Que los dos porcentajes sumen 100, que 100/0 sea
    aviso y que `calificacion_final` con segundo nivel sea error son hallazgos de R1, y viven
    en `test_validar.py` (D-08: un valor conocido y prohibido es juicio semántico, territorio
    de las reglas, no brecha de vocabulario).
    """

    NIVEL_60_40 = {
        "promedio": {"porcentaje": 60,
                     "etiqueta": "Valor del promedio antes del Examen Ordinario"},
        "ordinario": {"porcentaje": 40,
                      "etiqueta": "Valor del examen Ordinario"},
    }

    def _cargar(self, **evaluacion):
        """Copia del curso válido con las claves indicadas fundidas en `evaluacion:`."""
        d = copy.deepcopy(CURSO_VALIDO)
        d["evaluacion"] = {**copy.deepcopy(CURSO_VALIDO["evaluacion"]), **evaluacion}
        return modelo.desde_dict(d)

    def test_el_par_de_niveles_carga_con_sus_porcentajes_y_sus_etiquetas(self):
        c = self._cargar(segundo_nivel=self.NIVEL_60_40, exencion_contra="promedio")
        self.assertIsInstance(c.segundo_nivel, modelo.SegundoNivel)
        self.assertIsInstance(c.segundo_nivel.promedio, modelo.Nivel)
        self.assertEqual(60, c.segundo_nivel.promedio.porcentaje)
        self.assertEqual(40, c.segundo_nivel.ordinario.porcentaje)
        self.assertEqual(
            "Valor del promedio antes del Examen Ordinario",
            c.segundo_nivel.promedio.etiqueta,
        )
        self.assertEqual("Valor del examen Ordinario", c.segundo_nivel.ordinario.etiqueta)

    def test_un_curso_que_no_lo_declara_lo_deja_en_none(self):
        """El rasgo entero cuelga de este `None` (D-03): sin él no hay nada que activar."""
        c = self._cargar()
        self.assertIsNone(c.segundo_nivel)
        self.assertEqual("", c.exencion_contra)
        self.assertEqual("promedio", c.exencion_contra or "promedio")

    def test_segundo_nivel_sin_exencion_contra_es_error_modelo(self):
        """D-07: el dato que decide el significado no se infiere. Con dos niveles hay que
        decir contra cuál se mide el umbral."""
        with self.assertRaises(modelo.ErrorModelo) as ctx:
            self._cargar(segundo_nivel=self.NIVEL_60_40)
        self.assertIn("exencion_contra", str(ctx.exception))
        self.assertIn("segundo_nivel", str(ctx.exception))

    def test_exencion_contra_fuera_del_vocabulario_es_error_modelo(self):
        """D-06, siguiendo D-03 de la Fase 9: un valor que el modelo no conoce revienta al
        cargar, no al validar."""
        with self.assertRaises(modelo.ErrorModelo) as ctx:
            self._cargar(exencion_contra="promedio_del_curso")
        self.assertIn("promedio_del_curso", str(ctx.exception))
        self.assertIn("calificacion_final", str(ctx.exception))

    def test_calificacion_final_carga_aunque_r1_la_rechace(self):
        """La frontera de D-08: `calificacion_final` está en el vocabulario **a propósito**.
        Si reventara aquí, R1 no podría explicar la diferencia y el criterio 3 del roadmap se
        quedaría sin mensaje pedagógico."""
        c = self._cargar(segundo_nivel=self.NIVEL_60_40, exencion_contra="calificacion_final")
        self.assertEqual("calificacion_final", c.exencion_contra)

    def test_exencion_contra_sin_segundo_nivel_es_opcional(self):
        """Sin segundo nivel la clave es opcional y declararla no rompe nada: es el caso de los
        dos cursos de control, que no la declaran (REQ-48)."""
        self.assertEqual("", self._cargar().exencion_contra)
        self.assertEqual("promedio", self._cargar(exencion_contra="promedio").exencion_contra)
```

Y **amplía** `LosCursosExistentesNoCambian.test_los_tres_cursos_cargan_sin_declarar_nada_nuevo`
(`:356-366`) con las dos afirmaciones nuevas, dentro del mismo `subTest`, después del bucle de
metas:

```python
                self.assertIsNone(c.segundo_nivel)
                self.assertEqual("", c.exencion_contra)
```

Esa ampliación es la mitad de REQ-48 resuelta por construcción para esta fase: los tres
`curso.yaml` reales —39056, 39062 y **38985**— siguen cargando a un solo nivel. Nótese que 38985
sí entra **aquí** (carga) y no entrará en la prueba de silencio de R1 del plan 11-03, por D-18.

**Si alguna de estas pruebas falla, el defecto está en la Tarea 1, no en la prueba.** No
debilites una afirmación para que pase: la lista de `<behavior>` de la Tarea 1 es la misma que
esta clase, y se escribió antes de escribir el código.
  </action>

  <acceptance_criteria>
    - `pruebas/test_modelo.py` contiene `class ContratoDelSegundoNivel(unittest.TestCase):` con
      **seis** métodos `def test_`.
    - `pruebas/test_modelo.py` contiene la cadena literal
      `"Valor del promedio antes del Examen Ordinario"` y la cadena literal
      `"Valor del examen Ordinario"` (con la «e» de «examen» en minúscula).
    - `pruebas/test_modelo.py` contiene `self.assertIsNone(c.segundo_nivel)` dentro de
      `LosCursosExistentesNoCambian`.
    - `python -X utf8 -m unittest pruebas.test_modelo -v` sale con código 0.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y reporta **251**
      pruebas (245 + 6).
    - `git diff --name-only` no lista ningún archivo de `src/` en esta tarea.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
El contrato del segundo nivel está fijado: carga con sus dos etiquetas, la obligatoriedad
condicional de `exencion_contra` revienta al cargar, el vocabulario cerrado rechaza lo que no
conoce y admite lo que R1 tiene que rechazar, y los tres cursos reales siguen a un solo nivel.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: el catálogo declara el 60/40 de `zra-contabilidad`, y `AGENTS.md` el contrato</name>

  <files>config/esquemas-evaluacion.yaml, AGENTS.md</files>

  <read_first>
    - `config/esquemas-evaluacion.yaml:59-82` — el bloque `zra-contabilidad` entero, incluido el
      comentario de `:62-68` cuyas tres últimas líneas hay que corregir, y `:103-108` — el bloque
      `reglas` con `suma_exacta: 100`.
    - `conocimiento/ejemplos/531-contabilidad-financiera-2026-1.md:46-71` — **la fuente de los
      rótulos.** La tabla 1 está en `:61-65`. Léela antes de escribir las etiquetas y cópialas
      carácter por carácter.
    - `AGENTS.md:142-166` — §«Contrato de `curso.yaml`»: el bloque de claves de primer nivel
      (la línea de `evaluacion:` está en `:156-157`) y las dos líneas de prosa que lo siguen.
    - `.planning/STATE.md:99-102` — el hueco documental que dejó la Fase 9 en esta misma sección
      (`componentes:`, `unidad`, `total` sin listar) y a qué fase está asignado.
  </read_first>

  <action>
**1. El catálogo.** En `config/esquemas-evaluacion.yaml`, dentro de `zra-contabilidad`, añade el
bloque `segundo_nivel:` **entre `rubros:` y `exencion_ordinario: 90`** (es decir, después de la
línea 81 y antes de la 82):

```yaml
    segundo_nivel:
      promedio:
        porcentaje: 60
        etiqueta: "Valor del promedio antes del Examen Ordinario"
      ordinario:
        porcentaje: 40
        etiqueta: "Valor del examen Ordinario"
```

**Las dos etiquetas son literales del DI de origen y se copian carácter por carácter.** En la
primera, «Examen Ordinario» lleva mayúscula en ambas palabras; en la segunda, «examen Ordinario»
lleva la «e» de «examen» **en minúscula**. Es una inconsistencia real de la docente en su propio
documento, no un error de transcripción de este repositorio: **no la corrijas.** Si el catálogo
«arregla» esa minúscula, el `curso.yaml` de la Fase 14 arrancará con un aviso espurio de R1 el
día que declare el segundo nivel con el rótulo verdadero (D-05 y D-14). La prueba
`test_el_segundo_nivel_del_catalogo_no_produce_ningun_aviso` del plan 11-02 existe para
denunciarlo, pero eso es el plan siguiente: aquí la única defensa es copiar bien.

La estructura tiene que ser **exactamente** esa: dos claves de nivel (`promedio`, `ordinario`) y
dos claves por nivel (`porcentaje`, `etiqueta`), sin ninguna más. El contraste de D-14 compara
este dict del catálogo contra el del curso por igualdad, así que una clave extra o un nombre
distinto haría divergir siempre.

**2. El comentario que esta fase vuelve falso.** En el mismo archivo, las líneas 66-68 dicen hoy:

```yaml
    # Su documento reparte las actividades en puntos (10 por meta, 150 declarados) y añade
    # un segundo nivel: el promedio vale 60 % y el examen ordinario 40 %. Ese segundo nivel
    # NO existe en el modelo; ver conocimiento/ejemplos/531-contabilidad-financiera-2026-1.md.
```

Sustitúyelas por:

```yaml
    # Su documento reparte las actividades en puntos (10 por meta, 150 declarados) y añade
    # un segundo nivel: el promedio vale 60 % y el examen ordinario 40 %. Desde la Fase 11 el
    # modelo lo declara aquí abajo, con los rótulos literales de su DI —capitalización
    # irregular incluida, para que el contraste de R1 coincida—; ver
    # conocimiento/ejemplos/531-contabilidad-financiera-2026-1.md.
```

**No toques** las tres líneas de `rubros:` de `zra-contabilidad`, ni `exencion_ordinario: 90`,
ni el bloque `reglas` del final: `suma_exacta: 100` es el único sitio donde vive el 100 del
proyecto y el plan 11-02 lo reutiliza tal cual (D-12). **No añadas ninguna clave nueva a
`reglas:`.**

**3. `AGENTS.md` §«Contrato de `curso.yaml`».** Es requisito de `11-CONTEXT.md`
§canonical_refs que se actualice **en el mismo plan que añade las claves**, y este es ese plan.
Sustituye las líneas 156-157, que hoy dicen:

```
evaluacion:      {esquema_id, exencion_ordinario, rubros:[{id, etiqueta,
                                                           porcentaje, detalle, parciales}]}
```

por:

```
evaluacion:      {esquema_id, exencion_ordinario, exencion_contra,
                  segundo_nivel:{promedio:{porcentaje, etiqueta},
                                 ordinario:{porcentaje, etiqueta}},
                  rubros:[{id, etiqueta, porcentaje, detalle, parciales}]}
```

Y añade, justo después de la línea 166 (la que termina «Solo `encuadre` y `cierre` pueden valer
0 %.»), este párrafo:

```
`exencion_contra`: `promedio` · `calificacion_final`. **Obligatoria si el curso declara
`segundo_nivel`** —con dos niveles hay que decir contra cuál se mide el umbral— y opcional sin
él, donde ausente significa `promedio`. Un curso que no declara `segundo_nivel` califica a un
solo nivel: el promedio *es* la calificación. Las dos `etiqueta` del segundo nivel son del
contrato: el generador las imprime, no las redacta.
```

**Lo que NO se toca en `AGENTS.md`:** el hueco que dejó la Fase 9 en esta misma sección
—`componentes:` sin listar en las metas, `unidad`/`total` sin listar en los rubros— sigue
abierto **a propósito**. `STATE.md:99-102` lo asigna a la Fase 12, que es la que vuelve a tocar
el contrato. Cerrarlo aquí sería trabajo de otra fase colado en esta. Tampoco toques
§«Las ocho reglas de validación»: R1 no ha cambiado todavía y su actualización es del plan
11-02.
  </action>

  <acceptance_criteria>
    - `config/esquemas-evaluacion.yaml` contiene, dentro de `zra-contabilidad`, la cadena
      `segundo_nivel:` y las dos cadenas literales
      `etiqueta: "Valor del promedio antes del Examen Ordinario"` y
      `etiqueta: "Valor del examen Ordinario"`.
    - `config/esquemas-evaluacion.yaml` **no** contiene ya la cadena `NO existe en el modelo`.
    - `config/esquemas-evaluacion.yaml` sigue conteniendo `suma_exacta: 100` y **no** contiene
      ninguna clave nueva bajo `reglas:` (el bloque sigue teniendo cuatro claves).
    - Este comando imprime `60 Valor del examen Ordinario 90`:
      `python -X utf8 -c "import sys;sys.path.insert(0,'src');import modelo;e=modelo.Config().esquema('zra-contabilidad');n=e['segundo_nivel'];print(n['promedio']['porcentaje'],n['ordinario']['etiqueta'],e['exencion_ordinario'])"`
    - `AGENTS.md` contiene `exencion_contra` y `segundo_nivel:{promedio:{porcentaje, etiqueta}`.
    - `AGENTS.md` §«Contrato de `curso.yaml`» **no** menciona `componentes` ni `unidad, total`
      en el bloque de claves: el hueco de la Fase 9 sigue asignado a la Fase 12.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y sigue reportando 251
      pruebas: declarar el segundo nivel en el catálogo no cambia el comportamiento de nadie
      todavía, porque `Config.esquema()` solo lo consume `validar.py:157` y R1 aún no lo lee.
    - `python -X utf8 src/validar.py cursos/2026-2/38985-contabilidad-financiera/curso.yaml`
      sigue reportando **9 hallazgos** y `VÁLIDO`. El paso de 9 a 10 lo introduce el plan 11-02,
      no este.
    - `git status --porcelain` no lista nada bajo `cursos/`, `referencias/` ni `ejemplos/`.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
El catálogo declara el segundo nivel de Zurisaddai con los rótulos que su documento usa de
verdad, su comentario ya no afirma algo falso, y §«Contrato de `curso.yaml`» describe las dos
claves nuevas. Ningún informe de validación ha cambiado todavía.
  </done>
</task>

</tasks>

<verification>
1. `python -X utf8 -m unittest discover -s pruebas` sale 0 con 251 pruebas.
2. `python -X utf8 -m unittest pruebas.test_modelo.ContratoDelSegundoNivel -v` sale 0 con 6
   pruebas.
3. `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sale 0 y sigue
   reportando 5 hallazgos; ídem `39062-patrones-de-comportamiento`. `38985` sigue en 9.
4. `python src/plantillas.py verificar` sale 0: ninguna plantilla de `referencias/` se tocó.
5. `git diff --name-only` lista exactamente cuatro archivos: `src/modelo.py`,
   `pruebas/test_modelo.py`, `config/esquemas-evaluacion.yaml`, `AGENTS.md`.
6. `git status --porcelain cursos/` sale vacío.
</verification>

<success_criteria>
- REQ-41 tiene su mitad de modelo: el contrato declara el segundo nivel como par fijo con
  etiquetas obligatorias, y `exencion_contra` con vocabulario cerrado y obligatoriedad
  condicional.
- Un curso que no declara nada carga y valida **exactamente** como antes de esta fase: mismos
  hallazgos, mismos números.
- Los dos rótulos del catálogo son los literales del DI de origen, con su capitalización
  irregular intacta.
- `AGENTS.md` §«Contrato de `curso.yaml`» está al día con las dos claves nuevas.
- Ni `src/validar.py`, ni `src/generar.py`, ni `src/render_docx.py`, ni `src/grafo.py`, ni
  `config/politicas.yaml`, ni ningún `curso.yaml` de `cursos/` se tocaron.
</success_criteria>

<output>
Al terminar, escribe
`.planning/phases/11-segundo-nivel-de-la-calificacion/11-01-SUMMARY.md`, apuntando el número de
pruebas de la suite al cerrar el plan y el conteo de hallazgos de los tres cursos reales (que
debe seguir siendo 5, 5 y 9).
</output>
