---
phase: 11-segundo-nivel-de-la-calificacion
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/validar.py
  - pruebas/test_validar.py
  - AGENTS.md
  - .claude/skills/di-validar/SKILL.md
autonomous: true
requirements: [REQ-46]

must_haves:
  truths:
    - "Un curso con segundo nivel 60/40 y la exención contra el promedio no produce ningún hallazgo de R1."
    - "Un segundo nivel de 60/30 es error de R1, y el mensaje dice 90 y 100."
    - "Un segundo nivel de 100/0 y uno de 0/100 son aviso de R1, cada uno con su mensaje, y el curso sigue siendo válido."
    - "`exencion_contra: calificacion_final` con segundo nivel es error de R1, con un mensaje que explica la diferencia entre medir contra el promedio y medir contra la calificación final."
    - "`exencion_contra: calificacion_final` sin segundo nivel es aviso de R1, y el curso sigue siendo válido."
    - "Si el curso declara `esquema_id`, su segundo nivel se contrasta contra el del catálogo —porcentajes y etiquetas— con aviso."
    - "El fuente de `regla_1` sigue sin contener `.base`, `a_porcentaje`, `.unidad`, `.total` ni `.valor`, ni en código ni en comentarios."
  artifacts:
    - path: "src/validar.py"
      provides: "las comprobaciones del segundo nivel dentro de `regla_1`, antes del bloque de `esquema_id`, y el contraste de catálogo ampliado"
      contains: "self.c.segundo_nivel"
    - path: "pruebas/test_validar.py"
      provides: "las ocho pruebas del segundo nivel en R1"
      contains: "class Regla1SegundoNivel"
    - path: "AGENTS.md"
      provides: "§«Las ocho reglas de validación» con el comportamiento nuevo de R1"
      contains: "segundo_nivel"
  key_links:
    - from: "src/validar.py::regla_1"
      to: "src/modelo.py::SegundoNivel"
      via: "self.c.segundo_nivel, leído inline dentro de regla_1 (nunca desde un método auxiliar)"
      pattern: "self\\.c\\.segundo_nivel"
    - from: "src/validar.py::regla_1"
      to: "config/esquemas-evaluacion.yaml"
      via: "reglas[\"suma_exacta\"], ya cargado al principio de regla_1 como `exacta`"
      pattern: "!= exacta"
---

<objective>
Que R1 sepa leer el segundo nivel: que sus dos porcentajes sumen 100, que 100/0 y 0/100 se
señalen sin bloquear, que la exención declarada contra la calificación final sea error cuando hay
dos niveles y aviso cuando no los hay, y que el segundo nivel del curso se contraste contra el del
catálogo. Y actualizar §«Las ocho reglas de validación» de `AGENTS.md`, más la tabla gemela del
skill `di-validar`.

Propósito: REQ-46 y los criterios 1 y 3 del roadmap. El modelo ya sabe declarar el segundo nivel
(plan 11-01) pero nadie lo comprueba: un curso podría declarar 60 y 30 y validar. Aquí es también
donde vive la trampa medida de la fase — la guarda `getsource` de la Fase 10 es **textual** y
mira el fuente entero de `regla_1`, comentarios incluidos.

Salida: `regla_1` con dos bloques nuevos, una clase de ocho pruebas en `pruebas/test_validar.py`,
y las dos tablas de las ocho reglas al día.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-CONTEXT.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-RESEARCH.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-VALIDATION.md
@.planning/phases/11-segundo-nivel-de-la-calificacion/11-01-SUMMARY.md

Decisiones que gobiernan este plan:

- **D-11 — dónde va el código, y no es cuestión de estilo.** Las comprobaciones nuevas van
  **dentro de `regla_1`**, después de los rubros duplicados y **antes** del bloque de `esquema_id`
  (`validar.py:154`). Medido: `regla_1` tiene dos salidas tempranas —`:130` si el curso no declara
  rubros y `:160` si el `esquema_id` no existe en el catálogo—. Puestas al final, un curso con el
  `esquema_id` mal escrito **no recibiría** las comprobaciones del segundo nivel, y ese es
  exactamente el perfil del curso que motiva la fase.
- **D-11 — nunca un método auxiliar.** `test_el_codigo_de_r1_no_menciona_la_unidad_de_ningun_rubro`
  (`test_validar.py:287-292`) solo lee el fuente de `regla_1`. Sacar la aritmética nueva a un
  helper haría que **la guarda dejara de cubrirla en silencio**: nada fallaría. Se escribe inline.
- **D-12** — la suma se compara contra `reglas["suma_exacta"]`, que `regla_1` ya tiene cargado en
  la variable `exacta` (`:133`), con el mismo `round(..., 2)` del resto del archivo. **Ni una
  clave nueva en el config, ni un 100 literal.**
- **D-13** — 100/0 y 0/100 son **aviso**, no error: suman 100, así que no hay defecto aritmético.
  **Decidido para este plan:** dos mensajes distintos, uno por caso, siguiendo el patrón del
  archivo (cada comprobación tiene su propio mensaje) — así B7 y B8 son dos pruebas y no dos
  `subTest`.
- **D-14** — el contraste contra el catálogo se amplía al segundo nivel comparando **porcentajes y
  etiquetas**, con **aviso**, y se queda **dentro del `if self.c.esquema_id:`** donde ya vive el de
  los rubros. **Decidido para este plan:** un `if` independiente con su propio par
  catálogo/propio y su propio aviso, no fusionado con el de los rubros — el molde actual trata «los
  rubros divergen» como una condición atómica, y meter las dos fuentes de divergencia en un mismo
  dict comparado escondería cuál de las dos cambió.
- **D-08** — `calificacion_final` **con** segundo nivel es **error de R1**, no `ErrorModelo`: el
  curso con el defecto sigue cargando y se puede inspeccionar, y falla al validar.
- **D-09** — `calificacion_final` **sin** segundo nivel es **aviso**.
- **D-15** — restricción dura de nombres, ver la Tarea 1.
- **D-19** — fixtures por `deepcopy` de `CURSO_VALIDO` con los helpers `curso()`/`informe()` que ya
  existen (hoy en `test_validar.py:110-118`).

Fuera de este plan: **R2 y R3 no se tocan** —la Fase 10 las cerró y el segundo nivel no cambia lo
que aporta una meta a un rubro—, ni `src/modelo.py`, ni `src/generar.py` (el manifiesto es el plan
11-03), ni `src/render_docx.py`, ni `config/politicas.yaml` (D-10: la plantilla de exención ya dice
«promedio» y es la única frase que puede ser cierta), ni `src/grafo.py` (D-21), ni
`cursos/2026-2/38985-.../curso.yaml` (D-18).

<interfaces>
```python
# src/validar.py:106-122 — el canal de hallazgos
class _Validador:
    def __init__(self, curso, cfg, cal): self.c = curso; self.cfg = cfg; ...
    def error(self, regla: str, mensaje: str) -> None: ...
    def aviso(self, regla: str, mensaje: str) -> None: ...

# src/validar.py:126-169 — regla_1 tal como está hoy. Las variables ya en ámbito en el punto
# de inserción (justo después de :152, justo antes del comentario de :154):
#   reglas   = self.cfg.esquemas["reglas"]          (:127)
#   exacta   = reglas["suma_exacta"]                (:133)  → el 100 del proyecto
#   ex       = self.c.exencion_ordinario            (:145)
# Y dentro del `if self.c.esquema_id:` (:155-169):
#   esquema  = self.cfg.esquema(self.c.esquema_id)  (:157)  → el dict del catálogo
#   catalogo / propio                               (:161-162) → los dos dicts de rubros

# src/modelo.py, tras el plan 11-01
@dataclass
class Nivel:
    porcentaje: float
    etiqueta: str

@dataclass
class SegundoNivel:
    promedio: Nivel
    ordinario: Nivel

# en Curso
segundo_nivel: SegundoNivel | None = None   # None = un solo nivel
exencion_contra: str = ""                   # ausente = "promedio"
EXENCION_CONTRA = ("promedio", "calificacion_final")

# El bloque del catálogo, tras el plan 11-01 (config/esquemas-evaluacion.yaml, zra-contabilidad)
#   segundo_nivel:
#     promedio:  {porcentaje: 60, etiqueta: "Valor del promedio antes del Examen Ordinario"}
#     ordinario: {porcentaje: 40, etiqueta: "Valor del examen Ordinario"}

# pruebas/test_validar.py — helpers disponibles
CURSO_VALIDO, curso(**cambios), informe(**cambios), reglas_con_error(inf)
validar.ERROR / validar.AVISO / validar.RECORDATORIO
Informe.hallazgos, .errores, .valido, .de(nivel), .texto()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: R1 comprueba el segundo nivel, dentro de `regla_1`</name>

  <files>src/validar.py</files>

  <read_first>
    - `src/validar.py:126-169` — **`regla_1` entera, línea a línea, antes de escribir nada.** Las
      dos salidas tempranas (`:130` y `:160`), la variable `exacta` (`:133`), la variable `ex`
      (`:145`), el comentario que abre el bloque del catálogo (`:154`) y el contraste de rubros
      (`:161-169`), que es el molde del contraste nuevo.
    - `pruebas/test_validar.py:287-292` — la guarda `getsource`, transcrita. **Léela antes de
      escribir el código, no después:** busca cinco substrings en el texto completo del fuente de
      `regla_1`, comentarios y docstring incluidos.
    - `pruebas/test_validar.py:281-285` —
      `test_poner_un_rubro_en_puntos_no_altera_lo_que_r1_comprueba`. Compara los mensajes de R1
      con y sin rubros en puntos: si algo del código nuevo leyera la unidad de un rubro, se vería
      aquí como cambio de mensajes.
    - `src/modelo.py` tras el plan 11-01 — `Nivel`, `SegundoNivel` y los dos campos de `Curso`.
    - `.planning/phases/11-segundo-nivel-de-la-calificacion/11-RESEARCH.md` §«La guarda
      `getsource`» y §«Materia prima para el criterio de Claude» — los mensajes reales de R1 hoy,
      verbatim, y el contraste de cada nombre propuesto contra la lista de tokens prohibidos.
  </read_first>

  <behavior>
    Lo que la Tarea 2 va a fijar con pruebas. Con `esquema_id` vacío, sobre el curso base:

    - 60/40 + `exencion_contra: promedio` → **cero** hallazgos de R1; el curso es válido.
    - 60/30 + `promedio` → **error** de R1 cuyo mensaje contiene `90` y `100`.
    - 100/0 + `promedio` → **un** aviso de R1 que contiene `no hay segundo nivel`; cero errores;
      curso válido.
    - 0/100 + `promedio` → **un** aviso de R1 que contiene `Art. 68`; cero errores; curso válido.
    - 60/40 + `calificacion_final` → **error** de R1 cuyo mensaje contiene `calificación final`,
      `promedio`, `80` (el umbral del curso base) y `Art. 68`; el curso no es válido.
    - sin segundo nivel + `calificacion_final` → **un** aviso de R1 que contiene
      `calificación final`; cero errores; curso válido.
    - 60/40 + `esquema_id: estandar-2026` (que no declara segundo nivel) → **un** aviso de R1 que
      contiene `estandar-2026` y `segundo nivel`.
    - Los rubros, la exención y el segundo nivel de `zra-contabilidad` copiados tal cual del
      catálogo → **cero** hallazgos de R1.
  </behavior>

  <action>
**Antes de escribir una línea, interioriza la trampa.** La guarda de la Fase 10 hace
`inspect.getsource(validar._Validador.regla_1)` y afirma que ese texto **no contiene** ninguno de
estos cinco substrings: `.base`, `a_porcentaje`, `.unidad`, `.total`, `.valor`. `getsource`
devuelve el texto completo, así que **un comentario los rompe igual que el código**. La frase «a
diferencia de R2, aquí no hace falta convertir con `a_porcentaje` porque el segundo nivel ya está
en porcentaje» es cierta, es útil… y rompe la prueba. No la escribas, ni ninguna variante.
Detalle medido: la palabra `total` **suelta** (sin punto delante) es segura en cualquier parte —el
token vigilado es `.total`, con punto—, pero para no tener que pensarlo, el bloque nuevo usa
`suma` como nombre de variable.

**1. Las comprobaciones del segundo nivel.** Van **exactamente** entre el cierre del bloque del
umbral de exención (la línea `)` que cierra el `self.error(...)` de `:152`) y el comentario
`# Si se declaró un esquema del catálogo, debe coincidir con lo capturado.` de `:154`. Es después
de los rubros duplicados y antes del bloque de `esquema_id`, que es lo que D-11 exige, y queda
pegado a la comprobación del umbral de exención, que es la decisión hermana:

```python
        # La calificación puede tener dos niveles: el promedio del curso y el examen
        # ordinario (REQ-41). Un curso que no los declara califica a uno solo —el promedio
        # es la calificación— y no recibe ninguna de estas comprobaciones.
        sn = self.c.segundo_nivel
        contra = self.c.exencion_contra or "promedio"
        if sn is not None:
            suma = round(sn.promedio.porcentaje + sn.ordinario.porcentaje, 2)
            if suma != exacta:
                self.error(
                    "R1",
                    f"El segundo nivel suma {suma:g}, no {exacta}: "
                    f"{sn.promedio.etiqueta} {sn.promedio.porcentaje:g} + "
                    f"{sn.ordinario.etiqueta} {sn.ordinario.porcentaje:g}.",
                )
            elif sn.ordinario.porcentaje == 0:
                self.aviso(
                    "R1",
                    f"El segundo nivel deja el examen ordinario en 0 y el promedio en "
                    f"{sn.promedio.porcentaje:g}: es decir que no hay segundo nivel. Si el "
                    f"ordinario no pesa, quita `segundo_nivel` en vez de declararlo en cero.",
                )
            elif sn.promedio.porcentaje == 0:
                self.aviso(
                    "R1",
                    f"El segundo nivel deja el promedio del curso en 0 y el examen ordinario "
                    f"en {sn.ordinario.porcentaje:g}: nada de lo que el alumno haga durante el "
                    f"ciclo cuenta para su calificación. El Art. 68 pide evaluación permanente.",
                )
            if contra == "calificacion_final":
                self.error(
                    "R1",
                    f"La exención ({ex}) está declarada contra la calificación final, pero el "
                    f"curso califica a dos niveles: el promedio pesa "
                    f"{sn.promedio.porcentaje:g} % y el examen ordinario "
                    f"{sn.ordinario.porcentaje:g} %. Medido contra el promedio, el umbral se "
                    f"alcanza antes de presentar el ordinario —que es justo de lo que exenta—; "
                    f"medido contra la calificación final habría que presentarlo para poder "
                    f"llegar a él. Declara `exencion_contra: promedio` (Art. 68).",
                )
        elif contra == "calificacion_final":
            self.aviso(
                "R1",
                f"La exención ({ex}) está declarada contra la calificación final y el curso no "
                f"declara `segundo_nivel`: hoy el promedio es la calificación final, así que da "
                f"lo mismo. Dejará de darlo el día que el curso declare los dos niveles; "
                f"escríbelo como `exencion_contra: promedio` (Art. 68).",
            )
```

Tres detalles que no son adorno:

- **`elif` y no tres `if` sueltos** en los tres primeros: los avisos de 100/0 y 0/100 solo tienen
  sentido cuando la suma es correcta, que es lo que dice D-13 («suma 100, así que no hay defecto
  aritmético»). Con `if` independientes, un 60/0 recibiría el error de la suma **y** el aviso de
  «no hay segundo nivel», que ahí no explica nada.
- **`exacta` y `ex` ya existen**: no las vuelvas a leer del config ni las renombres. `exacta` es
  `reglas["suma_exacta"]` (D-12: un solo sitio donde vive el 100 del proyecto) y `ex` es
  `self.c.exencion_ordinario`.
- **`contra` se calcula con `or "promedio"`** porque el campo no tiene default (D-07): la cadena
  vacía significa «no declarado», y no declararlo significa `promedio`.

**2. El contraste contra el catálogo.** Va **dentro del `if self.c.esquema_id:`**, justo después
del `if catalogo != propio:` de los rubros (es decir, después de la línea 169, al final del
método). `sn` ya está en ámbito porque el bloque anterior corre siempre:

```python
            nivel_catalogo = esquema.get("segundo_nivel")
            nivel_propio = None
            if sn is not None:
                nivel_propio = {
                    "promedio": {
                        "porcentaje": sn.promedio.porcentaje,
                        "etiqueta": sn.promedio.etiqueta,
                    },
                    "ordinario": {
                        "porcentaje": sn.ordinario.porcentaje,
                        "etiqueta": sn.ordinario.etiqueta,
                    },
                }
            if nivel_catalogo != nivel_propio:
                self.aviso(
                    "R1",
                    f"El segundo nivel del curso no coincide con el esquema "
                    f"«{self.c.esquema_id}» del catálogo ({nivel_catalogo} contra "
                    f"{nivel_propio}). Si el cambio es intencional, quita `esquema_id` o "
                    f"registra un esquema nuevo.",
                )
```

Se compara dict contra dict, como el contraste de rubros: si los dos son `None` —el caso de 39056
y 39062, cuyos esquemas no declaran segundo nivel— la condición es falsa y R1 calla, que es lo que
REQ-48 exige. El dict propio se construye a mano con **exactamente** las dos claves por nivel que
el catálogo declara; una clave de más y la comparación divergiría siempre.

**Consecuencia medida y aceptada (D-18):** con esto,
`cursos/2026-2/38985-contabilidad-financiera/curso.yaml` —que declara `esquema_id:
zra-contabilidad` y **no** declara segundo nivel— pasa de **9 hallazgos a 10**: un aviso de R1 por
diferir del catálogo. Sigue siendo válido. **No lo arregles y no toques ese archivo:** está
blindado hasta la Fase 14 (D-13 de la Fase 10) y el aviso dice la verdad —ese `curso.yaml` está
traducido y le falta el segundo nivel—. Es el recordatorio automático de la deuda que la Fase 14
cierra.

**No toques nada más de `src/validar.py`.** `regla_2`, `regla_3`, el resto de las reglas y
`Validador.estilo` quedan intactos. Si al terminar `git diff src/validar.py` muestra un cambio
fuera de `regla_1`, revísalo.
  </action>

  <acceptance_criteria>
    - `src/validar.py` contiene `sn = self.c.segundo_nivel` y
      `contra = self.c.exencion_contra or "promedio"`, ambos **dentro de `regla_1`**.
    - `src/validar.py` contiene `nivel_catalogo = esquema.get("segundo_nivel")`.
    - `src/validar.py` **no** contiene ninguna función ni método nuevo: `git diff src/validar.py`
      no añade ninguna línea que empiece por `    def `.
    - **La guarda textual, comprobada de dos formas.** Este comando imprime exactamente `[]`:
      `python -X utf8 -c "import sys;sys.path.insert(0,'src');import inspect,validar;f=inspect.getsource(validar._Validador.regla_1);print([t for t in ('.base','a_porcentaje','.unidad','.total','.valor') if t in f])"`
      y `python -X utf8 -m unittest pruebas.test_validar.Regla1EsInsensibleALaUnidad -v` sale con
      código 0 con sus cuatro pruebas.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y sigue reportando 251
      pruebas: esta tarea no añade ninguna y **no rompe ninguna**, en particular
      `test_poner_un_rubro_en_puntos_no_altera_lo_que_r1_comprueba`.
    - `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sigue reportando
      **5 hallazgos** y `VÁLIDO`, sin ninguna línea de `R1`. Ídem
      `39062-patrones-de-comportamiento`.
    - `python -X utf8 src/validar.py cursos/2026-2/38985-contabilidad-financiera/curso.yaml`
      reporta **10 hallazgos** —uno más que antes, un aviso de `R1` que menciona
      `zra-contabilidad`— y sigue diciendo `VÁLIDO`.
    - `git diff --name-only` lista exactamente `src/validar.py`. `git status --porcelain cursos/`
      sale vacío.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
R1 comprueba el segundo nivel donde tiene que comprobarlo —dentro de `regla_1`, antes de la salida
temprana del `esquema_id`— y sin sacar un solo cálculo a un método auxiliar, así que la guarda de
la Fase 10 sigue cubriéndolo todo.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: las ocho pruebas del segundo nivel en R1</name>

  <files>pruebas/test_validar.py</files>

  <read_first>
    - `pruebas/test_validar.py:60-118` — `CURSO_VALIDO` y los helpers `curso()`, `informe()`,
      `reglas_con_error()`. **Los helpers viven hoy en `:110-118`**, no en `:104-112` como cita
      `11-CONTEXT.md`.
    - `pruebas/test_validar.py:228-256` — `Regla1Porcentajes`: el molde de las pruebas de R1, con
      su patrón `ev = copy.deepcopy(CURSO_VALIDO["evaluacion"])` + `informe(evaluacion=ev)`.
    - `pruebas/test_validar.py:258-292` — `Regla1EsInsensibleALaUnidad`, la clase junto a la que
      va la nueva, y la guarda `getsource` que la cierra.
    - `src/validar.py` tras la Tarea 1 — los cinco mensajes nuevos, para assertar sobre las
      subcadenas que de verdad se escribieron.
    - `config/esquemas-evaluacion.yaml:59-88` — el bloque `zra-contabilidad` con su
      `segundo_nivel:` ya declarado por el plan 11-01: sus tres rubros (30/50/20), su
      `exencion_ordinario: 90` y sus dos rótulos literales.
  </read_first>

  <action>
Añade a `pruebas/test_validar.py` una clase nueva **inmediatamente después de
`Regla1EsInsensibleALaUnidad` (termina en la línea 292) y antes de `class Regla2Metas`**:

```python
class Regla1SegundoNivel(unittest.TestCase):
    """REQ-46: con segundo nivel, R1 comprueba que los dos porcentajes sumen 100 y que la
    exención esté declarada contra el promedio, no contra la calificación final.

    Los fixtures son variantes de `CURSO_VALIDO` por `deepcopy` (D-19). `esquema_id` se vacía
    por omisión: el contraste contra el catálogo es otra comprobación y tiene sus dos pruebas
    propias al final, así que no debe meter avisos en las demás.
    """

    NIVEL_60_40 = {
        "promedio": {"porcentaje": 60,
                     "etiqueta": "Valor del promedio antes del Examen Ordinario"},
        "ordinario": {"porcentaje": 40,
                      "etiqueta": "Valor del examen Ordinario"},
    }

    def _informe(self, nivel=None, contra=None, esquema_id=""):
        ev = copy.deepcopy(CURSO_VALIDO["evaluacion"])
        ev["esquema_id"] = esquema_id
        if nivel is not None:
            ev["segundo_nivel"] = copy.deepcopy(nivel)
        if contra is not None:
            ev["exencion_contra"] = contra
        return informe(evaluacion=ev)

    def _r1(self, inf, nivel=None):
        hallazgos = inf.hallazgos if nivel is None else inf.de(nivel)
        return [h.mensaje for h in hallazgos if h.regla == "R1"]

    # -- la suma de los dos niveles -----------------------------------------

    def test_un_segundo_nivel_de_60_40_no_produce_ningun_hallazgo_de_r1(self):
        """Criterio 1 del roadmap, la mitad que valida."""
        inf = self._informe(self.NIVEL_60_40, contra="promedio")
        self.assertEqual([], self._r1(inf))
        self.assertTrue(inf.valido, "\n".join(str(h) for h in inf.errores))

    def test_un_segundo_nivel_de_60_30_es_error_de_r1(self):
        """Criterio 1 del roadmap, la mitad que falla: 60 y 30 no suman 100. El ordinario no se
        deriva del promedio (D-04) justamente para que este curso sea expresable."""
        nivel = copy.deepcopy(self.NIVEL_60_40)
        nivel["ordinario"]["porcentaje"] = 30
        inf = self._informe(nivel, contra="promedio")
        self.assertIn("R1", reglas_con_error(inf))
        mensajes = " ".join(self._r1(inf, validar.ERROR))
        self.assertIn("90", mensajes)
        self.assertIn("100", mensajes)
        self.assertFalse(inf.valido)

    def test_un_segundo_nivel_de_100_0_es_aviso(self):
        """Suma 100, así que no hay defecto aritmético: declarar 100/0 es decir «no hay segundo
        nivel» por el camino largo, y puede ser deliberado (D-13)."""
        nivel = copy.deepcopy(self.NIVEL_60_40)
        nivel["promedio"]["porcentaje"] = 100
        nivel["ordinario"]["porcentaje"] = 0
        inf = self._informe(nivel, contra="promedio")
        self.assertEqual([], self._r1(inf, validar.ERROR))
        avisos = self._r1(inf, validar.AVISO)
        self.assertEqual(1, len(avisos))
        self.assertIn("no hay segundo nivel", avisos[0])
        self.assertTrue(inf.valido)

    def test_un_segundo_nivel_de_0_100_es_aviso(self):
        """El otro extremo: las metas del curso no cuentan para nada y la calificación es el
        examen ordinario."""
        nivel = copy.deepcopy(self.NIVEL_60_40)
        nivel["promedio"]["porcentaje"] = 0
        nivel["ordinario"]["porcentaje"] = 100
        inf = self._informe(nivel, contra="promedio")
        self.assertEqual([], self._r1(inf, validar.ERROR))
        avisos = self._r1(inf, validar.AVISO)
        self.assertEqual(1, len(avisos))
        self.assertIn("Art. 68", avisos[0])
        self.assertTrue(inf.valido)

    # -- contra qué se mide la exención -------------------------------------

    def test_la_exencion_contra_la_calificacion_final_con_segundo_nivel_es_error(self):
        """Criterio 3 del roadmap: rechazado por R1 —no al cargar (D-08)— con un mensaje que
        explica la diferencia entre medir contra el promedio y medir contra la nota final."""
        inf = self._informe(self.NIVEL_60_40, contra="calificacion_final")
        self.assertIn("R1", reglas_con_error(inf))
        mensaje = " ".join(self._r1(inf, validar.ERROR))
        self.assertIn("calificación final", mensaje)
        self.assertIn("promedio", mensaje)
        self.assertIn("80", mensaje)          # el umbral que declara el curso base
        self.assertIn("Art. 68", mensaje)
        self.assertFalse(inf.valido)

    def test_la_exencion_contra_la_calificacion_final_sin_segundo_nivel_es_aviso(self):
        """D-09: aquí el promedio *es* la calificación final, así que aritméticamente no está
        mal; solo está dicho de una forma que se volverá falsa."""
        inf = self._informe(contra="calificacion_final")
        self.assertEqual([], self._r1(inf, validar.ERROR))
        avisos = self._r1(inf, validar.AVISO)
        self.assertEqual(1, len(avisos))
        self.assertIn("calificación final", avisos[0])
        self.assertTrue(inf.valido)

    # -- el contraste contra el catálogo ------------------------------------

    def test_avisa_si_el_segundo_nivel_se_aparta_del_catalogo(self):
        """`estandar-2026` no declara segundo nivel: declararlo en el curso es divergir (D-14)."""
        inf = self._informe(self.NIVEL_60_40, contra="promedio", esquema_id="estandar-2026")
        avisos = self._r1(inf, validar.AVISO)
        self.assertEqual(1, len(avisos))
        self.assertIn("estandar-2026", avisos[0])
        self.assertIn("segundo nivel", avisos[0])
        self.assertTrue(inf.valido)

    def test_el_segundo_nivel_del_catalogo_no_produce_ningun_aviso(self):
        """La guarda de los rótulos literales (D-05). Si alguien «corrige» la minúscula de
        «Valor del examen Ordinario» en el catálogo, esta prueba se rompe: el `curso.yaml` de la
        Fase 14 declarará el rótulo tal como está en el DI de origen, y el contraste de D-14
        dejaría de coincidir."""
        ev = copy.deepcopy(CURSO_VALIDO["evaluacion"])
        ev["esquema_id"] = "zra-contabilidad"
        ev["rubros"] = copy.deepcopy(modelo.Config().esquema("zra-contabilidad")["rubros"])
        ev["exencion_ordinario"] = 90
        ev["segundo_nivel"] = copy.deepcopy(self.NIVEL_60_40)
        ev["exencion_contra"] = "promedio"
        inf = informe(evaluacion=ev)
        self.assertEqual([], [h.mensaje for h in inf.hallazgos if h.regla == "R1"])
```

Dos avisos sobre esta clase, para que no te desconcierten al correrla:

- **La última prueba deja el curso con errores de R2** —las metas siguen imputadas a `tareas` y
  `proyecto`, que no existen en los rubros de Zurisaddai— y eso está bien: la prueba filtra por
  `h.regla == "R1"` y solo afirma cosas de R1. **No arregles las metas ni añadas rubros para
  «dejar el curso limpio»**: cambiar el fixture le quitaría el sentido, que es contrastar los
  rubros y el segundo nivel del catálogo contra sí mismos.
- **`NIVEL_60_40` lleva los rótulos literales escritos a mano**, con la «e» minúscula de «Valor
  del examen Ordinario». Es deliberado: son la copia independiente contra la que se contrasta el
  catálogo. Si los dos no coinciden, uno de los dos está mal transcrito y hay que ir al DI de
  origen (`conocimiento/ejemplos/531-contabilidad-financiera-2026-1.md:61-65`), no «igualarlos».

Si alguna prueba falla, el defecto está en la Tarea 1: la lista de `<behavior>` de esa tarea es
esta misma. No debilites una afirmación para que pase.
  </action>

  <acceptance_criteria>
    - `pruebas/test_validar.py` contiene `class Regla1SegundoNivel(unittest.TestCase):` con
      **ocho** métodos `def test_`.
    - `pruebas/test_validar.py` contiene la cadena literal
      `"etiqueta": "Valor del examen Ordinario"` con la «e» de «examen» en minúscula, y la cadena
      `Valor del promedio antes del Examen Ordinario`.
    - `pruebas/test_validar.py` contiene `modelo.Config().esquema("zra-contabilidad")` y **no**
      contiene `38985`.
    - `python -X utf8 -m unittest pruebas.test_validar.Regla1SegundoNivel -v` sale con código 0 y
      corre 8 pruebas.
    - `python -X utf8 -m unittest discover -s pruebas` sale con código 0 y reporta **259** pruebas
      (251 + 8).
    - `git diff --name-only` no lista ningún archivo de `src/` en esta tarea.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
Los ocho comportamientos de R1 sobre el segundo nivel están fijados, incluida la guarda que
denunciaría un rótulo «corregido» en el catálogo.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: las dos tablas de las ocho reglas, al día</name>

  <files>AGENTS.md, .claude/skills/di-validar/SKILL.md</files>

  <read_first>
    - `AGENTS.md:168-206` — §«Las ocho reglas de validación (`src/validar.py`)»: la tabla, los
      tres niveles de hallazgo, el guardia `ESTILO` y los dos párrafos sobre R2. La fila de R1
      está en `:172`.
    - `.claude/skills/di-validar/SKILL.md:30-45` — la tabla gemela de las ocho reglas. Compara su
      fila de R2 (`:35`) y de R3 (`:36`) con las de `AGENTS.md:173-174`: **están desincronizadas
      desde la Fase 10**, que amplió R2 a los componentes y a la unidad del rubro y R3 a los
      exámenes declarados como componente.
    - `.claude/skills/di-validar/SKILL.md` completo — para confirmar lo que dice
      `11-RESEARCH.md` §Riesgos 3: el skill **invoca `src/validar.py` como subproceso** y solo
      reporta lo que sale, así que **no necesita ningún cambio funcional**; las comprobaciones
      nuevas de R1 le llegan solas.
  </read_first>

  <action>
Es requisito de `11-CONTEXT.md` §canonical_refs que §«Las ocho reglas de validación» se actualice
**en el mismo plan que cambia el comportamiento de R1**, y este es ese plan.

**1. `AGENTS.md`, la fila de R1** (`:172`). Hoy dice:

```
| R1 | Los porcentajes del esquema suman exactamente 100; la exención cae en [60, 100]. | Arts. 65 y 67 |
```

Sustitúyela por:

```
| R1 | Los porcentajes del esquema suman exactamente 100 y la exención cae en [60, 100]. Si el curso declara un segundo nivel, el promedio y el examen ordinario también suman 100, y el umbral de exención tiene que medirse contra el promedio: contra la calificación final es error. | Arts. 65, 67 y 68 |
```

**2. `AGENTS.md`, el párrafo del segundo nivel.** Añádelo **después** del párrafo que empieza
«Desde la Fase 10, R2 lee `Curso.aportes()`…» (`:200-206`), como cierre de la sección:

```
Desde la Fase 11, R1 sabe que la calificación puede tener **dos niveles**. `segundo_nivel:`
declara cuánto pesa el promedio del curso y cuánto el examen ordinario —par fijo, con la
`etiqueta` de cada uno, que el generador imprime y no redacta— y `exencion_contra:` dice contra
cuál de los dos se mide el umbral de exención. Un curso que no declara `segundo_nivel` califica
a un nivel —el promedio *es* la calificación— y no recibe ninguna comprobación nueva. Con
segundo nivel: los dos porcentajes suman 100 o es error; 100/0 y 0/100 son aviso, porque suman
pero dicen algo raro; la exención contra la **calificación final** es **error** con un mensaje
que explica la diferencia (Art. 68); y si el curso declara `esquema_id`, su segundo nivel se
contrasta —porcentajes **y** etiquetas— contra el del catálogo, con aviso. Ese contraste es más
estricto que el de los rubros a propósito: para el segundo nivel, el catálogo es también la
redacción canónica del esquema.
```

**3. `.claude/skills/di-validar/SKILL.md`, la tabla resumen.** Sustituye sus filas de R1, R2 y R3
(`:34-36`) por:

```
| R1 | Los porcentajes del esquema suman exactamente 100 y la exención cae en [60, 100]. Con segundo nivel, el promedio y el examen ordinario también suman 100 y la exención se mide contra el promedio. | Arts. 65, 67 y 68 |
| R2 | Todo aporte a un rubro —la meta y cada uno de sus componentes— suma lo que ese rubro declara, en la unidad de ese rubro y rubro por rubro, no solo en total. | Art. 67 |
| R3 | Hay al menos dos exámenes parciales, se declaren como meta propia o como componente de la actividad de otra meta. | Art. 68 |
```

**Por qué esta tercera edición entra en este plan aunque `11-CONTEXT.md` no la pida:** esa tabla
es un duplicado literal de la de `AGENTS.md`, es la que lee el usuario cuando corre `/di-validar`,
y sus filas de R2 y R3 llevan describiendo mal el validador **desde el cierre de la Fase 10**. El
plan que reescribe la fila de R1 es el único momento en que las dos tablas están abiertas a la
vez; dejarla para después significa dejarla. El riesgo es nulo: `di-validar` no cambia de
comportamiento —invoca `src/validar.py` y reporta su salida—, así que esto es corrección
documental, no funcional. **No toques nada más del skill**: ni su sección «Cómo leer el informe»,
ni «Al corregir», ni el comando que invoca.

**Nada de código en esta tarea.** `git diff src/` y `git diff pruebas/` tienen que salir vacíos.
  </action>

  <acceptance_criteria>
    - `AGENTS.md` contiene `Arts. 65, 67 y 68` y la cadena `Desde la Fase 11, R1 sabe que la
      calificación puede tener`.
    - `AGENTS.md` contiene `exencion_contra:` dentro de §«Las ocho reglas de validación».
    - `.claude/skills/di-validar/SKILL.md` contiene `Con segundo nivel` y
      `en la unidad de ese rubro`; **no** contiene ya la cadena
      `Hay al menos dos exámenes parciales. |`.
    - Ni `AGENTS.md` ni el skill mencionan una regla R9: siguen siendo **ocho** reglas, y las dos
      tablas tienen exactamente ocho filas de regla.
    - `git diff --name-only` en esta tarea lista exactamente `AGENTS.md` y
      `.claude/skills/di-validar/SKILL.md`.
    - `python -X utf8 -m unittest discover -s pruebas` sigue saliendo con código 0 y 259 pruebas.
  </acceptance_criteria>

  <verify>
    <automated>python -X utf8 -m unittest discover -s pruebas</automated>
  </verify>

  <done>
Las dos tablas de las ocho reglas describen lo que el validador hace de verdad, incluida la deuda
documental que la Fase 10 dejó abierta en el skill.
  </done>
</task>

</tasks>

<verification>
1. `python -X utf8 -m unittest discover -s pruebas` sale 0 con 259 pruebas.
2. `python -X utf8 -m unittest pruebas.test_validar.Regla1SegundoNivel -v` sale 0 con 8 pruebas.
3. `python -X utf8 -m unittest pruebas.test_validar.Regla1EsInsensibleALaUnidad -v` sale 0: la
   guarda `getsource` de la Fase 10 sigue en verde y sigue cubriendo todo el código nuevo.
4. `python -X utf8 -c "import sys;sys.path.insert(0,'src');import inspect,validar;f=inspect.getsource(validar._Validador.regla_1);print([t for t in ('.base','a_porcentaje','.unidad','.total','.valor') if t in f])"`
   imprime `[]`.
5. `python -X utf8 src/validar.py cursos/2026-2/39056-big-data/curso.yaml` sale 0, 5 hallazgos,
   ninguno de R1. Ídem `39062-patrones-de-comportamiento`.
6. `python -X utf8 src/validar.py cursos/2026-2/38985-contabilidad-financiera/curso.yaml` sale 0,
   **10** hallazgos, uno de R1 y de nivel aviso, `VÁLIDO`.
7. `python src/plantillas.py verificar` sale 0.
8. `git status --porcelain cursos/` sale vacío. `git diff --name-only` lista exactamente
   `src/validar.py`, `pruebas/test_validar.py`, `AGENTS.md` y
   `.claude/skills/di-validar/SKILL.md`.
</verification>

<success_criteria>
- REQ-46 cumplido: con segundo nivel, R1 comprueba la suma de los dos porcentajes y contra qué se
  mide la exención.
- Criterio 1 del roadmap: 60/40 valida, 60/30 es error de R1 con el desglose.
- Criterio 3 del roadmap: `exencion_contra: calificacion_final` con segundo nivel es rechazado por
  R1 con un mensaje que explica la diferencia; la exención de 90 de `zra` se contrasta contra el
  promedio, que es lo que dice su DI.
- La guarda `getsource` de la Fase 10 sigue cubriendo el código nuevo: cero tokens prohibidos y
  ningún método auxiliar.
- 39056 y 39062 no ganaron ni perdieron un hallazgo. 38985 ganó el aviso que D-18 mide y acepta,
  y su `curso.yaml` no se tocó.
- R2, R3, `src/modelo.py`, `src/generar.py`, `src/render_docx.py`, `src/grafo.py` y
  `config/politicas.yaml` intactos.
</success_criteria>

<output>
Al terminar, escribe
`.planning/phases/11-segundo-nivel-de-la-calificacion/11-02-SUMMARY.md`, transcribiendo los cinco
mensajes nuevos de R1 tal como quedaron redactados, la salida literal de la comprobación de la
guarda `getsource`, y el conteo de hallazgos de los tres cursos (5, 5 y 10) con la confirmación de
que 38985 sigue siendo válido.
</output>
