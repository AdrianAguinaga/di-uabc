# Generador de Diseño Instruccional UABC
python-docx>=1.2.0    # rellenar las plantillas CIAD reales
pdfplumber>=0.11      # reconstruir la §VI del PUA (tabla de prácticas)
PyYAML>=6.0           # curso.yaml, calendarios, config
openpyxl>=3.1         # leer la rúbrica IEDI
pywin32>=306          # Word COM para exportar a PDF (solo Windows)
